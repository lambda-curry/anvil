# Rule Candidates — lambda-curry/saffron-atlas
*Mined: 2026-07-05 · PRs analyzed: 86 · Comments reviewed: 48 · Candidates surfaced: 5*

---

## Theme: Naming (20 comments, score 13.3)

### Candidate: Use descriptive, convention-aligned names for variables, functions, and classes
*Frequency: 20 · Severity signals: medium · Representativeness: high*

**Supporting comments:**
- PR #86: "_⚠️ Potential issue_ | _🟡 Minor_ **Resolve charter-model contradiction on the same page.** Lines 30-32, 47-48, and 64 switch to an “active charter set” model, but later copy still says Podcast Platform is outside Scout’s “four standing loops.” This creates conflicting guidance for readers. <details> <summary>💡 Suggested copy alignment</summary> ```diff - <p className="text-sm text-slate-300">A direct Scout-owned product charter for a multi-show RSS feed platform. It sits outside Scout&apos;s four standing loops, but it is now part of Scout&apos;s current operating model: when a new product needs focused execution, Scout can pick it up without pretending it is another permanent maintenance loop.</p> + <p className="text-sm text-slate-300">A direct Scout-owned product charter for a multi-show RSS feed platform. It sits alongside Scout&apos;s standing loops as part of Scout&apos;s active charter rotation: when a new product needs focused execution, Scout can carry it without pretending it is a permanent maintenance loop.</p> ``` </details> Also applies to: 47-48, 64-64 <details> <summary>🤖 Prompt for AI Agents</summary> ``` Verify each finding against the current code and only fix it if needed. In `@apps/network-viz/app/routes/agent/scout.tsx` around lines 30 - 32, The page copy for the Scout route is inconsistent: some sections describe an “active charter set” model while other lines say Podcast Platform is outside Scout’s “four standing loops”; reconcile these by choosing one model and updating all mentions accordingly—either (A) adjust the text around “Scout is the always-on autonomous agent…” and the later lines referencing the “four standing loops” to state that Scout manages both the four standing loops (Watchtower, Atlas, Anvil, etc.) and an active charter set (including Podcast Platform) or (B) remove the “active charter set” language and make all references assert that Podcast Platform is separate from the four standing loops; update every occurrence of the phrases “active charter set”, “four standing loops”, and “Podcast Platform” on this page to match the chosen model so the narrative is consistent. ``` </details> <!-- fingerprinting:phantom:poseidon:hawk --> <!-- This is an auto-generated comment by CodeRabbit -->"
- PR #85: "_⚠️ Potential issue_ | _🟡 Minor_ **Rename the section heading to match the new charter-loop taxonomy.** Line 512 still says **“Scout Sub-Projects”** while this block now describes **standing charter loops**. Keeping both terms together can blur the distinction this PR is trying to sharpen. <details> <summary>✏️ Suggested copy fix</summary> ```diff - <span>🔭</span> Scout Sub-Projects + <span>🔭</span> Scout Charter Loops ``` </details> <!-- suggestion_start --> <details> <summary>📝 Committable suggestion</summary> > ‼️ **IMPORTANT** > Carefully review the code before committing. Ensure that it accurately replaces the highlighted code, contains no missing lines, and has no issues with indentation. Thoroughly test & benchmark the code to ensure it meets the requirements. ```suggestion <span>🔭</span> Scout Charter Loops </h3> <p className="text-slate-400 text-sm mb-5"> Scout keeps four standing charter loops on recurring cycles, each with its own workspace, progress log, and SCRATCHPAD. It can also pick up direct product charters like podcast-platform when a new build needs steady execution without changing that core four-project rhythm. ``` </details> <!-- suggestion_end --> <details> <summary>🤖 Prompt for AI Agents</summary> ``` Verify each finding against the current code and only fix it if needed. In `@apps/network-viz/app/routes/capabilities.tsx` around lines 512 - 517, The section heading text "Scout Sub-Projects" should be renamed to reflect the new taxonomy—replace that literal with "Standing Charter Loops" (or "Scout Standing Charter Loops" if you want the org name included) in the JSX heading element so the UI matches the description about standing charter loops and avoids mixing terms; update the string in the same h3 element that currently contains "<span>🔭</span> Scout Sub-Projects". ``` </details> <!-- fingerprinting:phantom:poseidon:hawk --> <!-- This is an auto-generated comment by CodeRabbit -->"
- PR #82: "_⚠️ Potential issue_ | _🟡 Minor_ **Sync page metadata with the new Reportory copy.** The body now says “Podcast Platform and Reportory,” but the meta description still mentions only Podcast Platform. This can cause stale search/share previews. <details> <summary>Suggested patch</summary> ```diff - 'The Saffron Network charter map: Scout product tools, recurring process charters run by Atticus, Clawdy, Grafton, Samwise, and Currybot, starter templates, and product-specific charters like Podcast Platform. What each is, who owns it, and how it works.', + 'The Saffron Network charter map: Scout product tools, recurring process charters run by Atticus, Clawdy, Grafton, Samwise, and Currybot, starter templates, and product-specific charters like Podcast Platform and Reportory. What each is, who owns it, and how it works.', ``` </details> <details> <summary>🤖 Prompt for AI Agents</summary> ``` Verify each finding against the current code and only fix it if needed. In `@apps/network-viz/app/routes/projects.tsx` around lines 94 - 95, The meta description for the Projects page is stale (mentions only "Podcast Platform") and must be updated to match the new body copy that includes "Reportory"; locate the metadata definition in the projects route (the exported meta function or the <meta name="description"> entry in apps/network-viz/app/routes/projects.tsx) and update its description string to include both "Podcast Platform and Reportory" (or mirror the exact sentence used in the page body) so search/share previews reflect the new copy. ``` </details> <!-- fingerprinting:phantom:poseidon:hawk --> <!-- This is an auto-generated comment by CodeRabbit -->"
- PR #82: "_⚠️ Potential issue_ | _🟡 Minor_ **Use a consistent badge color for `Active` status.** Reportory is listed as `Active`, but this list renders all product-charter statuses with amber. The detailed Reportory section uses green for `Active`, so the two sections currently disagree. <details> <summary>Suggested patch</summary> ```diff - <StatusBadge label={status} color="amber" /> + <StatusBadge label={status} color={status === 'Active' ? 'green' : 'amber'} /> ``` </details> <details> <summary>🤖 Prompt for AI Agents</summary> ``` Verify each finding against the current code and only fix it if needed. In `@apps/network-viz/app/routes/projects.tsx` around lines 365 - 370, The product-charters grid is rendering statuses with an amber badge for "Active", causing a mismatch with the detailed Reportory section; update the JSX that maps the array (the map callback iterating over { emoji, name, owner, tech, status, desc }) so the badge color logic uses green for status === 'Active' (same token used in the detailed Reportory card) while keeping amber for statuses like 'Kickoff'; adjust the conditional/className that derives the badge color from the status prop to return the green variant for 'Active' so Reportory's small-list badge matches the detailed view. ``` </details> <!-- fingerprinting:phantom:poseidon:hawk --> <!-- This is an auto-generated comment by CodeRabbit -->"
- [+16 more]

### Candidate Rule — Naming

**Why (failure mode):** _⚠️ Potential issue_ | _🟠 Major_ **Dormant status is clear here, but the page still reads as operational in downstream...

**The Rule:** Use descriptive, convention-aligned names for variables, functions, and classes. Favor consistency with existing project naming standards.

**✅ DO:** ```tsx
Don&apos;t try to memorize it — just notice that there are six active agents (Scout, Currybot, Atticus, Meg, Grafton, Samwise)
                <strong className="text-cyan-200">You&apos;ll know it worked when:</strong> you can identify one active operator (for example, Scout or Samwise),
                and point to Atticus as &ldquo;the policy check before big actions.&rdquo; (Clawdy is historical/dormant in current-state views.)
```

**❌ DON'T:** ```tsx
Don&apos;t try to memorize it — just notice that there are six agents (Clawdy, Scout, Currybot, Atticus, Meg, Grafton)
                <strong className="text-cyan-200">You&apos;ll know it worked when:</strong> you can point to Clawdy on the graph and say
                &ldquo;that&apos;s Jake&apos;s front door,&rdquo; and to Atticus and say &ldquo;that&apos;s the policy check before big actions.&rdquo;
```

**Scope:** alwaysApply: true

*Candidate rule — review before adopting. Frequency=20 in 86 PRs (23.3%). Evidence: 4 PR comments shown above.*

---

## Theme: Error Handling (7 comments, score 6.1)

### Candidate: Handle failure paths explicitly
*Frequency: 7 · Severity signals: medium · Representativeness: high*

**Supporting comments:**
- PR #80: "_⚠️ Potential issue_ | _🟠 Major_ **Decision-labeled paths currently bypass the decision layer.** Line 264 and Lines 564-575 frame this lane as **Decision**, but route users to `/capabilities` (District). That conflicts with the new decision-layer positioning and creates first-click routing ambiguity. <details> <summary>Suggested alignment fix</summary> ```diff @@ - route: '/capabilities', - cta: 'Open Capabilities', - path: 'Decision first, then Street if you need the exact workflow', + route: '/coordination', + cta: 'Open Coordination', + path: 'Decision first, then District or Street as needed', @@ - Skip the tour and go straight to Capabilities when your real question is who should handle this and how you should ask. + Skip the tour and go straight to Coordination when your real question is who should handle this and what the next handoff should be. @@ - to="/capabilities" + to="/coordination" @@ - Open Capabilities first + Open Coordination first ``` </details> Also applies to: 564-575 <details> <summary>🤖 Prompt for AI Agents</summary> ``` Verify each finding against the current code and only fix it if needed. In `@apps/network-viz/app/routes/home.tsx` around lines 261 - 266, The "Decision" lane objects are pointing to the District route '/capabilities', bypassing the decision layer; update the route property for any object where tag === 'Decision' (e.g., the object with summary 'Go straight to the ownership page...' and the similar object around lines 564-575) to the correct decision-layer route (replace '/capabilities' with the decision route your app uses, e.g., '/decision' or '/decisions') so clicking a Decision-tagged path navigates to the decision layer instead of the capabilities page. ``` </details> <!-- fingerprinting:phantom:poseidon:hawk --> <!-- This is an auto-generated comment by CodeRabbit --> ✅ Addressed in commit 8c57fe4"
- PR #60: "_⚠️ Potential issue_ | _🟡 Minor_ **Keep “Recently Updated” entries in strict reverse-chronological order.** Line 292 adds an Apr 13 entry, but it currently renders below older Apr 9 items. Since the UI maps in array order, this makes the recency feed misleading. <details> <summary>🤖 Prompt for AI Agents</summary> ``` Verify each finding against the current code and only fix it if needed. In `@apps/network-viz/app/routes/home.tsx` around lines 292 - 295, The "Recently Updated" list has its Apr 13 object (date: 'Apr 13', label: 'Freshness') placed after older Apr 9 entries so the UI (which renders array order) shows items out of reverse-chronological order; fix this by reordering the array in routes/home.tsx so the object with date: 'Apr 13' and label: 'Freshness' appears before the Apr 9 items (i.e., ensure the recently-updated array is strictly reverse-chronological) so the mapped output renders newest-first. ``` </details> <!-- fingerprinting:phantom:poseidon:hawk --> <!-- This is an auto-generated comment by CodeRabbit --> ✅ Addressed in commit daa68c2"
- PR #30: "_⚠️ Potential issue_ | _🔴 Critical_ <details> <summary>🧩 Analysis chain</summary> 🏁 Script executed: ```shell #!/bin/bash # Check if samwise is registered in the AGENT_PAGES mapping rg -n "samwise" apps/network-viz/app/routes/agent.\$agentId.tsx # Check for a dedicated samwise route file fd -t f "samwise" apps/network-viz/app/routes/ ``` Repository: lambda-curry/saffron-atlas Length of output: 52 --- 🏁 Script executed: ```shell # View the agent.$agentId.tsx file to see AGENT_PAGES and fallback handling head -n 50 apps/network-viz/app/routes/agent.\$agentId.tsx # Also check the color maps in home.tsx sed -n '205,219p' apps/network-viz/app/routes/home.tsx ``` Repository: lambda-curry/saffron-atlas Length of output: 2792 --- </details> **Missing route handler and color map entries for Samwise agent.** Two issues with this addition: 1. **Broken link**: The route `/agent/samwise` will render "Unknown Agent District" because `samwise` is not registered in `AGENT_PAGES` in `apps/network-viz/app/routes/agent.$agentId.tsx` (lines 11-17). 2. **Missing color**: `color: 'orange'` is used but `orange` is not defined in `agentColorMap` (lines 205-211) or `agentBadgeMap` (lines 213-219), causing the agent card styling to fail. <details> <summary>Proposed fix for color maps</summary> ```diff const agentColorMap: Record<string, string> = { purple: 'border-purple-400/50 bg-purple-500/10 hover:bg-purple-500/20 hover:border-purple-400/70', blue: 'border-blue-400/50 bg-blue-500/10 hover:bg-blue-500/20 hover:border-blue-400/70', emerald: 'border-emerald-400/50 bg-emerald-500/10 hover:bg-emerald-500/20 hover:border-emerald-400/70', amber: 'border-amber-400/50 bg-amber-500/10 hover:bg-amber-500/20 hover:border-amber-400/70', pink: 'border-pink-400/50 bg-pink-500/10 hover:bg-pink-500/20 hover:border-pink-400/70', + orange: 'border-orange-400/50 bg-orange-500/10 hover:bg-orange-500/20 hover:border-orange-400/70', }; const agentBadgeMap: Record<string, string> = { purple: 'bg-purple-500/20 text-purple-300', blue: 'bg-blue-500/20 text-blue-300', emerald: 'bg-emerald-500/20 text-emerald-300', amber: 'bg-amber-500/20 text-amber-300', pink: 'bg-pink-500/20 text-pink-300', + orange: 'bg-orange-500/20 text-orange-300', }; ``` </details> Also add `samwise` to the AGENT_PAGES mapping in `agent.$agentId.tsx` with its corresponding district and component imports. <details> <summary>🤖 Prompt for AI Agents</summary> ``` Verify each finding against the current code and only fix it if needed. In `@apps/network-viz/app/routes/home.tsx` around lines 120 - 128, Add the new agent entry 'samwise' to the AGENT_PAGES mapping used by the agent route handler (AGENT_PAGES in agent.$agentId.tsx), importing its component (e.g., Samwise page/component) and assigning the correct district string so /agent/samwise resolves instead of "Unknown Agent District"; also add the missing color key 'orange' to both agentColorMap and agentBadgeMap so the card styling uses the defined color (ensure the value matches the project's color token format and the badge variant used elsewhere). ``` </details> <!-- fingerprinting:phantom:poseidon:ocelot --> <!-- This is an auto-generated comment by CodeRabbit -->"
- PR #28: "_⚠️ Potential issue_ | _🟠 Major_ <details> <summary>🧩 Analysis chain</summary> 🏁 Script executed: ```shell #!/bin/bash # Check if route handlers exist for samwise streets (triage, execution) echo "=== Checking for samwise street route handlers ===" # Look for any files that might handle /agent/samwise/triage or /agent/samwise/execution fd -t f -e tsx -e ts . apps/network-viz/app/routes | xargs grep -l "samwise" 2>/dev/null || echo "No matches" # Check if there's a generic agent.$agentId.$detail.tsx handler fd -t f "agent.*detail" apps/network-viz/app/routes # Search for any street handling in agent routes rg -n "triage|execution" apps/network-viz/app/routes/agent --type ts --type tsx 2>/dev/null || rg -n "triage|execution" apps/network-viz/app/routes/agent 2>/dev/null || echo "No triage/execution handlers found" # Check for a catch-all or detail route rg -l '\$detail|\$streetId' apps/network-viz/app/routes/ 2>/dev/null || echo "No detail route found" ``` Repository: lambda-curry/saffron-atlas Length of output: 3010 --- 🏁 Script executed: ```shell cat -n apps/network-viz/app/routes/agent.$agentId.$detail.tsx ``` Repository: lambda-curry/saffron-atlas Length of output: 141 --- 🏁 Script executed: ```shell #!/bin/bash echo "=== Files in apps/network-viz/app/routes/agent/ ===" ls -la apps/network-viz/app/routes/agent/ 2>/dev/null || echo "Directory not found" echo "" echo "=== All route files in apps/network-viz/app/routes/ ===" fd -t f . apps/network-viz/app/routes/ | head -30 echo "" echo "=== Files with 'detail' in name ===" fd -t f "detail" apps/network-viz/app/routes/ ``` Repository: lambda-curry/saffron-atlas Length of output: 2244 --- 🏁 Script executed: ```shell head -100 "apps/network-viz/app/routes/agent.\$agentId.\$detail.tsx" ``` Repository: lambda-curry/saffron-atlas Length of output: 2859 --- 🏁 Script executed: ```shell rg -A 20 "samwise" apps/network-viz/app/lib/information-architecture.ts | head -50 ``` Repository: lambda-curry/saffron-atlas Length of output: 1341 --- </details> **Street pages for Samwise are not implemented.** The "Explore This District" section renders links to `/agent/samwise/triage` and `/agent/samwise/execution`, but these streets are not mapped in the `AGENT_STREETS` configuration in `agent.$agentId.$detail.tsx`. When users click these links, they will see "No page is mapped to this street yet" instead of the intended content. Add `triage` and `execution` page components and map them in `agent.$agentId.$detail.tsx` before merging, or disable the street links if the pages are not yet ready. <details> <summary>🤖 Prompt for AI Agents</summary> ``` Verify each finding against the current code and only fix it if needed. In `@apps/network-viz/app/routes/agent/samwise.tsx` around lines 243 - 258, The district "Explore This District" links point to streets not implemented: add page components for "triage" and "execution" and register them in the AGENT_STREETS mapping used by agent.$agentId.$detail.tsx (or alternatively remove/disable those Link entries in the component rendering the district to avoid broken links); specifically create components named e.g. SamwiseTriage and SamwiseExecution (or route components matching the street.to paths) and add entries for their detail keys in the AGENT_STREETS configuration so the router returns the correct page instead of the "No page is mapped to this street yet" placeholder. ``` </details> <!-- fingerprinting:phantom:poseidon:ocelot --> <!-- This is an auto-generated comment by CodeRabbit -->"
- [+3 more]

### Candidate Rule — Error Handling

**Why (failure mode):** _⚠️ Potential issue_ | _🟠 Major_ **Decision-labeled paths currently bypass the decision layer.** Line 264 and Lines 564...

**The Rule:** Handle failure paths explicitly. For async calls and risky operations, wrap with try/catch (or equivalent) and surface actionable errors instead of allowing silent failures.

**✅ DO:** ```tsx
route: '/coordination',
    cta: 'Open Coordination',
    path: 'Decision first, then District or Street as needed',
                Skip the tour and go straight to Coordination when your real question is who should handle this and what the next handoff should be.
                to="/coordination"
                Open Coordination first
```

**❌ DON'T:** ```tsx
route: '/capabilities',
    cta: 'Open Capabilities',
    path: 'Decision first, then Street if you need the exact workflow',
                Skip the tour and go straight to Capabilities when your real question is who should handle this and how you should ask.
                to="/capabilities"
                Open Capabilities first
```

**Scope:** alwaysApply: true

*Candidate rule — review before adopting. Frequency=7 in 86 PRs (8.1%). Evidence: 4 PR comments shown above.*

---

## Theme: Imports (7 comments, score 4.3)

### Candidate: Use consistent import/export structure and avoid ambiguous barrel patterns when they reduce clarity or create coupling
*Frequency: 7 · Severity signals: low · Representativeness: high*

**Supporting comments:**
- PR #80: "_⚠️ Potential issue_ | _🟡 Minor_ **Recent-changes chronology conflicts with current active status.** The new Apr 18 entry says Currybot is active in the host-level assistant stack, but the Apr 9 entry still reads as current tense (“presently stopped”). Please make the Apr 9 line explicitly historical/superseded to avoid mixed state signals. <details> <summary>Suggested wording adjustment</summary> ```diff - { when: 'Apr 9', change: 'Status rail now calls out that Currybot is presently stopped so routing guidance does not read like live status.' }, + { when: 'Apr 9', change: 'At that point in time, status rail called out that Currybot was stopped; this was superseded by the Apr 18 host-level assistant stack update.' }, ``` </details> <!-- suggestion_start --> <details> <summary>📝 Committable suggestion</summary> > ‼️ **IMPORTANT** > Carefully review the code before committing. Ensure that it accurately replaces the highlighted code, contains no missing lines, and has no issues with indentation. Thoroughly test & benchmark the code to ensure it meets the requirements. ```suggestion { when: 'Apr 18', change: 'Status rail now frames Currybot as part of the host-level OpenClaw assistant stack instead of a guaranteed Railway or local Docker deployment.' }, { when: 'Apr 9', change: 'At that point in time, status rail called out that Currybot was stopped; this was superseded by the Apr 18 host-level assistant stack update.' }, { when: 'Apr 9', change: 'Added quick routing links so first-time readers can move from summary to action without scanning the whole page.' }, ``` </details> <!-- suggestion_end --> <details> <summary>🤖 Prompt for AI Agents</summary> ``` Verify each finding against the current code and only fix it if needed. In `@apps/network-viz/app/lib/agent-context.ts` around lines 56 - 58, Update the Apr 9 changelog entries in apps/network-viz/app/lib/agent-context.ts so they read as historical/superseded rather than current: modify the two entries containing "Apr 9" (the strings that mention "presently stopped" and "Added quick routing links") to use past-tense phrasing or a prefix like "Previously (Apr 9):" so they no longer conflict with the Apr 18 active-status entry; ensure the wording clearly indicates those notes are superseded by the Apr 18 update. ``` </details> <!-- fingerprinting:phantom:poseidon:hawk --> <!-- This is an auto-generated comment by CodeRabbit --> ✅ Addressed in commit 8c57fe4"
- PR #65: "_⚠️ Potential issue_ | _🟠 Major_ **Remove the retired charter example from the home summary.** Line 811 still includes `Forge` in the charters list, which conflicts with the PR goal to replace retired charter surfaces with current ones. <details> <summary>Suggested update</summary> ```diff - { value: 'Growing', label: 'Charters', sub: 'Lantern · Spade · Shears · Watering Bucket · Barq Client Pilot · Foundry · Crucible · Forge · and more', color: 'text-indigo-300' }, + { value: 'Growing', label: 'Charters', sub: 'Lantern · Spade · Shears · Trellis · Watering Bucket · Rake · Barq Client Pilot · Foundry · Crucible · Compass · and more', color: 'text-indigo-300' }, ``` </details> <!-- suggestion_start --> <details> <summary>📝 Committable suggestion</summary> > ‼️ **IMPORTANT** > Carefully review the code before committing. Ensure that it accurately replaces the highlighted code, contains no missing lines, and has no issues with indentation. Thoroughly test & benchmark the code to ensure it meets the requirements. ```suggestion { value: 'Growing', label: 'Charters', sub: 'Lantern · Spade · Shears · Trellis · Watering Bucket · Rake · Barq Client Pilot · Foundry · Crucible · Compass · and more', color: 'text-indigo-300' }, ``` </details> <!-- suggestion_end --> <details> <summary>🤖 Prompt for AI Agents</summary> ``` Verify each finding against the current code and only fix it if needed. In `@apps/network-viz/app/routes/home.tsx` at line 811, The summary object for the home page still lists the retired charter "Forge" in the 'sub' string of the object with value 'Growing' and label 'Charters'; edit that object (the entry containing value: 'Growing', label: 'Charters', sub: ...) to remove the "Forge" token from the bullet list (and adjust surrounding separators if needed) so the charters list only contains current surfaces. ``` </details> <!-- fingerprinting:phantom:poseidon:hawk --> <!-- This is an auto-generated comment by CodeRabbit -->"
- PR #37: "_⚠️ Potential issue_ | _🟡 Minor_ **Port map changed, but page freshness date is still old.** After this update, the header freshness tag still shows **Line 29 = `2026-03-26`**. Consider updating it to the same drift date used elsewhere in this PR. <details> <summary>Suggested patch</summary> ```diff - <FreshnessTag date="2026-03-26" /> + <FreshnessTag date="2026-04-08" /> ``` </details> <!-- suggestion_start --> <details> <summary>📝 Committable suggestion</summary> > ‼️ **IMPORTANT** > Carefully review the code before committing. Ensure that it accurately replaces the highlighted code, contains no missing lines, and has no issues with indentation. Thoroughly test & benchmark the code to ensure it meets the requirements. ```suggestion <FreshnessTag date="2026-04-08" /> ``` </details> <!-- suggestion_end --> <details> <summary>🤖 Prompt for AI Agents</summary> ``` Verify each finding against the current code and only fix it if needed. In `@apps/network-viz/app/routes/api-reference.tsx` at line 597, The header freshness tag in routes/api-reference.tsx still contains the old date string "2026-03-26" after the port map update; locate the header freshness element (the date string "2026-03-26") in this file and replace it with the PR's current drift date used elsewhere (ensure the displayed date matches the other updated pages in this PR). ``` </details> <!-- fingerprinting:phantom:poseidon:hawk --> <!-- This is an auto-generated comment by CodeRabbit -->"
- PR #37: "_⚠️ Potential issue_ | _🟡 Minor_ **Refresh-date marker is now stale relative to the updated current-state copy.** This line updates live-state port guidance, but the page freshness badge still shows **Line 53 = `2026-04-01`**. Please bump it so readers can trust recency. <details> <summary>Suggested patch</summary> ```diff - <FreshnessTag date="2026-04-01" /> + <FreshnessTag date="2026-04-08" /> ``` </details> <!-- suggestion_start --> <details> <summary>📝 Committable suggestion</summary> > ‼️ **IMPORTANT** > Carefully review the code before committing. Ensure that it accurately replaces the highlighted code, contains no missing lines, and has no issues with indentation. Thoroughly test & benchmark the code to ensure it meets the requirements. ```suggestion <FreshnessTag date="2026-04-08" /> ``` </details> <!-- suggestion_end --> <details> <summary>🤖 Prompt for AI Agents</summary> ``` Verify each finding against the current code and only fix it if needed. In `@apps/network-viz/app/routes/projects.tsx` at line 119, The freshness badge date string "2026-04-01" is stale relative to the updated live-state text (the Watchtower port change); update the displayed refresh/freshness date value from "2026-04-01" to "2026-04-08" wherever that literal appears in the projects.tsx UI (the freshness badge / refresh-date marker near the project listing) so the page reflects the current update date. ``` </details> <!-- fingerprinting:phantom:poseidon:hawk --> <!-- This is an auto-generated comment by CodeRabbit -->"
- [+3 more]

### Candidate Rule — Imports

**Why (failure mode):** _⚠️ Potential issue_ | _🟡 Minor_ **Port map changed, but page freshness date is still old.** After this update, the hea...

**The Rule:** Use consistent import/export structure and avoid ambiguous barrel patterns when they reduce clarity or create coupling.

**✅ DO:** ```tsx
<FreshnessTag date="2026-04-08" />
```

**❌ DON'T:** ```tsx
<FreshnessTag date="2026-03-26" />
```

**Scope:** globs: ["**/*.ts", "**/*.tsx", "**/*.js"]

*Candidate rule — review before adopting. Frequency=7 in 86 PRs (8.1%). Evidence: 4 PR comments shown above.*

---

## Theme: Testing (5 comments, score 3.4)

### Candidate: Add or update tests when behavior changes
*Frequency: 5 · Severity signals: low · Representativeness: high*

**Supporting comments:**
- PR #87: "_⚠️ Potential issue_ | _🟠 Major_ <details> <summary>🧩 Analysis chain</summary> 🏁 Script executed: ```shell #!/bin/bash # Verify timestamp mapping/parsing assumptions in the consumer. # Expected: Confirm string-based Date parsing path for ts/_timestamp values. set -euo pipefail rg -n -C3 'hit\.ts \|\| hit\._timestamp|new Date\(event\.ts\)\.getTime\(\)' --iglob '*.ts' ``` Repository: lambda-curry/saffron-atlas Length of output: 1942 --- 🏁 Script executed: ```shell cat -n .github/workflows/cf-pages-deploy.yml | sed -n '60,75p' ``` Repository: lambda-curry/saffron-atlas Length of output: 998 --- </details> **Emit an ISO `ts` field instead of (or alongside) `_timestamp` for reliable downstream parsing.** Line 66 emits `_timestamp` as a microsecond epoch string. The consumer (`apps/network-viz/app/routes/api.topology.ts:86`) reads this via fallback (`hit.ts || hit._timestamp`), then attempts `new Date(event.ts).getTime()` on line 114. Passing a numeric string like `"1234567890000"` to `Date()` produces `Invalid Date` and causes `.getTime()` to return `NaN`, breaking age calculations downstream. Emit `ts` as an ISO string (e.g., via `new Date().toISOString()`) so the consumer's fallback chain picks it first and parsing succeeds. <details> <summary>Suggested fix</summary> ```diff + "ts": "$(bun -e 'process.stdout.write(new Date().toISOString())')", "_timestamp": "$(bun -e 'process.stdout.write(String(Date.now() * 1000))')", ``` </details> <!-- suggestion_start --> <details> <summary>📝 Committable suggestion</summary> > ‼️ **IMPORTANT** > Carefully review the code before committing. Ensure that it accurately replaces the highlighted code, contains no missing lines, and has no issues with indentation. Thoroughly test & benchmark the code to ensure it meets the requirements. ```suggestion "ts": "$(bun -e 'process.stdout.write(new Date().toISOString())')", "_timestamp": "$(bun -e 'process.stdout.write(String(Date.now() * 1000))')", ``` </details> <!-- suggestion_end --> <details> <summary>🤖 Prompt for AI Agents</summary> ``` Verify each finding against the current code and only fix it if needed. In @.github/workflows/cf-pages-deploy.yml at line 66, The workflow currently emits "_timestamp" as a microsecond epoch string which consumers use via hit.ts || hit._timestamp and then call new Date(event.ts).getTime(), causing Invalid Date; change the emitted payload to include an ISO timestamp field named "ts" (e.g., using new Date().toISOString()) alongside or instead of "_timestamp" so the consumer's fallback picks a parseable ISO string; update the line producing "_timestamp" in the workflow to also emit "ts" as ISO and keep "_timestamp" if needed for backwards compatibility so that apps/network-viz/app/routes/api.topology.ts's use of hit.ts/new Date(event.ts) succeeds. ``` </details> <!-- fingerprinting:phantom:poseidon:hawk --> <!-- This is an auto-generated comment by CodeRabbit -->"
- PR #87: "_⚠️ Potential issue_ | _🟡 Minor_ <details> <summary>🧩 Analysis chain</summary> 🏁 Script executed: ```shell #!/bin/bash # Check current Bun version pinning in package.json rg -n '"packageManager"' package.json # Check what version 'latest' currently resolves to curl -s https://api.github.com/repos/oven-sh/bun/releases/latest | jq -r '.tag_name' ``` Repository: lambda-curry/saffron-atlas Length of output: 118 --- </details> **Version mismatch: CI uses `latest` (1.3.13) but package.json pins `bun@1.3.3`.** The composite action now defaults to `latest`, which currently resolves to `bun-v1.3.13`, while `package.json` declares `"packageManager": "bun@1.3.3"`. This creates a version drift between CI and local development. While the difference is only patch-level (1.3.13 vs 1.3.3), using `latest` is not recommended: - Developers working locally will use 1.3.3 - CI will use 1.3.13 (and could shift to future patch versions) - Different versions may produce different lockfile or dependency resolution behavior Update `package.json` to `"packageManager": "bun@1.3.13"` or pin the action to a specific stable version for consistency. <details> <summary>🤖 Prompt for AI Agents</summary> ``` Verify each finding against the current code and only fix it if needed. In @.github/composite/bun-install/action.yml at line 8, CI composite action is defaulting to 'latest' (in .github/composite/bun-install/action.yml default: 'latest') while package.json pins "packageManager": "bun@1.3.3", causing a version drift; fix by either updating package.json to "packageManager": "bun@1.3.13" to match the current resolved latest, or change the composite action default from 'latest' to the specific version '1.3.13' so both the action (default in bun-install/action.yml) and package.json ("packageManager") are pinned to the same bun version for deterministic CI and local dev. ``` </details> <!-- fingerprinting:phantom:poseidon:ocelot --> <!-- This is an auto-generated comment by CodeRabbit -->"
- PR #69: "_⚠️ Potential issue_ | _🟠 Major_ **Resolve conflicting “who to ask first” guidance for code work.** Line 82 routes “repo change” asks to **Currybot**, but existing guidance on Line 53 and Line 655 routes code-change asks to **Scout**. This creates contradictory onboarding instructions in the same page. <details> <summary>💡 Suggested content fix</summary> ```diff { - ask: 'I need a repo change, PR help, or code review', - agent: 'Currybot', - why: 'Currybot is the strongest first stop for GitHub work, implementation review, and code-focused follow-through.', - expect: 'Expect direct repo work or review feedback. If the request changes product direction, policy, or ownership, another agent may get pulled in.', + ask: 'I need PR help or code review', + agent: 'Currybot', + why: 'Currybot is the strongest first stop for GitHub reviews and implementation feedback.', + expect: 'Expect direct review feedback. For net-new product changes, route to Scout first.', ``` </details> <!-- suggestion_start --> <details> <summary>📝 Committable suggestion</summary> > ‼️ **IMPORTANT** > Carefully review the code before committing. Ensure that it accurately replaces the highlighted code, contains no missing lines, and has no issues with indentation. Thoroughly test & benchmark the code to ensure it meets the requirements. ```suggestion ask: 'I need PR help or code review', agent: 'Currybot', why: 'Currybot is the strongest first stop for GitHub reviews and implementation feedback.', expect: 'Expect direct review feedback. For net-new product changes, route to Scout first.', ``` </details> <!-- suggestion_end --> <details> <summary>🤖 Prompt for AI Agents</summary> ``` Verify each finding against the current code and only fix it if needed. In `@apps/network-viz/app/routes/onboarding.tsx` around lines 81 - 84, The onboarding text has conflicting guidance: the entry with ask: 'I need a repo change, PR help, or code review' currently sets agent: 'Currybot' but other entries (referenced around the code symbols at lines referencing 'Scout') route code-change requests to 'Scout'; pick the canonical agent (either 'Scout' or 'Currybot') and update the agent field for this object (the object containing ask/agent/why/expect) to match the canonical choice, then scan the file for other entries containing the same ask text or mentions of 'repo change', 'PR help', or 'code review' and make them consistent; also update the why/expect strings if they reference the alternate agent name so wording remains coherent with the selected agent. ``` </details> <!-- fingerprinting:phantom:poseidon:hawk --> <!-- This is an auto-generated comment by CodeRabbit -->"
- PR #11: "_⚠️ Potential issue_ | _🟠 Major_ **Align Grafton stale timing text with current policy.** Line 345 says Grafton flags stalled work at “1+ week”, but documented policy indicates stale at day 5 and escalation at day 10 (see `data/progress-log.md` Lines 523-526). This creates avoidable drift in operator expectations. <details> <summary>🛠️ Proposed wording fix</summary> ```diff - <li> - <strong>Work Items</strong> — concrete execution units spawned from promoted Seeds. Assigned to domain agents - (Scout, Meg, Currybot, etc.). Grafton flags anything stalled for 1+ week and routes chronic scope disputes to Atticus. - </li> + <li> + <strong>Work Items</strong> — concrete execution units spawned from promoted Seeds. Assigned to domain agents + (Scout, Meg, Currybot, etc.). Grafton marks items stale at day 5, escalates at day 10, and routes chronic scope disputes to Atticus. + </li> ``` </details> <!-- suggestion_start --> <details> <summary>📝 Committable suggestion</summary> > ‼️ **IMPORTANT** > Carefully review the code before committing. Ensure that it accurately replaces the highlighted code, contains no missing lines, and has no issues with indentation. Thoroughly test & benchmark the code to ensure it meets the requirements. ```suggestion <FAQItem question="Who is Grafton and what is seed/initiative lifecycle management?" answer={ <> <p> <strong>Grafton</strong> is the seed and initiative lifecycle manager — the gardener of the Saffron Network. Grafton doesn't decide what to build; Grafton makes sure ideas that are ready get tracked, stale work gets flagged, and completed work gets harvested back into new opportunities. </p> <p className="mt-2"> The lifecycle has two stages: </p> <ul className="list-disc list-inside space-y-1 mt-2"> <li> <strong>Seeds</strong> — lightweight idea records (problem framing, rough upside, named owner). Grafton tags the domain, routes to the right sponsor, and watches for stale seeds. Before a Seed can be promoted to active work, Grafton runs a promotion gate: is there a falsifiable problem framing? A named owner? At least one scoped work item? </li> <li> <strong>Work Items</strong> — concrete execution units spawned from promoted Seeds. Assigned to domain agents (Scout, Meg, Currybot, etc.). Grafton marks items stale at day 5, escalates at day 10, and routes chronic scope disputes to Atticus. </li> </ul> <p className="mt-2"> When a Work Item finishes, Grafton runs a <strong>harvest loop</strong>: verify the result summary is filled, check if dormant Seeds are now unblocked, and create linked Draft Seeds from new opportunities the result reveals. Harvest plants the next crop. </p> <p className="mt-2"> <strong>Trust tier: Operator.</strong> Grafton can read/write Seeds and Work Items and send bridge messages, but cannot approve promotions, make strategic recommendations, or override sponsor decisions. </p> <p className="mt-2"> Learn more: <Link to="/grafton-lifecycle" className="text-purple-400 hover:underline">Lifecycle Model</Link> {" · "}<Link to="/grafton-integration" className="text-purple-400 hover:underline">Integration Guide</Link> </p> </> } /> ``` </details> <!-- suggestion_end --> <details> <summary>🤖 Prompt for AI Agents</summary> ``` Verify each finding against the current code and only fix it if needed. In `@apps/network-viz/app/routes/faq.tsx` around lines 325 - 363, Update the Grafton timing wording inside the FAQItem answer: replace the phrase "Grafton flags anything stalled for 1+ week and routes chronic scope disputes to Atticus." with language that matches policy (e.g., "Grafton flags work stalled for 5+ days as stale and escalates persistent issues at 10 days") so it aligns with data/progress-log.md; edit the JSX in the FAQItem component (the answer prop in the FAQItem block) to reflect the 5-day stale / 10-day escalation wording. ``` </details> <!-- fingerprinting:phantom:poseidon:hawk --> <!-- This is an auto-generated comment by CodeRabbit -->"
- [+1 more]

### Candidate Rule — Testing

**Why (failure mode):** _⚠️ Potential issue_ | _🟡 Minor_ <details> <summary>🧩 Analysis chain</summary> 🏁 Script executed: ```shell #!/bin/bas...

**The Rule:** Add or update tests when behavior changes. Prefer tests that assert observable behavior and cover likely regressions.

**✅ DO:** ```tsx
ask: 'I need PR help or code review',
    agent: 'Currybot',
    why: 'Currybot is the strongest first stop for GitHub reviews and implementation feedback.',
    expect: 'Expect direct review feedback. For net-new product changes, route to Scout first.',
```

**❌ DON'T:** ```tsx
ask: 'I need a repo change, PR help, or code review',
    agent: 'Currybot',
    why: 'Currybot is the strongest first stop for GitHub work, implementation review, and code-focused follow-through.',
    expect: 'Expect direct repo work or review feedback. If the request changes product direction, policy, or ownership, another agent may get pulled in.',
```

**Scope:** globs: ["**/*.test.ts", "**/*.spec.ts", "**/*.test.tsx"]

*Candidate rule — review before adopting. Frequency=5 in 86 PRs (5.8%). Evidence: 4 PR comments shown above.*

---

## Theme: Types (3 comments, score 2.7)

### Candidate: Prefer precise types over `any` and unsafe assertions
*Frequency: 3 · Severity signals: medium · Representativeness: medium*

**Supporting comments:**
- PR #69: "_⚠️ Potential issue_ | _🟡 Minor_ <details> <summary>🧩 Analysis chain</summary> 🏁 Script executed: ```shell #!/bin/bash set -euo pipefail # 1) Inspect available route files for Meg/Atticus pages fd -t f -i 'atticus|meg' apps/network-viz/app/routes # 2) Compare all onboarding link targets related to Meg/Atticus rg -n --type=tsx -C2 '(/meg-capabilities|/atticus-decisions|/agent/meg/design-system|/agent/atticus/decisions)' apps/network-viz/app/routes/onboarding.tsx ``` Repository: lambda-curry/saffron-atlas Length of output: 500 --- 🏁 Script executed: ```shell # Search onboarding.tsx for all route/atlas references without type flag rg -n 'atlasTo|atlasLabel' apps/network-viz/app/routes/onboarding.tsx ``` Repository: lambda-curry/saffron-atlas Length of output: 663 --- 🏁 Script executed: ```shell # Also check the specific lines mentioned in the review (94-105) sed -n '80,110p' apps/network-viz/app/routes/onboarding.tsx ``` Repository: lambda-curry/saffron-atlas Length of output: 1778 --- 🏁 Script executed: ```shell # Verify what paths the route files actually map to by checking their content head -20 apps/network-viz/app/routes/meg-capabilities.tsx head -20 apps/network-viz/app/routes/meg-design-system.tsx head -20 apps/network-viz/app/routes/atticus-decisions.tsx ``` Repository: lambda-curry/saffron-atlas Length of output: 1894 --- 🏁 Script executed: ```shell # Check if /agent/ paths are used anywhere in onboarding.tsx rg -n '/agent/' apps/network-viz/app/routes/onboarding.tsx ``` Repository: lambda-curry/saffron-atlas Length of output: 793 --- 🏁 Script executed: ```shell # Also check what the actual route definitions export for these paths rg -n 'export' apps/network-viz/app/routes/meg-capabilities.tsx apps/network-viz/app/routes/atticus-decisions.tsx | head -20 ``` Repository: lambda-curry/saffron-atlas Length of output: 450 --- </details> **Standardize matrix link targets and fix dead links throughout the file.** The code at lines 94–105 correctly uses `/meg-capabilities` and `/atticus-decisions`. However, the file contains inconsistent and dead links elsewhere: lines 860, 878, and others reference `/agent/atticus/decisions`, `/agent/meg/design-system`, and similar paths that do not correspond to actual route files. The canonical routes are flat: `/meg-capabilities`, `/meg-design-system`, and `/atticus-decisions`. Replace all `/agent/`-style links with their flat equivalents to avoid broken navigation. <details> <summary>🤖 Prompt for AI Agents</summary> ``` Verify each finding against the current code and only fix it if needed. In `@apps/network-viz/app/routes/onboarding.tsx` around lines 94 - 105, The file uses atlasTo link targets (e.g., atlasTo: '/meg-capabilities' and atlasTo: '/atticus-decisions') but other places use broken "/agent/..." paths; locate all atlasTo, href, Link, and button navigation usages (search for the atlasTo property and any string links like '/agent/atticus/decisions' or '/agent/meg/design-system') and replace them with the canonical flat routes: '/atticus-decisions', '/meg-capabilities', and '/meg-design-system' (and '/meg-design-system' instead of any '/agent/meg/design-system' variants), updating every occurrence so navigation is consistent and no dead links remain, then verify navigation in the running app. ``` </details> <!-- fingerprinting:phantom:poseidon:hawk --> <!-- This is an auto-generated comment by CodeRabbit -->"
- PR #6: "_⚠️ Potential issue_ | _🟡 Minor_ **Replace placeholder timestamps with concrete values.** Using `16:xx CDT` weakens traceability for cycle history and content-ship auditing. Please record exact times (or ISO-8601 timestamps). <details> <summary>Suggested edit</summary> ```diff -# Atlas Scratchpad — 2026-03-19 16:xx CDT +# Atlas Scratchpad — 2026-03-19 16:42 CDT ... -**Last cycle:** 2026-03-19 16:xx CDT +**Last cycle:** 2026-03-19 16:42 CDT ... -**Last network audit:** 2026-03-19 16:xx CDT (local Atlas drift review during charter cycle) +**Last network audit:** 2026-03-19 16:42 CDT (local Atlas drift review during charter cycle) ... -**Last content ship:** 2026-03-19 16:xx CDT — workflow public-exposure copy drift fix +**Last content ship:** 2026-03-19 16:42 CDT — workflow public-exposure copy drift fix ``` </details> Also applies to: 5-8 <details> <summary>🤖 Prompt for AI Agents</summary> ``` Verify each finding against the current code and only fix it if needed. In `@SCRATCHPAD.md` at line 1, The header "Atlas Scratchpad — 2026-03-19 16:xx CDT" (and other occurrences around lines 5-8) uses a placeholder timestamp; replace each "16:xx CDT" (or any similar "xx" placeholders) with a concrete timestamp, preferably in ISO-8601 format (e.g., 2026-03-19T16:23:00-05:00) so the header becomes "Atlas Scratchpad — 2026-03-19T16:23:00-05:00" (or appropriate local/UTC ISO time) to ensure precise traceability. ``` </details> <!-- fingerprinting:phantom:triton:hawk --> <!-- This is an auto-generated comment by CodeRabbit --> ✅ Addressed in commits 4dce75d to f9212f6"
- PR #2: "_⚠️ Potential issue_ | _🔴 Critical_ **Missing Bun test imports causing pipeline failure.** The pipeline fails because `describe`, `test`, and `expect` are not imported. Bun test globals need explicit imports for TypeScript compilation. As per coding guidelines, this project uses Bun test. <details> <summary>🐛 Proposed fix</summary> ```diff /** * Smoke tests for api.status and api.topology data transforms. * These exercise the pure logic (classifyStatus, hits mapping) without * hitting the network — good enough to satisfy bun test:ci. */ +import { describe, test, expect } from "bun:test"; // ---- classifyStatus logic (inlined since it's not exported) ---- ``` </details> <!-- suggestion_start --> <details> <summary>📝 Committable suggestion</summary> > ‼️ **IMPORTANT** > Carefully review the code before committing. Ensure that it accurately replaces the highlighted code, contains no missing lines, and has no issues with indentation. Thoroughly test & benchmark the code to ensure it meets the requirements. ```suggestion /** * Smoke tests for api.status and api.topology data transforms. * These exercise the pure logic (classifyStatus, hits mapping) without * hitting the network — good enough to satisfy bun test:ci. */ import { describe, test, expect } from "bun:test"; ``` </details> <!-- suggestion_end --> <details> <summary>🤖 Prompt for AI Agents</summary> ``` Verify each finding against the current code and only fix it if needed. In `@apps/network-viz/app/routes/api.routes.test.ts` around lines 1 - 6, Add explicit Bun test imports to the top of the test file so TypeScript can compile the test globals: import { describe, test, expect } from "bun:test". Update the file containing the tests that call describe, test, and expect (the api.routes.test.ts suite) to include that import statement before any test code so the pipeline recognizes the Bun test globals. ``` </details> <!-- fingerprinting:phantom:poseidon:ocelot --> <!-- This is an auto-generated comment by CodeRabbit -->"

### Candidate Rule — Types

**Why (failure mode):** _⚠️ Potential issue_ | _🟡 Minor_ <details> <summary>🧩 Analysis chain</summary> 🏁 Script executed: ```shell #!/bin/bas...

**The Rule:** Prefer precise types over `any` and unsafe assertions. Model contracts with interfaces/types so invalid states are caught at compile time.

**✅ DO:** ```ts
import { describe, test, expect } from "bun:test";
```

**❌ DON'T:** ```ts
/**
 * Smoke tests for api.status and api.topology data transforms.
 * These exercise the pure logic (classifyStatus, hits mapping) without
 * hitting the network — good enough to satisfy bun test:ci.
 */
import { describe, test, expect } from "bun:test";
```

**Scope:** alwaysApply: true

*Candidate rule — review before adopting. Frequency=3 in 86 PRs (3.5%). Evidence: 3 PR comments shown above.*

---

## Skipped (low signal)
- 0 comments below length threshold
- 0 pure approvals
- 0 questions without suggestions
