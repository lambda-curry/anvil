# Rule Candidates — lambda-curry/saffron-watchtower
*Mined: 2026-07-05 · PRs analyzed: 50 · Comments reviewed: 72 · Candidates surfaced: 5*

---

## Theme: Error Handling (25 comments, score 25.6)

### Candidate: Handle failure paths explicitly
*Frequency: 25 · Severity signals: high · Representativeness: high*

**Supporting comments:**
- PR #50: "_⚠️ Potential issue_ | _🔴 Critical_ | _⚡ Quick win_ **Persisted fingerprints are not reloaded, so cross-run dedup won’t work.** At Line 271, dedup starts from `state.fingerprints`, but `readState()` currently returns only `alerts` and `updatedAt`. That makes `fingerprints` empty every cycle, so repeated failures still create new IDs/notifications. <details> <summary>Suggested fix</summary> ```diff async function readState(): Promise<AlertStateFile> { try { const raw = await readFile(STATE_FILE, 'utf8'); const parsed = JSON.parse(raw) as Partial<AlertStateFile>; @@ const alerts: Record<string, AlertStateEntry> = {}; @@ + const fingerprints: Record<string, string> = {}; + if (parsed.fingerprints && typeof parsed.fingerprints === 'object') { + for (const [fp, alertId] of Object.entries(parsed.fingerprints)) { + if (typeof fp !== 'string' || typeof alertId !== 'string') continue; + // keep only mappings that still point to an existing alert state entry + if (alerts[alertId]) fingerprints[fp] = alertId; + } + } + return { alerts, + fingerprints, updatedAt: typeof parsed.updatedAt === 'number' ? parsed.updatedAt : Date.now() }; } catch (error) { ``` </details> <!-- suggestion_start --> <details> <summary>📝 Committable suggestion</summary> > ‼️ **IMPORTANT** > Carefully review the code before committing. Ensure that it accurately replaces the highlighted code, contains no missing lines, and has no issues with indentation. Thoroughly test & benchmark the code to ensure it meets the requirements. ```suggestion async function readState(): Promise<AlertStateFile> { try { const raw = await readFile(STATE_FILE, 'utf8'); const parsed = JSON.parse(raw) as Partial<AlertStateFile>; const alerts: Record<string, AlertStateEntry> = {}; // ... existing alerts deserialization logic ... const fingerprints: Record<string, string> = {}; if (parsed.fingerprints && typeof parsed.fingerprints === 'object') { for (const [fp, alertId] of Object.entries(parsed.fingerprints)) { if (typeof fp !== 'string' || typeof alertId !== 'string') continue; // keep only mappings that still point to an existing alert state entry if (alerts[alertId]) fingerprints[fp] = alertId; } } return { alerts, fingerprints, updatedAt: typeof parsed.updatedAt === 'number' ? parsed.updatedAt : Date.now() }; } catch (error) { // ... error handling ... } } ``` </details> <!-- suggestion_end --> <details> <summary>🤖 Prompt for AI Agents</summary> ``` Verify each finding against current code. Fix only still-valid issues, skip the rest with a brief reason, keep changes minimal, and validate. In `@scripts/alert-monitor.ts` around lines 271 - 272, The persisted fingerprints are never reloaded because readState() only returns alerts and updatedAt, so the local const fingerprints: Record<string,string> = { ...(state.fingerprints ?? {}) } will always be empty each run; fix by updating readState() to load and return fingerprints (include fingerprints in its return shape), ensure writeState()/persist logic also saves the fingerprints field after dedup operations, and keep using the existing state/fingerprints variable names so the dedup logic that references state.fingerprints, fingerprints, readState(), and writeState() will work across runs. ``` </details> <!-- fingerprinting:phantom:poseidon:hawk --> <!-- This is an auto-generated comment by CodeRabbit --> ✅ Addressed in commit 4454ae9"
- PR #48: "_⚠️ Potential issue_ | _🔴 Critical_ | _⚡ Quick win_ <details> <summary>🧩 Analysis chain</summary> 🏁 Script executed: ```shell #!/bin/bash # Verify OwnedCronJob usage vs import presence in scripts/alert-monitor.ts rg -n '\bOwnedCronJob\b' scripts/alert-monitor.ts rg -n '^import type .*OwnedCronJob' scripts/alert-monitor.ts ``` Repository: lambda-curry/saffron-watchtower Length of output: 106 --- 🏁 Script executed: ```shell # Check if the proposed import path exists and exports OwnedCronJob fd -t f 'openclaw' rg -n 'export.*OwnedCronJob' --type ts --type tsx ``` Repository: lambda-curry/saffron-watchtower Length of output: 239 --- 🏁 Script executed: ```shell # Find where OwnedCronJob is exported rg -n 'export.*OwnedCronJob' --type ts # Check the structure of apps/web/app/types/openclaw.ts cat -n apps/web/app/types/openclaw.ts | head -50 ``` Repository: lambda-curry/saffron-watchtower Length of output: 1651 --- 🏁 Script executed: ```shell # Check the imports in scripts/alert-monitor.ts to verify the path structure head -30 scripts/alert-monitor.ts ``` Repository: lambda-curry/saffron-watchtower Length of output: 1417 --- </details> **Import `OwnedCronJob` to fix TypeScript compilation failure.** `AlertMonitorContext` on line 26 references `OwnedCronJob`, but the type is not imported. Add the missing import to resolve the unresolved symbol error. <details> <summary>🛠️ Proposed fix</summary> ```diff import type { SystemHealthPayload } from '../apps/web/app/lib/system-health'; import type { Alert, AlertSeverity, AlertStateEntry, AlertStateFile } from '../apps/web/app/types/alerts'; +import type { OwnedCronJob } from '../apps/web/app/types/openclaw'; import type { ObservabilityEvent } from '../apps/web/app/types/observability'; ``` </details> <details> <summary>🤖 Prompt for AI Agents</summary> ``` Verify each finding against current code. Fix only still-valid issues, skip the rest with a brief reason, keep changes minimal, and validate. In `@scripts/alert-monitor.ts` around lines 24 - 27, AlertMonitorContext references the type OwnedCronJob but that type isn't imported; add a named import for OwnedCronJob from the module that exports it (the cron/cron-job types module used in this codebase) so the AlertMonitorContext declaration compiles—update the top of the file to import OwnedCronJob alongside other imports and keep the type name exact to match exports. ``` </details> <!-- fingerprinting:phantom:poseidon:hawk --> <!-- This is an auto-generated comment by CodeRabbit --> ✅ Addressed in commit 839ded6"
- PR #40: "_⚠️ Potential issue_ | _🟠 Major_ | _⚡ Quick win_ **Surface Atticus source degradation instead of silently dropping `follow_up` alerts.** When the Atticus fetch fails, this path falls back to `[]` and produces no warning. That can hide due/overdue follow-up alerts without any degraded indicator. Please add a dedicated partial warning (similar to ack/history warnings) or include this source in route degradation messaging. <details> <summary>Suggested direction</summary> ```diff + const atticusDecisionWarning = + atticusDecisionEventsResult.status === "fulfilled" + ? null + : "Atticus decision events are unavailable on this refresh. Follow-up alerts may be incomplete."; const atticusDecisionEvents = atticusDecisionEventsResult.status === "fulfilled" ? atticusDecisionEventsResult.value : []; ``` ```diff - const partialWarnings = [acknowledgementsWarning, alertHistoryWarning].filter( + const partialWarnings = [ + acknowledgementsWarning, + alertHistoryWarning, + atticusDecisionWarning, + ].filter( (warning): warning is string => Boolean(warning), ); ``` </details> Also applies to: 377-377 <details> <summary>🤖 Prompt for AI Agents</summary> ``` Verify each finding against current code. Fix only still-valid issues, skip the rest with a brief reason, keep changes minimal, and validate. In `@apps/web/app/routes/alerts.tsx` around lines 362 - 365, The code currently silences Atticus failures by setting atticusDecisionEvents to [] when atticusDecisionEventsResult.status !== "fulfilled", which hides degraded follow_up alerts; update the route to surface Atticus degradation similarly to existing ack/history warnings by: detecting atticusDecisionEventsResult.status !== "fulfilled" (the atticusDecisionEventsResult symbol), adding a boolean flag or partial warning message (e.g., atticusDegraded or an Atticus-specific warning component) and rendering it in the route degradation UI alongside the other warnings, and ensure follow_up alerts rendering logic (where atticusDecisionEvents is consumed) uses the degradation flag to show a warning instead of silently dropping alerts. ``` </details> <!-- fingerprinting:phantom:poseidon:hawk --> <!-- This is an auto-generated comment by CodeRabbit --> ✅ Addressed in commit a3cfaaf"
- PR #34: "_⚠️ Potential issue_ | _🟠 Major_ | _⚡ Quick win_ **Handle auth failures per observability call and skip synthetic `services.empty`.** `obsAuthBlocked` is computed as one shared flag, so a 401 from `getEvents()` will suppress a non-auth `getServices()` failure (or vice versa). On top of that, `services` falls back to `[]`, so the later `services.empty` issue is emitted even when the service list was never fetched. That turns mixed upstream failures into the wrong diagnosis. <details> <summary>Suggested fix</summary> ```diff - const upstreamBlockers: SystemHealthSourceCoverage['upstreamBlockers'] = []; - const obsAuthBlocked = - (servicesResult.status !== 'fulfilled' && isUnauthorizedAccessError(servicesResult.reason)) || - (eventsResult.status !== 'fulfilled' && isUnauthorizedAccessError(eventsResult.reason)); - - if (obsAuthBlocked) { + const upstreamBlockers: SystemHealthSourceCoverage['upstreamBlockers'] = []; + const servicesAuthBlocked = + servicesResult.status !== 'fulfilled' && isUnauthorizedAccessError(servicesResult.reason); + const eventsAuthBlocked = + eventsResult.status !== 'fulfilled' && isUnauthorizedAccessError(eventsResult.reason); + const obsAuthBlocked = servicesAuthBlocked || eventsAuthBlocked; + + if (obsAuthBlocked) { const message = 'Hosted OpenObserve is reachable but rejected auth (401 Unauthorized Access), so live observability evidence is unavailable.'; upstreamBlockers.push({ code: 'services.datasource.auth', message }); addIssue(issues, 'services.datasource.auth', 'critical', message); - } else if (servicesResult.status !== 'fulfilled') { + } + + if (servicesResult.status !== 'fulfilled' && !servicesAuthBlocked) { addIssue(issues, 'services.list.unavailable', 'critical', 'Failed to fetch service list from observability.'); } - if (eventsResult.status !== 'fulfilled' && !obsAuthBlocked) { + if (eventsResult.status !== 'fulfilled' && !eventsAuthBlocked) { addIssue(issues, 'services.events.unavailable', 'degraded', 'Failed to fetch recent service events.'); } @@ - if (services.length === 0) { + if (servicesResult.status === 'fulfilled' && services.length === 0) { addIssue(issues, 'services.empty', 'degraded', 'Service list is empty.'); } ``` </details> Also applies to: 425-427 <details> <summary>🤖 Prompt for AI Agents</summary> ``` Verify each finding against the current code and only fix it if needed. In `@apps/web/app/lib/system-health.ts` around lines 403 - 418, Compute auth-blocking per call instead of a shared obsAuthBlocked: create obsAuthBlockedServices = isUnauthorizedAccessError(servicesResult.reason) when servicesResult.status !== 'fulfilled' and obsAuthBlockedEvents = isUnauthorizedAccessError(eventsResult.reason) when eventsResult.status !== 'fulfilled'; when adding the upstreamBlockers and addIssue calls, use the per-call flags (push { code: 'services.datasource.auth', message } and addIssue only for the specific call that returned 401), and only emit the synthetic 'services.empty' issue if servicesResult.status === 'fulfilled' and the returned services array is empty (do not emit it when servicesResult failed or was auth-blocked); apply the same per-call auth logic to the events-related addIssue paths referenced around lines 425-427. ``` </details> <!-- fingerprinting:phantom:medusa:grasshopper --> <!-- This is an auto-generated comment by CodeRabbit --> ✅ Addressed in commit 9db6a56"
- [+21 more]

### Candidate Rule — Error Handling

**Why (failure mode):** _⚠️ Potential issue_ | _🟠 Major_ **Loader is brittle because optional enrichment is treated as required.** A failure in...

**The Rule:** Handle failure paths explicitly. For async calls and risky operations, wrap with try/catch (or equivalent) and surface actionable errors instead of allowing silent failures.

**✅ DO:** ```ts
const codexBarData = context?.codexBarData;
  const dailyEntries = codexBarData?.daily ?? [];
  const todayCodexCost = dailyEntries
  const historicalCosts = dailyEntries
```

**❌ DON'T:** ```ts
const codexBarData = context?.codexBarData;
  if (!codexBarData?.daily?.length) return [];
  const todayCodexCost = codexBarData.daily
  const historicalCosts = codexBarData.daily
```

**Scope:** alwaysApply: true

*Candidate rule — review before adopting. Frequency=25 in 50 PRs (50.0%). Evidence: 4 PR comments shown above.*

---

## Theme: Naming (19 comments, score 14.2)

### Candidate: Use descriptive, convention-aligned names for variables, functions, and classes
*Frequency: 19 · Severity signals: high · Representativeness: high*

**Supporting comments:**
- PR #50: "_⚠️ Potential issue_ | _🟠 Major_ | _⚡ Quick win_ **Avoid fingerprint collisions when `jobId` or `signature` is missing.** Lines 242-244 coerce missing metadata to `''`, so different malformed RCF alerts on the same agent collapse into the same `rcf:${agentId}::` fingerprint and may suppress valid new alerts. <details> <summary>Suggested fix</summary> ```diff function computeAlertFingerprint(alert: Alert): string | null { if (alert.category !== 'returned_cron_failure') return null; const meta = alert.metadata ?? {}; - const jobId = String(meta.jobId ?? ''); - const signature = String(meta.signature ?? ''); + const jobId = String(meta.jobId ?? '').trim(); + const signature = String(meta.signature ?? '').trim(); + if (!jobId || !signature) return null; @@ return `rcf:${alert.agentId}:${jobId}:${signature}`; } ``` </details> <!-- suggestion_start --> <details> <summary>📝 Committable suggestion</summary> > ‼️ **IMPORTANT** > Carefully review the code before committing. Ensure that it accurately replaces the highlighted code, contains no missing lines, and has no issues with indentation. Thoroughly test & benchmark the code to ensure it meets the requirements. ```suggestion function computeAlertFingerprint(alert: Alert): string | null { if (alert.category !== 'returned_cron_failure') return null; const meta = alert.metadata ?? {}; const jobId = String(meta.jobId ?? '').trim(); const signature = String(meta.signature ?? '').trim(); if (!jobId || !signature) return null; return `rcf:${alert.agentId}:${jobId}:${signature}`; } ``` </details> <!-- suggestion_end --> <details> <summary>🤖 Prompt for AI Agents</summary> ``` Verify each finding against current code. Fix only still-valid issues, skip the rest with a brief reason, keep changes minimal, and validate. In `@scripts/alert-monitor.ts` around lines 242 - 244, The current coercion of meta.jobId and meta.signature to '' causes distinct missing fields to collide in the generated fingerprint; change the fingerprint construction logic (where jobId and signature are read/used in scripts/alert-monitor.ts) to use explicit missing markers or include the field name in the token instead of an empty string (e.g., use '<missing-jobId>' and '<missing-signature>' or prefix tokens like 'job:...' and 'sig:...') so fingerprints become rcf:${agentId}:job:<...>:sig:<...> and cannot collapse different malformed alerts into the same rcf:${agentId}:: fingerprint. ``` </details> <!-- fingerprinting:phantom:poseidon:hawk --> <!-- This is an auto-generated comment by CodeRabbit --> ✅ Addressed in commit 4454ae9"
- PR #31: "_⚠️ Potential issue_ | _🟡 Minor_ **Avoid hardcoding “unhealthy” in the evidence note body.** Line 28 can produce contradictory copy when `health.summary` is `healthy` (partial coverage + “not proof ... unhealthy”). Use neutral wording tied to “this state.” <details> <summary>Proposed copy fix</summary> ```diff - body: `This ${health.summary} state comes from partial network coverage (${coverageGaps.join(' · ')}). Treat it as limited evidence, not proof the whole network is unhealthy right now.`, + body: `This ${health.summary} state comes from partial network coverage (${coverageGaps.join(' · ')}). Treat it as limited evidence, not proof the whole network is in this state right now.`, ``` </details> <details> <summary>🤖 Prompt for AI Agents</summary> ``` Verify each finding against the current code and only fix it if needed. In `@apps/web/app/lib/system-health-copy.ts` at line 28, The body string in lib/system-health-copy.ts currently hardcodes "unhealthy" which can contradict health.summary; update the template used where body is constructed (reference the body template containing health.summary and coverageGaps) to use neutral wording tied to the current state—e.g., replace the hardcoded "unhealthy" with either `${health.summary}` or a neutral phrase like "in this state" so the sentence reads consistently when health.summary is "healthy" or "unhealthy", and keep the rest of the sentence about limited evidence and coverageGaps intact. ``` </details> <!-- fingerprinting:phantom:poseidon:hawk --> <!-- This is an auto-generated comment by CodeRabbit --> ✅ Addressed in commit 2c0242d"
- PR #26: "_⚠️ Potential issue_ | _🔴 Critical_ <details> <summary>🧩 Analysis chain</summary> 🏁 Script executed: ```shell # Find the system-health.ts file find . -name "system-health.ts" -type f | head -20 ``` Repository: lambda-curry/saffron-watchtower Length of output: 154 --- 🏁 Script executed: ```shell # Check if the file exists and view the relevant lines if [ -f "apps/web/app/lib/system-health.ts" ]; then echo "=== system-health.ts lines 175-200 ===" sed -n '175,200p' "apps/web/app/lib/system-health.ts" else echo "File not found at expected path" fi ``` Repository: lambda-curry/saffron-watchtower Length of output: 851 --- 🏁 Script executed: ```shell # Find AgentNetworkClient definition find . -name "*agent-network-client*" -type f | head -20 ``` Repository: lambda-curry/saffron-watchtower Length of output: 118 --- 🏁 Script executed: ```shell # Search for AgentNetworkClient constructor definition rg "class AgentNetworkClient|constructor.*Agent\[\]" --type ts --type tsx -A 5 | head -50 ``` Repository: lambda-curry/saffron-watchtower Length of output: 103 --- 🏁 Script executed: ```shell # Search for AgentNetworkClient constructor definition (adjust file type flags) rg "class AgentNetworkClient" -A 10 --type ts | head -50 ``` Repository: lambda-curry/saffron-watchtower Length of output: 920 --- 🏁 Script executed: ```shell # Read the agent-network-client.ts file to see constructor cat -n apps/web/app/lib/agent-network-client.ts | head -50 ``` Repository: lambda-curry/saffron-watchtower Length of output: 1853 --- 🏁 Script executed: ```shell # Check AGENTS constant definition rg "const AGENTS|export.*AGENTS" --type ts -B 2 -A 5 | head -30 ``` Repository: lambda-curry/saffron-watchtower Length of output: 1027 --- 🏁 Script executed: ```shell # Check if there's a TypeScript compilation or if the code actually compiles # Look for tsconfig.json find . -name "tsconfig.json" -type f | head -5 ``` Repository: lambda-curry/saffron-watchtower Length of output: 175 --- 🏁 Script executed: ```shell # Search for similar patterns where readonly arrays are passed to constructors rg "new AgentNetworkClient" --type ts -B 2 -A 2 ``` Repository: lambda-curry/saffron-watchtower Length of output: 5473 --- 🏁 Script executed: ```shell # Check if there's a cron-health-summary.ts file mentioned in the review comment find . -name "*cron*health*" -o -name "*cron*summary*" | head -10 ``` Repository: lambda-curry/saffron-watchtower Length of output: 393 --- 🏁 Script executed: ```shell # Look at the actual Agent type definition rg "type Agent|interface Agent" --type ts -A 10 | head -40 ``` Repository: lambda-curry/saffron-watchtower Length of output: 2453 --- 🏁 Script executed: ```shell # Check cron-health-summary.ts for the same pattern sed -n '1,50p' apps/web/app/lib/cron-health-summary.ts ``` Repository: lambda-curry/saffron-watchtower Length of output: 1500 --- 🏁 Script executed: ```shell # Check the exact type of agents parameter in cron-health-summary rg "agents = AGENTS" -B 5 -A 10 apps/web/app/lib/cron-health-summary.ts ``` Repository: lambda-curry/saffron-watchtower Length of output: 919 --- 🏁 Script executed: ```shell # Check TypeScript compiler strict settings cat apps/web/tsconfig.json | grep -A 5 -B 5 "strict\|noImplicitAny" | head -30 ``` Repository: lambda-curry/saffron-watchtower Length of output: 57 --- 🏁 Script executed: ```shell # Verify that buildSystemHealthPayload and buildCronHealthSummary have identical signatures rg "export async function build.*Health" -A 15 --type ts apps/web/app/lib/ ``` Repository: lambda-curry/saffron-watchtower Length of output: 3434 --- 🏁 Script executed: ```shell # Check if there are any test files that show how this function is being called cat -n apps/web/app/lib/__tests__/cron-health-summary.test.ts | head -100 ``` Repository: lambda-curry/saffron-watchtower Length of output: 2869 --- </details> **Resolve the readonly array type error in the default `networkClient`.** Line 183 has a type incompatibility: `agents` is `readonly Agent[]`, so it cannot be passed directly to `new AgentNetworkClient(...)` while that constructor expects a mutable `Agent[]`. <details> <summary>Proposed fix</summary> ```diff export async function buildSystemHealthPayload({ agents = AGENTS, - networkClient = new AgentNetworkClient(agents), + networkClient = new AgentNetworkClient([...agents]), obsClient = new HybridObsClient(), checkedAt = new Date().toISOString(), ``` </details> <!-- suggestion_start --> <details> <summary>📝 Committable suggestion</summary> > ‼️ **IMPORTANT** > Carefully review the code before committing. Ensure that it accurately replaces the highlighted code, contains no missing lines, and has no issues with indentation. Thoroughly test & benchmark the code to ensure it meets the requirements. ```suggestion export async function buildSystemHealthPayload({ agents = AGENTS, networkClient = new AgentNetworkClient([...agents]), obsClient = new HybridObsClient(), checkedAt = new Date().toISOString(), nowMs = Date.now(), }: { agents?: readonly Agent[]; networkClient?: AgentNetworkLike; obsClient?: ObsClientLike; checkedAt?: string; nowMs?: number; } = {}): Promise<SystemHealthPayload> { ``` </details> <!-- suggestion_end --> <details> <summary>🧰 Tools</summary> <details> <summary>🪛 GitHub Actions: PR Quality Checks</summary> [error] 183-183: TypeScript (TS2345): Argument of type 'readonly Agent[]' is not assignable to parameter of type 'Agent[]' because 'readonly' cannot be assigned to mutable type 'Agent[]'. </details> </details> <details> <summary>🤖 Prompt for AI Agents</summary> ``` Verify each finding against the current code and only fix it if needed. In `@apps/web/app/lib/system-health.ts` around lines 181 - 193, The default parameter uses the readonly AGENTS array and passes it into AgentNetworkClient, causing a type error because AgentNetworkClient expects a mutable Agent[]; update the default networkClient to construct AgentNetworkClient with a shallow mutable copy of agents (for example by spreading or Array.from) so AgentNetworkClient receives a non-readonly Agent[]; refer to buildSystemHealthPayload, AGENTS, agents, and AgentNetworkClient when making this change. ``` </details> <!-- fingerprinting:phantom:medusa:grasshopper --> <!-- This is an auto-generated comment by CodeRabbit --> ✅ Addressed in commit c54f98d"
- PR #24: "_⚠️ Potential issue_ | _🔴 Critical_ <details> <summary>🧩 Analysis chain</summary> 🏁 Script executed: ```shell #!/bin/bash set -euo pipefail echo "Checking install chain and sed portability risk..." rg -n -C2 '"postinstall"\s*:\s*"bash scripts/patch-react-act.sh"' package.json rg -n -C2 'bun install --frozen-lockfile' .github/composite/bun-install/action.yml rg -n -C2 'sed -i ' scripts/patch-react-act.sh ``` Repository: lambda-curry/saffron-watchtower Length of output: 883 --- 🏁 Script executed: ```shell #!/bin/bash # 1. Check the exact pr-quality-checks.yml file to confirm macOS runner at line 29 echo "=== Checking pr-quality-checks.yml around line 29 ===" sed -n '25,35p' .github/workflows/pr-quality-checks.yml # 2. Search for all sed -i usages in shell scripts to identify scope echo -e "\n=== All sed -i usages in shell scripts ===" rg 'sed -i' --type sh --type bash -n # 3. Check if there are other postinstall scripts echo -e "\n=== Checking for other postinstall hooks ===" rg '"postinstall"' package.json ``` Repository: lambda-curry/saffron-watchtower Length of output: 561 --- 🏁 Script executed: ```shell #!/bin/bash # Search for sed -i without type restrictions echo "=== All sed -i usages in repository ===" rg 'sed -i' -n # Get the full content of patch-react-act.sh to understand the context echo -e "\n=== Full content of patch-react-act.sh ===" cat -n scripts/patch-react-act.sh # Verify the proposed fix would work on both macOS and Linux echo -e "\n=== Testing sed syntax compatibility ===" echo "GNU sed (Linux): sed -i 's/pattern/replacement/' file" echo "BSD sed (macOS): sed -i '' 's/pattern/replacement/' file" ``` Repository: lambda-curry/saffron-watchtower Length of output: 1556 --- </details> **macOS runner switch will fail dependency install due to non-portable `sed -i` in postinstall.** At line 29 of `.github/workflows/pr-quality-checks.yml`, the job now runs on macOS, but `bun install --frozen-lockfile` triggers the postinstall script (`bash scripts/patch-react-act.sh`). Line 10 of that script uses GNU-style `sed -i` which fails on BSD/macOS `sed` because macOS requires the backup extension argument (even if empty): `sed -i ''`. This will break the workflow before quality gates run. Fix by applying OS-specific sed syntax in `scripts/patch-react-act.sh`: ```diff - sed -i 's/return React.act(callback);/if (typeof React.act === "function") return React.act(callback); var result = callback(); if (result \&\& typeof result.then === "function") return result; return undefined;/' "$TEST_UTILS_FILE" + if [[ "$OSTYPE" == "darwin"* ]]; then + sed -i '' 's/return React.act(callback);/if (typeof React.act === "function") return React.act(callback); var result = callback(); if (result \&\& typeof result.then === "function") return result; return undefined;/' "$TEST_UTILS_FILE" + else + sed -i 's/return React.act(callback);/if (typeof React.act === "function") return React.act(callback); var result = callback(); if (result \&\& typeof result.then === "function") return result; return undefined;/' "$TEST_UTILS_FILE" + fi ``` <details> <summary>🤖 Prompt for AI Agents</summary> ``` Verify each finding against the current code and only fix it if needed. In @.github/workflows/pr-quality-checks.yml at line 29, The macOS runner fails because scripts/patch-react-act.sh uses GNU-only sed -i; update that script to detect Darwin (uname -s == "Darwin") and choose the correct in-place syntax (use sed -i '' on macOS, sed -i on Linux) or set a SED_INPLACE variable accordingly, then use that variable where the script currently calls sed (the problematic sed invocation around line 10 in scripts/patch-react-act.sh). Ensure the selected syntax preserves quoting/escaping so the replacement command works the same across platforms. ``` </details> <!-- fingerprinting:phantom:poseidon:hawk --> <!-- This is an auto-generated comment by CodeRabbit --> ✅ Addressed in commit 121664a"
- [+15 more]

### Candidate Rule — Naming

**Why (failure mode):** _⚠️ Potential issue_ | _🟡 Minor_ **This subtitle should say “per-actor,” not “per-agent.”** The shared report model als...

**The Rule:** Use descriptive, convention-aligned names for variables, functions, and classes. Favor consistency with existing project naming standards.

**✅ DO:** ```tsx
Per-actor Codex credit consumption by day (last 14 days). Partial attribution — excludes Unknown/Desktop usage.
```

**❌ DON'T:** ```tsx
Per-agent Codex credit consumption by day (last 14 days). Partial attribution — excludes Unknown/Desktop usage.
```

**Scope:** alwaysApply: true

*Candidate rule — review before adopting. Frequency=19 in 50 PRs (38.0%). Evidence: 4 PR comments shown above.*

---

## Theme: Testing (9 comments, score 7.0)

### Candidate: Add or update tests when behavior changes
*Frequency: 9 · Severity signals: medium · Representativeness: high*

**Supporting comments:**
- PR #50: "_⚠️ Potential issue_ | _🟠 Major_ | _⚡ Quick win_ **Fix inverted fingerprint filter in the non-RCF assertion.** Line 237 currently filters keys that **do not** start with `rcf:`; that can still pass even when unwanted `rcf:` fingerprints exist. The assertion should explicitly check for `rcf:` keys. <details> <summary>Suggested fix</summary> ```diff - const rcfFps = Object.keys(result1.state.fingerprints ?? {}).filter((k) => !k.startsWith('rcf:')); + const rcfFps = Object.keys(result1.state.fingerprints ?? {}).filter((k) => k.startsWith('rcf:')); expect(rcfFps).toHaveLength(0); ``` </details> <details> <summary>🤖 Prompt for AI Agents</summary> ``` Verify each finding against current code. Fix only still-valid issues, skip the rest with a brief reason, keep changes minimal, and validate. In `@scripts/__tests__/alert-monitor-fingerprint.test.ts` around lines 237 - 238, The test currently builds rcfFps by filtering keys that do NOT start with 'rcf:' which hides presence of unwanted 'rcf:' fingerprints; update the filter on result1.state.fingerprints to select keys that DO startWith('rcf:') (e.g., change the predicate to k.startsWith('rcf:')) and keep the expect(rcfFps).toHaveLength(0) assertion so the test explicitly fails if any 'rcf:' fingerprints are present; reference the rcfFps variable and result1.state.fingerprints in the test to locate and fix the predicate. ``` </details> <!-- fingerprinting:phantom:poseidon:hawk --> <!-- This is an auto-generated comment by CodeRabbit -->"
- PR #47: "_⚠️ Potential issue_ | _🟠 Major_ | _⚡ Quick win_ **Freeze `nowMs` in snapshot-based tests to prevent date-driven flakiness.** Line 11 hard-codes `checkedAt`, but Lines 155, 196, and 230 rely on default `Date.now()`. After 24h, those cases can fail via the stale branch instead of the intended branch. <details> <summary>🧪 Proposed fix</summary> ```diff const baseSnapshot: SystemHealthPayload = { @@ checkedAt: "2026-05-13T13:06:49.704Z", @@ }; + +const TEST_NOW_MS = Date.parse("2026-05-13T13:06:49.704Z"); @@ it("blocks the calm zero-alert copy when coverage is partial", () => { const result = getAlertsEmptyStateContent({ @@ systemHealthSnapshotWarning: null, + nowMs: TEST_NOW_MS, }); @@ it("blocks the calm zero-alert copy when returned cron failures are present", () => { const result = getAlertsEmptyStateContent({ @@ systemHealthSnapshotWarning: null, + nowMs: TEST_NOW_MS, }); @@ it("blocks the calm zero-alert copy when the snapshot summary is not healthy", () => { const result = getAlertsEmptyStateContent({ @@ systemHealthSnapshotWarning: null, + nowMs: TEST_NOW_MS, }); ``` </details> Also applies to: 154-254 <details> <summary>🤖 Prompt for AI Agents</summary> ``` Verify each finding against current code. Fix only still-valid issues, skip the rest with a brief reason, keep changes minimal, and validate. In `@apps/web/app/routes/__tests__/alerts.surface-state.test.ts` around lines 9 - 12, Tests use a hard-coded checkedAt in baseSnapshot while other assertions rely on Date.now(), causing flakiness; fix by defining a single frozen nowMs constant at the top of the test file and use it for both baseSnapshot.checkedAt and any places that call Date.now() (either by mocking Date.now()/using vi.setSystemTime(nowMs) or by passing nowMs into helpers), e.g., declare const nowMs = Date.parse("2026-05-13T13:06:49.704Z") and set baseSnapshot.checkedAt = new Date(nowMs).toISOString(), and ensure tests that previously used Date.now() are either mocked to return nowMs or use the same nowMs value. ``` </details> <!-- fingerprinting:phantom:poseidon:hawk --> <!-- This is an auto-generated comment by CodeRabbit --> ✅ Addressed in commit 764a644"
- PR #28: "_⚠️ Potential issue_ | _🟠 Major_ <details> <summary>🧩 Analysis chain</summary> 🏁 Script executed: ```shell #!/bin/bash # Verify current permissive checks and missing malformed-row regression cases. sed -n '98,131p' apps/web/app/lib/codexbar-report.ts rg -n "primary:\\s*\\{\\}|summary30d:\\s*\\[\\s*null|daily:\\s*\\[\\s*null" apps/web/app/lib/__tests__/codexbar-report.test.ts # Expected: guard uses non-null checks for windows; no regression tests for malformed non-null rows/windows. ``` Repository: lambda-curry/saffron-watchtower Length of output: 1830 --- 🏁 Script executed: ```shell #!/bin/bash # Find type definitions for CodexBarUsageWindow, CodexBarActorSummary, CodexBarActorDailyEntry rg -n "type CodexBarUsageWindow|interface CodexBarUsageWindow" --type ts --type tsx rg -n "type CodexBarActorSummary|interface CodexBarActorSummary" --type ts --type tsx rg -n "type CodexBarActorDailyEntry|interface CodexBarActorDailyEntry" --type ts --type tsx ``` Repository: lambda-curry/saffron-watchtower Length of output: 159 --- 🏁 Script executed: ```shell #!/bin/bash # Check for test file and what tests exist for hasUsableCodexBarUsageReport fd -e test.ts -e test.tsx -e spec.ts | grep -i codexbar find . -name "*codexbar*" -type f | head -20 ``` Repository: lambda-curry/saffron-watchtower Length of output: 640 --- 🏁 Script executed: ```shell #!/bin/bash # Look for usages of hasUsableCodexBarUsageReport to understand its impact rg -n "hasUsableCodexBarUsageReport" --type ts --type tsx -A 2 -B 2 ``` Repository: lambda-curry/saffron-watchtower Length of output: 103 --- 🏁 Script executed: ```shell #!/bin/bash # Search for type definitions without file type restrictions rg -n "type CodexBarUsageWindow|interface CodexBarUsageWindow" apps/web/app/lib/ rg -n "type CodexBarActorSummary|interface CodexBarActorSummary" apps/web/app/lib/ rg -n "type CodexBarActorDailyEntry|interface CodexBarActorDailyEntry" apps/web/app/lib/ ``` Repository: lambda-curry/saffron-watchtower Length of output: 313 --- 🏁 Script executed: ```shell #!/bin/bash # Read the full codexbar-report.ts file to see type definitions wc -l apps/web/app/lib/codexbar-report.ts ``` Repository: lambda-curry/saffron-watchtower Length of output: 115 --- 🏁 Script executed: ```shell #!/bin/bash # Read the test file to check coverage wc -l apps/web/app/lib/__tests__/codexbar-report.test.ts head -150 apps/web/app/lib/__tests__/codexbar-report.test.ts ``` Repository: lambda-curry/saffron-watchtower Length of output: 3605 --- 🏁 Script executed: ```shell #!/bin/bash # Search for hasUsableCodexBarUsageReport usages without file type filter rg -n "hasUsableCodexBarUsageReport" apps/web/app/ -A 2 -B 1 ``` Repository: lambda-curry/saffron-watchtower Length of output: 4110 --- 🏁 Script executed: ```shell #!/bin/bash # Read the type definitions to understand expected structure sed -n '1,70p' apps/web/app/lib/codexbar-report.ts ``` Repository: lambda-curry/saffron-watchtower Length of output: 1850 --- 🏁 Script executed: ```shell #!/bin/bash # Read more of the type definitions and the guard function sed -n '38,97p' apps/web/app/lib/codexbar-report.ts ``` Repository: lambda-curry/saffron-watchtower Length of output: 1732 --- 🏁 Script executed: ```shell #!/bin/bash # Check if there are any tests that currently validate malformed window objects like primary: {} grep -n "primary.*{}" apps/web/app/lib/__tests__/codexbar-report.test.ts ``` Repository: lambda-curry/saffron-watchtower Length of output: 57 --- 🏁 Script executed: ```shell #!/bin/bash # Verify the mapCodexBarUsageReport function to understand impact of receiving malformed data rg -n "mapCodexBarUsageReport" apps/web/app/ -A 5 | head -30 ``` Repository: lambda-curry/saffron-watchtower Length of output: 1146 --- </details> **Type guard is too permissive for malformed nested content** `primary != null` and `secondary != null` accept empty objects (e.g., `{}`) without the required `usedPercent: number` field. `hasArrayEntries()` only checks length, not entry validity, allowing arrays like `[null]` to pass. This causes downstream code accessing `entry.actorId` and `entry.actorName` (lines 2131–2134 in tokens.tsx) to crash at runtime. Tighten validation by adding type predicates for `CodexBarUsageWindow`, `CodexBarActorSummary`, and `CodexBarActorDailyEntry` that check required fields: <details> <summary>Suggested fix</summary> ```diff + const isUsageWindow = (value: unknown): value is CodexBarUsageWindow => { + if (!value || typeof value !== "object") return false; + const window = value as Partial<CodexBarUsageWindow>; + return typeof window.usedPercent === "number"; + }; + + const hasTypedEntry = <T>(value: unknown, predicate: (entry: unknown) => entry is T): boolean => + Array.isArray(value) && value.some(predicate); + + const isActorSummary = (entry: unknown): entry is CodexBarActorSummary => + Boolean(entry) && + typeof entry === "object" && + typeof (entry as CodexBarActorSummary).actorId === "string"; + + const isActorDaily = (entry: unknown): entry is CodexBarActorDailyEntry => + Boolean(entry) && + typeof entry === "object" && + typeof (entry as CodexBarActorDailyEntry).actorId === "string" && + typeof (entry as CodexBarActorDailyEntry).day === "string"; + const hasSubscriptionData = @@ - subscription?.primary != null || - subscription?.secondary != null || + isUsageWindow(subscription?.primary) || + isUsageWindow(subscription?.secondary) || @@ - const hasActorData = hasArrayEntries(actors?.summary30d) || hasArrayEntries(actors?.daily); + const hasActorData = + hasTypedEntry(actors?.summary30d, isActorSummary) || + hasTypedEntry(actors?.daily, isActorDaily); ``` </details> <details> <summary>🤖 Prompt for AI Agents</summary> ``` Verify each finding against the current code and only fix it if needed. In `@apps/web/app/lib/codexbar-report.ts` around lines 116 - 129, The current truthiness checks (subscription?.primary/secondary, hasArrayEntries(subscription?.dailyCredits), hasArrayEntries(actors?.summary30d), hasArrayEntries(actors?.daily)) are too permissive and allow malformed objects/arrays (e.g., {} or [null]) to pass; add and use narrow type predicate functions (e.g., isCodexBarUsageWindow, isCodexBarActorSummary, isCodexBarActorDailyEntry) that validate required fields like usedPercent:number on CodexBarUsageWindow and actorId/actorName on actor entries, then replace the permissive checks in the hasSubscriptionData and hasActorData logic to call these predicates (and filter arrays with them) so downstream code in tokens.tsx can safely access entry.actorId/entry.actorName without runtime crashes. ``` </details> <!-- fingerprinting:phantom:poseidon:hawk --> <!-- This is an auto-generated comment by CodeRabbit --> ✅ Addressed in commit db74851"
- PR #24: "_⚠️ Potential issue_ | _🟠 Major_ **Protect self-hosted runners from untrusted PR execution.** Line 29 runs `pull_request` code on a self-hosted runner. If fork PRs are enabled, this is a high-risk runner compromise path. Gate this job to trusted PRs (or route fork PRs to GitHub-hosted runners). <details> <summary>Suggested guard (minimal)</summary> ```diff jobs: quality-checks: + if: ${{ github.event.pull_request.head.repo.fork == false }} runs-on: [self-hosted, mini2, macos, arm64] ``` </details> <details> <summary>🧰 Tools</summary> <details> <summary>🪛 actionlint (1.7.12)</summary> [error] 29-29: label "mini2" is unknown. available labels are "windows-latest", "windows-latest-8-cores", "windows-2025", "windows-2025-vs2026", "windows-2022", "windows-11-arm", "ubuntu-slim", "ubuntu-latest", "ubuntu-latest-4-cores", "ubuntu-latest-8-cores", "ubuntu-latest-16-cores", "ubuntu-24.04", "ubuntu-24.04-arm", "ubuntu-22.04", "ubuntu-22.04-arm", "macos-latest", "macos-latest-xlarge", "macos-latest-large", "macos-26-intel", "macos-26-xlarge", "macos-26-large", "macos-26", "macos-15-intel", "macos-15-xlarge", "macos-15-large", "macos-15", "macos-14-xlarge", "macos-14-large", "macos-14", "self-hosted", "x64", "arm", "arm64", "linux", "macos", "windows". if it is a custom label for self-hosted runner, set list of labels in actionlint.yaml config file (runner-label) </details> </details> <details> <summary>🤖 Prompt for AI Agents</summary> ``` Verify each finding against the current code and only fix it if needed. In @.github/workflows/pr-quality-checks.yml at line 29, The workflow currently specifies a self-hosted runner via the runs-on setting which allows untrusted fork PRs to execute on your self-hosted mini2/macOS runner; update the job to gate execution for untrusted PRs by adding a conditional that detects forked PRs (github.event.pull_request.head.repo.fork) and either skip the job or switch runs-on to GitHub-hosted runners for forks, or alternatively restrict the job to trusted collaborators only (e.g., require pull_request from same-repo or use a trusted-pr contributor check); modify the job that contains runs-on: [self-hosted, mini2, macos, arm64] so it uses this conditional routing or gating logic. ``` </details> <!-- fingerprinting:phantom:poseidon:hawk --> <!-- This is an auto-generated comment by CodeRabbit -->"
- [+5 more]

### Candidate Rule — Testing

**Why (failure mode):** _⚠️ Potential issue_ | _🟠 Major_ **Avoid committing environment-identifying hostnames and absolute local paths.** These...

**The Rule:** Add or update tests when behavior changes. Prefer tests that assert observable behavior and cover likely regressions.

**✅ DO:** ```md
Dev server: ✅ expected at configured local dev URL (see local env docs)
- **Bridge CLI broken** — workspace `scripts/bridge.ts` symlink target missing. Blocks Atticus/Meg consultation and Saffron Doc via Scout.
- **CodexBar data missing** — workspace `data/codex-usage.json` doesn't exist. CodexBar panel shows graceful null state. Host cron not yet configured.
```

**❌ DON'T:** ```md
Dev server: ✅ expected at http://jakes-mac-mini:5175/
- **Bridge CLI broken** — `/home/node/.openclaw/workspace/scripts/bridge.ts` symlink target missing. Blocks Atticus/Meg consultation and Saffron Doc via Scout.
- **CodexBar data missing** — `/home/node/.openclaw/workspace/data/codex-usage.json` doesn't exist. CodexBar panel shows graceful null state. Host cron not yet configured.
```

**Scope:** globs: ["**/*.test.ts", "**/*.spec.ts", "**/*.test.tsx"]

*Candidate rule — review before adopting. Frequency=9 in 50 PRs (18.0%). Evidence: 4 PR comments shown above.*

---

## Theme: Types (5 comments, score 4.0)

### Candidate: Prefer precise types over `any` and unsafe assertions
*Frequency: 5 · Severity signals: medium · Representativeness: high*

**Supporting comments:**
- PR #40: "_⚠️ Potential issue_ | _🟠 Major_ | _⚡ Quick win_ **Filter to canonical decision events before updating latest-by-key state.** On Line 163-Line 164, any `atticus` event carrying a `decisionKey` can replace the current decision snapshot. That can incorrectly clear a deferred follow-up alert if a newer non-canonical event reuses the same key. <details> <summary>Suggested fix</summary> ```diff +function hasCanonicalDecisionSignal(meta: Record<string, unknown> | undefined): boolean { + const raw = meta?.outcome ?? meta?.decision ?? meta?.result; + return typeof raw === 'string' && raw.trim().length > 0; +} + export function getActiveAtticusFollowUps( events: ObservabilityEvent[], now = Date.now(), ): ActiveAtticusFollowUp[] { @@ events .filter((event) => event.service === 'atticus') .sort((a, b) => a.ts - b.ts) .forEach((event, index) => { const decision = parseAtticusDecision(event, index); if (!decision.decisionKey) return; + const isCanonical = hasCanonicalDecisionSignal(event.meta) || Boolean(decision.followUpField); + if (!isCanonical) return; latestByDecisionKey.set(decision.decisionKey, { decision, event }); }); ``` </details> <details> <summary>🤖 Prompt for AI Agents</summary> ``` Verify each finding against current code. Fix only still-valid issues, skip the rest with a brief reason, keep changes minimal, and validate. In `@apps/web/app/lib/atticus-decisions.ts` around lines 158 - 165, The loop currently updates latestByDecisionKey for any parsed atticus event with a decisionKey; change it to only update when the parsed decision is a canonical decision to avoid non-canonical events overwriting the snapshot. Specifically, inside the forEach that calls parseAtticusDecision(event, index), guard the latestByDecisionKey.set(...) with a check for the canonical flag on the parsed decision (e.g., decision.canonical or decision.isCanonical) in addition to decision.decisionKey so only canonical decisions update latestByDecisionKey. ``` </details> <!-- fingerprinting:phantom:poseidon:hawk --> <!-- This is an auto-generated comment by CodeRabbit --> ✅ Addressed in commit a3cfaaf"
- PR #26: "_⚠️ Potential issue_ | _🔴 Critical_ <details> <summary>🧩 Analysis chain</summary> 🏁 Script executed: ```shell # First, check if the file exists and read the relevant section cat -n apps/web/app/lib/cron-health-summary.ts | head -170 | tail -20 ``` Repository: lambda-curry/saffron-watchtower Length of output: 748 --- 🏁 Script executed: ```shell # Check AgentNetworkClient definition cat -n apps/web/app/lib/agent-network-client.ts | head -30 ``` Repository: lambda-curry/saffron-watchtower Length of output: 1141 --- 🏁 Script executed: ```shell # Look for Agent type definition rg "type Agent|interface Agent" --type ts --type tsx -A 3 ``` Repository: lambda-curry/saffron-watchtower Length of output: 103 --- 🏁 Script executed: ```shell # Check if there are any TypeScript errors reported fd "cron-health-summary.ts" -x wc -l ``` Repository: lambda-curry/saffron-watchtower Length of output: 157 --- 🏁 Script executed: ```shell # Find the AGENTS constant definition rg "const AGENTS|export.*AGENTS" apps/web/app/lib/cron-health-summary.ts -B 2 -A 5 ``` Repository: lambda-curry/saffron-watchtower Length of output: 57 --- 🏁 Script executed: ```shell # Also check the types/openclaw to understand Agent type cat -n apps/web/app/types/openclaw.ts | head -50 ``` Repository: lambda-curry/saffron-watchtower Length of output: 1403 --- 🏁 Script executed: ```shell # Let's verify if there are actual TypeScript compilation errors fd "tsconfig" -type f | head -5 ``` Repository: lambda-curry/saffron-watchtower Length of output: 246 --- 🏁 Script executed: ```shell # Search for AGENTS constant more broadly rg "AGENTS\s*=" apps/web/app/lib/ -B 2 -A 2 ``` Repository: lambda-curry/saffron-watchtower Length of output: 637 --- 🏁 Script executed: ```shell # Search for Agent interface/type definition rg "export.*Agent|interface Agent|type Agent" apps/web/app/types/ -B 1 -A 5 ``` Repository: lambda-curry/saffron-watchtower Length of output: 400 --- 🏁 Script executed: ```shell # Check the full openclaw.ts file for Agent cat -n apps/web/app/types/openclaw.ts ``` Repository: lambda-curry/saffron-watchtower Length of output: 4939 --- 🏁 Script executed: ```shell # Search for AGENTS constant definition in cron-health-summary.ts cat -n apps/web/app/lib/cron-health-summary.ts | head -30 ``` Repository: lambda-curry/saffron-watchtower Length of output: 1273 --- 🏁 Script executed: ```shell # Search for AgentNetworkLike definition rg "AgentNetworkLike|interface AgentNetworkLike" apps/web/app/lib/ -B 2 -A 10 ``` Repository: lambda-curry/saffron-watchtower Length of output: 3607 --- 🏁 Script executed: ```shell # Check what AGENTS imports or references rg "AGENTS" apps/web/app/lib/cron-health-summary.ts -B 5 -A 2 ``` Repository: lambda-curry/saffron-watchtower Length of output: 605 --- </details> **Fix the readonly/mutable type mismatch in the default `networkClient` initializer.** Line 161 fails TypeScript compilation: `agents` parameter is typed as `readonly Agent[]`, but `AgentNetworkClient` constructor expects a mutable `Agent[]`. Use the spread operator to create a mutable copy. <details> <summary>Proposed fix</summary> ```diff export async function buildCronHealthSummary({ agents = AGENTS, - networkClient = new AgentNetworkClient(agents), + networkClient = new AgentNetworkClient([...agents]), generatedAt = new Date().toISOString(), }: { ``` </details> <!-- suggestion_start --> <details> <summary>📝 Committable suggestion</summary> > ‼️ **IMPORTANT** > Carefully review the code before committing. Ensure that it accurately replaces the highlighted code, contains no missing lines, and has no issues with indentation. Thoroughly test & benchmark the code to ensure it meets the requirements. ```suggestion export async function buildCronHealthSummary({ agents = AGENTS, networkClient = new AgentNetworkClient([...agents]), generatedAt = new Date().toISOString(), }: { agents?: readonly Agent[]; networkClient?: AgentNetworkLike; generatedAt?: string; } = {}): Promise<CronHealthSummarySnapshot> { ``` </details> <!-- suggestion_end --> <details> <summary>🧰 Tools</summary> <details> <summary>🪛 GitHub Actions: PR Quality Checks</summary> [error] 161-161: TypeScript (TS2345): Argument of type 'readonly Agent[]' is not assignable to parameter of type 'Agent[]' because 'readonly' cannot be assigned to mutable type 'Agent[]'. </details> </details> <details> <summary>🤖 Prompt for AI Agents</summary> ``` Verify each finding against the current code and only fix it if needed. In `@apps/web/app/lib/cron-health-summary.ts` around lines 159 - 167, The default initializer for networkClient in buildCronHealthSummary uses AGENTS (typed readonly Agent[]) directly but AgentNetworkClient expects a mutable Agent[]; fix by passing a shallow mutable copy of agents to the constructor (e.g., new AgentNetworkClient([...agents])) so the readonly/mutable type mismatch is resolved; update the networkClient default expression to construct the client from a spread copy of the agents parameter. ``` </details> <!-- fingerprinting:phantom:medusa:grasshopper --> <!-- This is an auto-generated comment by CodeRabbit -->"
- PR #21: "_⚠️ Potential issue_ | _🟡 Minor_ **Unsafe cast for `ackFilter` allows invalid URL values.** If a user manually enters `?ack=invalid`, the cast passes it through, resulting in confusing UI (neither "All" nor "Unacknowledged" button is highlighted). Consider normalizing invalid values to a default. <details> <summary>🛡️ Proposed fix to validate ackFilter</summary> ```diff const [searchParams, setSearchParams] = useSearchParams(); - const ackFilter = (searchParams.get('ack') ?? 'all') as 'all' | 'unacknowledged'; + const ackParam = searchParams.get('ack'); + const ackFilter: 'all' | 'unacknowledged' = ackParam === 'unacknowledged' ? 'unacknowledged' : 'all'; ``` </details> <!-- suggestion_start --> <details> <summary>📝 Committable suggestion</summary> > ‼️ **IMPORTANT** > Carefully review the code before committing. Ensure that it accurately replaces the highlighted code, contains no missing lines, and has no issues with indentation. Thoroughly test & benchmark the code to ensure it meets the requirements. ```suggestion const [searchParams, setSearchParams] = useSearchParams(); const ackParam = searchParams.get('ack'); const ackFilter: 'all' | 'unacknowledged' = ackParam === 'unacknowledged' ? 'unacknowledged' : 'all'; const severityParam = searchParams.get('severity') ?? ''; const categoryParam = searchParams.get('category') ?? ''; const activeSeverities: Set<string> | null = severityParam ? new Set(severityParam.split(',')) : null; const activeCategories: Set<string> | null = categoryParam ? new Set(categoryParam.split(',')) : null; ``` </details> <!-- suggestion_end --> <details> <summary>🤖 Prompt for AI Agents</summary> ``` Verify each finding against the current code and only fix it if needed. In `@apps/todo-app/app/routes/alerts.tsx` around lines 311 - 316, The ackFilter assignment currently unsafely casts any search param to 'all' | 'unacknowledged'; instead read the raw value from searchParams.get('ack'), check if it equals 'unacknowledged' (otherwise treat as 'all'), and assign that validated string to ackFilter (or use a helper like normalizeAck(value): 'all' | 'unacknowledged'); update any code that relies on ackFilter to use this validated value so invalid URL values default to 'all' and the UI highlights consistently. ``` </details> <!-- fingerprinting:phantom:medusa:ocelot --> <!-- This is an auto-generated comment by CodeRabbit --> ✅ Addressed in commit d323f22"
- PR #19: "_⚠️ Potential issue_ | _🟡 Minor_ <details> <summary>🧩 Analysis chain</summary> 🌐 Web query: `In React Router v7, what is the recommended typed pattern for consuming route loader data in a component?` 💡 Result: In React Router v7 framework mode (the recommended approach for data loading with route modules), the typed pattern for consuming route loader data in a component is to destructure loaderData from typed Route.ComponentProps. Example from official docs: import type { Route } from "./+types/product"; export default function Product({ loaderData, }: Route.ComponentProps) { const { name, description } = loaderData; // TypeScript knows the shape of loaderData based on your loader function return ( <div> <h1>{name}</h1> <p>{description}</p> </div> ); } This requires setup with the React Router Vite plugin (`@react-router/dev`), a routes.ts config using route, and type generation enabled (via react-router typegen). Types are auto-generated in ./+types/[route-name] or .react-router/types/, inferring loaderData from your exported loader function's return type. For child components deeper in the tree, use the typed useLoaderData<typeof loader> hook, passing typeof loader (avoids type widening). This is the official, first-class typed pattern, replacing manual LoaderData types or unknown casts from v6. Citations: - 1: https://reactrouter.com/7.0.1/start/framework/data-loading - 2: https://reactrouter.com/7.0.1/start/framework/route-module - 3: https://reactrouter.com/start/framework/data-loading - 4: https://reactrouter.com/7.0.1/explanation/type-safety - 5: https://reactrouter.com/7.12.0/how-to/route-module-type-safety - 6: https://reactrouter.com/7.0.1/start/framework/routing --- 🏁 Script executed: ```shell # Check if project uses React Router v7 framework mode cd apps/todo-app && find . -maxdepth 3 -type f \( -name "package.json" -o -name "react-router.config.ts" -o -name "vite.config.ts" \) | head -20 ``` Repository: lambda-curry/saffron-watchtower Length of output: 159 --- 🏁 Script executed: ```shell # Look at the alerts route file to understand its structure head -50 apps/todo-app/app/routes/alerts.tsx ``` Repository: lambda-curry/saffron-watchtower Length of output: 2026 --- 🏁 Script executed: ```shell # Check for React Router imports and setup in the file grep -n "useLoaderData\|loader\|Route\|AlertsLoaderData" apps/todo-app/app/routes/alerts.tsx | head -30 ``` Repository: lambda-curry/saffron-watchtower Length of output: 944 --- 🏁 Script executed: ```shell # Check package.json to see React Router version and `@react-router/dev` presence cat apps/todo-app/package.json | grep -A 5 -B 5 "react-router" ``` Repository: lambda-curry/saffron-watchtower Length of output: 1447 --- 🏁 Script executed: ```shell # Check if loader function is exported at the point of use grep -n "export.*loader\|export async function loader" apps/todo-app/app/routes/alerts.tsx ``` Repository: lambda-curry/saffron-watchtower Length of output: 136 --- 🏁 Script executed: ```shell # Get full context around the loader definition and its export sed -n '153,210p' apps/todo-app/app/routes/alerts.tsx ``` Repository: lambda-curry/saffron-watchtower Length of output: 2265 --- </details> **Replace the type assertion with a typed generic for type-safe loader data access.** `useLoaderData() as AlertsLoaderData` should be `useLoaderData<typeof loader>()`. This avoids the type assertion and ensures TypeScript infers the shape from your loader's actual return type, preventing silent drift. <details> <summary>🤖 Prompt for AI Agents</summary> ``` Verify each finding against the current code and only fix it if needed. In `@apps/todo-app/app/routes/alerts.tsx` at line 249, The code currently uses a type assertion on useLoaderData() ("useLoaderData() as AlertsLoaderData"); replace that assertion with a typed generic to derive the type from the route loader. Change the call site using useLoaderData so it becomes useLoaderData<typeof loader>() (so variables like loaderAlerts, loaderMonitorEvents, alertHistory keep their existing destructuring), ensuring TypeScript infers the loader return shape instead of relying on AlertsLoaderData assertion. ``` </details> <!-- fingerprinting:phantom:medusa:grasshopper --> <!-- This is an auto-generated comment by CodeRabbit --> ✅ Addressed in commits 5c53668 to 7b55746"
- [+1 more]

### Candidate Rule — Types

**Why (failure mode):** _⚠️ Potential issue_ | _🟡 Minor_ <details> <summary>🧩 Analysis chain</summary> 🏁 Script executed: ```shell #!/bin/bas...

**The Rule:** Prefer precise types over `any` and unsafe assertions. Model contracts with interfaces/types so invalid states are caught at compile time.

**✅ DO:** ```tsx
Verify each finding against the current code and only fix it if needed.

In `@apps/todo-app/app/routes/tokens.tsx` around lines 750 - 789,
computeWindowForecast is using the absolute usedPercent to compute
burnRatePerHour, which is wrong; change the function to accept a previous
snapshot usage (e.g., add parameter previousUsedPercent: number | null) and
compute deltaUsed = usedPercent - previousUsedPercent; if previousUsedPercent is
null or deltaUsed <= 0 return the null/low-confidence result; compute
burnRatePerHour = (deltaUsed) / elapsedHours (not usedPercent), then continue
existing projection logic (update references to burnRatePerHour,
remainingPercent, projectedDepletionMs, depleteBeforeResetMs, willRunOut
accordingly) so forecasts use the usage delta between snapshots instead of the
absolute value.
```

**❌ DON'T:** ```shell
# Check if there's a previous snapshot comparison anywhere
rg -n 'previous|prior|delta|change.*percent' apps/todo-app/app/routes/tokens.tsx
```

**Scope:** alwaysApply: true

*Candidate rule — review before adopting. Frequency=5 in 50 PRs (10.0%). Evidence: 4 PR comments shown above.*

---

## Theme: Imports (6 comments, score 4.0)

### Candidate: Use consistent import/export structure and avoid ambiguous barrel patterns when they reduce clarity or create coupling
*Frequency: 6 · Severity signals: low · Representativeness: high*

**Supporting comments:**
- PR #26: "_⚠️ Potential issue_ | _🔴 Critical_ **Keep `SystemHealthPayload` exported here until consumers are updated.** `apps/web/app/routes/dashboard.tsx` still imports `SystemHealthPayload` from this route module at Lines 36-42 and uses it at Lines 1106-1120. Removing that export in this PR breaks the existing contract. Re-export the type from `~/lib/system-health` here, or update the dashboard import in the same change. <details> <summary>Proposed fix</summary> ```diff import { createFileRoute } from '@tanstack/react-router'; import { buildSystemHealthResponse } from '~/lib/system-health'; +export type { SystemHealthPayload } from '~/lib/system-health'; export const Route = createFileRoute('/api/system-health')({ ``` </details> <!-- suggestion_start --> <details> <summary>📝 Committable suggestion</summary> > ‼️ **IMPORTANT** > Carefully review the code before committing. Ensure that it accurately replaces the highlighted code, contains no missing lines, and has no issues with indentation. Thoroughly test & benchmark the code to ensure it meets the requirements. ```suggestion import { buildSystemHealthResponse } from '~/lib/system-health'; export type { SystemHealthPayload } from '~/lib/system-health'; export const Route = createFileRoute('/api/system-health')({ server: { handlers: { GET: () => buildSystemHealthResponse(), }, }, }); ``` </details> <!-- suggestion_end --> <details> <summary>🤖 Prompt for AI Agents</summary> ``` Verify each finding against the current code and only fix it if needed. In `@apps/web/app/routes/api/system-health.ts` around lines 2 - 10, This route file removed an exported type used elsewhere; re-export SystemHealthPayload from ~/lib/system-health so consumers keep working: add an export line that re-exports the SystemHealthPayload type (e.g., export type { SystemHealthPayload } from '~/lib/system-health') alongside the existing import of buildSystemHealthResponse and ensure the module still defines Route and GET using buildSystemHealthResponse. ``` </details> <!-- fingerprinting:phantom:medusa:grasshopper --> <!-- This is an auto-generated comment by CodeRabbit -->"
- PR #19: "_⚠️ Potential issue_ | _🟠 Major_ **`INSERT OR IGNORE` without a UNIQUE constraint will not prevent duplicate history entries.** The comment on line 146 states the intent is to "Insert history entry only if not already recorded for this alert_id," but `INSERT OR IGNORE` only suppresses constraint violations. Since `alert_id` lacks a UNIQUE constraint, duplicates will be inserted on every call to `acknowledgeAlert` with `alertMeta`. <details> <summary>🐛 Proposed fix — add UNIQUE constraint</summary> ```diff CREATE TABLE IF NOT EXISTS alert_history ( id INTEGER PRIMARY KEY AUTOINCREMENT, - alert_id TEXT NOT NULL, + alert_id TEXT NOT NULL UNIQUE, severity TEXT NOT NULL, category TEXT NOT NULL, agent_id TEXT NOT NULL, agent_name TEXT NOT NULL, message TEXT NOT NULL, first_seen_at INTEGER NOT NULL, acknowledged_by TEXT NOT NULL DEFAULT 'Operator', acknowledged_at INTEGER NOT NULL ); ``` If the table already exists in production, you'll need a migration or `CREATE UNIQUE INDEX IF NOT EXISTS` to add the constraint. </details> <!-- suggestion_start --> <details> <summary>📝 Committable suggestion</summary> > ‼️ **IMPORTANT** > Carefully review the code before committing. Ensure that it accurately replaces the highlighted code, contains no missing lines, and has no issues with indentation. Thoroughly test & benchmark the code to ensure it meets the requirements. ```suggestion CREATE TABLE IF NOT EXISTS alert_history ( id INTEGER PRIMARY KEY AUTOINCREMENT, alert_id TEXT NOT NULL UNIQUE, severity TEXT NOT NULL, category TEXT NOT NULL, agent_id TEXT NOT NULL, agent_name TEXT NOT NULL, message TEXT NOT NULL, first_seen_at INTEGER NOT NULL, acknowledged_by TEXT NOT NULL DEFAULT 'Operator', acknowledged_at INTEGER NOT NULL ); ``` </details> <!-- suggestion_end --> <details> <summary>🤖 Prompt for AI Agents</summary> ``` Verify each finding against the current code and only fix it if needed. In `@apps/todo-app/app/lib/alert-ack-db.ts` around lines 45 - 56, The alert_history table lacks a UNIQUE constraint on alert_id, so the current INSERT OR IGNORE in acknowledgeAlert will not prevent duplicate history rows; add a uniqueness constraint (e.g., UNIQUE(alert_id) on the alert_history table or create a UNIQUE INDEX IF NOT EXISTS on alert_id) and update any migration logic so the existing production table is altered safely; locate the table definition for alert_history and the acknowledgeAlert code path to ensure the INSERT OR IGNORE now relies on that UNIQUE constraint and adjust tests accordingly. ``` </details> <!-- fingerprinting:phantom:medusa:ocelot --> <!-- This is an auto-generated comment by CodeRabbit --> ✅ Addressed in commit 7302883"
- PR #16: "_⚠️ Potential issue_ | _🟠 Major_ **Build the fallback day keys with the same UTC normalizer as `dailyUsage`.** `dailyUsage.date` comes from `toDayKey()` on Line 484-Line 489, which uses UTC. This block builds local-calendar keys instead, so on any non-UTC host a boundary event like `2026-03-30T00:30:00Z` can be stored under `2026-03-30` while the fallback looks for `2026-03-29`. <details> <summary>🗓️ Reuse the existing day-key helper</summary> ```diff - const toLocalDateKey = (date: Date) => { - const year = date.getFullYear(); - const month = String(date.getMonth() + 1).padStart(2, '0'); - const day = String(date.getDate()).padStart(2, '0'); - return `${year}-${month}-${day}`; - }; const currybotSessions = sessionCosts.filter(session => session.agentId === 'currybot'); const currybotTopSessions = [...currybotSessions].sort((a, b) => b.totalCost - a.totalCost).slice(0, 5); const currybotRecentDateKeys = Array.from({ length: 7 }, (_, index) => { const date = new Date(); - date.setHours(0, 0, 0, 0); - date.setDate(date.getDate() - (6 - index)); - return toLocalDateKey(date); + date.setUTCHours(0, 0, 0, 0); + date.setUTCDate(date.getUTCDate() - (6 - index)); + return toDayKey(date.getTime())!; }); ``` </details> <!-- suggestion_start --> <details> <summary>📝 Committable suggestion</summary> > ‼️ **IMPORTANT** > Carefully review the code before committing. Ensure that it accurately replaces the highlighted code, contains no missing lines, and has no issues with indentation. Thoroughly test & benchmark the code to ensure it meets the requirements. ```suggestion const currybotSessions = sessionCosts.filter(session => session.agentId === 'currybot'); const currybotTopSessions = [...currybotSessions].sort((a, b) => b.totalCost - a.totalCost).slice(0, 5); const currybotRecentDateKeys = Array.from({ length: 7 }, (_, index) => { const date = new Date(); date.setUTCHours(0, 0, 0, 0); date.setUTCDate(date.getUTCDate() - (6 - index)); return toDayKey(date.getTime())!; }); const currybotDailyMap = new Map( dailyUsage .filter(day => day.agentId === 'currybot') .map(day => [day.date, day]) ); ``` </details> <!-- suggestion_end --> <details> <summary>🤖 Prompt for AI Agents</summary> ``` Verify each finding against the current code and only fix it if needed. In `@apps/todo-app/app/routes/tokens.tsx` around lines 1731 - 1749, The fallback recent-day keys are being generated with local-time normalization (toLocalDateKey) while dailyUsage.date uses the UTC-based toDayKey; update the recent-key generation to use the same UTC normalizer as toDayKey (or call the shared toDayKey helper) so currybotRecentDateKeys matches dailyUsage.date format, and ensure currybotDailyMap still maps by those UTC day keys (references: toLocalDateKey, toDayKey, currybotRecentDateKeys, dailyUsage, currybotDailyMap). ``` </details> <!-- fingerprinting:phantom:medusa:grasshopper --> <!-- This is an auto-generated comment by CodeRabbit -->"
- PR #13: "_⚠️ Potential issue_ | _🟡 Minor_ **Resolve contradictory PR-status statements in this section.** This block now records PR `#13` as open (Line 29), but still says there are no open PRs (Line 26). Please update/remove the “no open PRs” bullet to keep cycle guidance unambiguous. <details> <summary>Suggested doc fix</summary> ```diff - Confirmed there are no open PRs on the active repo path used by the cycle gate + Confirmed the prior no-PR state changed; PR `#13` is now the active open review path ``` </details> <!-- suggestion_start --> <details> <summary>📝 Committable suggestion</summary> > ‼️ **IMPORTANT** > Carefully review the code before committing. Ensure that it accurately replaces the highlighted code, contains no missing lines, and has no issues with indentation. Thoroughly test & benchmark the code to ensure it meets the requirements. ```suggestion - Recreated repo-local orientation surfaces in `.project/` - Confirmed dirty state was clean before sync-in - Confirmed the prior no-PR state changed; PR `#13` is now the active open review path - Confirmed `jake/cron-delivery-contract` already merged as PR `#9`, so it should no longer be treated as the strongest unmatched feature branch - Replayed the leftover charter-cleanup intent on a fresh branch by removing repo-root lifecycle files (`CHANGELOG.md`, `GOALS.md`, `HEARTBEAT.md`, `SCRATCHPAD.md`) so `.project/` is the sole cycle continuity surface again - Re-validated PR `#13` as draft/open/clean with green checks so the next action can be an explicit ready-for-review decision instead of more branch archaeology ``` </details> <!-- suggestion_end --> <details> <summary>🤖 Prompt for AI Agents</summary> ``` Verify each finding against the current code and only fix it if needed. In @.project/BLOCKERS.md around lines 24 - 29, The "no open PRs" bullet is now contradictory with the later line that records PR `#13` as open; update the `.project/BLOCKERS.md` section by removing or editing the bullet that says "Confirmed there are no open PRs on the active repo path used by the cycle gate" so it no longer contradicts the later statement about PR `#13`, or replace it with an accurate statement that acknowledges PR `#13` is open/draft; ensure the change references the existing bullets about PR `#9` and PR `#13` so the cycle guidance remains unambiguous. ``` </details> <!-- fingerprinting:phantom:triton:hawk --> <!-- This is an auto-generated comment by CodeRabbit -->"
- [+2 more]

### Candidate Rule — Imports

**Why (failure mode):** _⚠️ Potential issue_ | _🟠 Major_ **Date-only alert IDs block same-day escalation paths.** Line 303 generates one ID per...

**The Rule:** Use consistent import/export structure and avoid ambiguous barrel patterns when they reduce clarity or create coupling.

**✅ DO:** ```ts
id: `alert-cost-trend-${today}-${severity}`,
```

**❌ DON'T:** ```ts
id: `alert-cost-trend-${today}`,
```

**Scope:** globs: ["**/*.ts", "**/*.tsx", "**/*.js"]

*Candidate rule — review before adopting. Frequency=6 in 50 PRs (12.0%). Evidence: 4 PR comments shown above.*

---

## Skipped (low signal)
- 0 comments below length threshold
- 0 pure approvals
- 0 questions without suggestions
