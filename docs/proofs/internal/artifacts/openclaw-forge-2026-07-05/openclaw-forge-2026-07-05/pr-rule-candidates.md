# Rule Candidates — lambda-curry/openclaw-forge
*Mined: 2026-07-05 · PRs analyzed: 47 · Comments reviewed: 81 · Candidates surfaced: 6*

---

## Theme: Error Handling (28 comments, score 31.3)

### Candidate: Handle failure paths explicitly
*Frequency: 28 · Severity signals: high · Representativeness: high*

**Supporting comments:**
- PR #38: "`@jaruesink` — confirmed, the fix looks great. The implemented version in the file even improves on the suggestion by including the original parser message in the error (e.g. `"Comparison baseline is not valid JSON: " + compareBaselinePath + " (" + reason + ")"`), which makes diagnostics clearer. Good call adding test coverage for the failure path too. <!-- <review_comment_addressed> --> <!-- This is an auto-generated reply by CodeRabbit -->"
- PR #38: "Fixed in 028c82c: baseline parsing now wraps invalid JSON with a contextual error that includes the baseline path and original parser message, and the focused test suite covers the failure path."
- PR #38: "_⚠️ Potential issue_ | _🟡 Minor_ | _⚡ Quick win_ **Unhandled JSON parse error.** `JSON.parse` throws a `SyntaxError` for malformed JSON, which will bubble up with a cryptic message rather than the context-aware error you provide for other failures. <details> <summary>🛡️ Proposed fix</summary> ```diff - const parsed = JSON.parse(baselineContent) as { - generatedAt?: unknown; - source?: unknown; - candidates?: unknown; - }; + let parsed: { + generatedAt?: unknown; + source?: unknown; + candidates?: unknown; + }; + try { + parsed = JSON.parse(baselineContent); + } catch { + throw new Error("Comparison baseline is not valid JSON: " + compareBaselinePath); + } ``` </details> <!-- suggestion_start --> <details> <summary>📝 Committable suggestion</summary> > ‼️ **IMPORTANT** > Carefully review the code before committing. Ensure that it accurately replaces the highlighted code, contains no missing lines, and has no issues with indentation. Thoroughly test & benchmark the code to ensure it meets the requirements. ```suggestion let parsed: { generatedAt?: unknown; source?: unknown; candidates?: unknown; }; try { parsed = JSON.parse(baselineContent); } catch { throw new Error("Comparison baseline is not valid JSON: " + compareBaselinePath); } ``` </details> <!-- suggestion_end --> <details> <summary>🤖 Prompt for AI Agents</summary> ``` Verify each finding against current code. Fix only still-valid issues, skip the rest with a brief reason, keep changes minimal, and validate. In `@forge/knowledge-extraction/report.ts` around lines 260 - 264, The JSON.parse call for baselineContent in report.ts is unprotected and can throw a SyntaxError; wrap the parse in a try/catch around the line that defines parsed = JSON.parse(baselineContent) (where baselineContent is processed in the report generation flow) and on catch throw or log a new, context-aware error (e.g., "Failed to parse baseline JSON") that includes the original error.message and relevant identifier (such as generatedAt/source/candidates or a snippet of baselineContent) so callers get a clear diagnostic instead of a raw SyntaxError. ``` </details> <!-- fingerprinting:phantom:medusa:ocelot --> <!-- This is an auto-generated reply by CodeRabbit --> ✅ Addressed in commit 028c82c"
- PR #32: "_⚠️ Potential issue_ | _🟡 Minor_ | _⚡ Quick win_ <details> <summary>🧩 Analysis chain</summary> 🌐 Web query: `According to MDN, does Number.parseInt("3foo", 10) return 3, and what is the recommended way to validate an integer string with no trailing characters in JavaScript?` 💡 Result: Yes—per MDN, `Number.parseInt("3foo", 10)` returns `3`, because `parseInt` “ignores [characters] and all succeeding characters” after it has parsed the initial valid integer portion of the string in the specified radix [1][2]. Recommended way to validate that a string is an integer with no trailing characters: - Use a full-match regular expression (anchors) so the entire string must be an integer: `/^[+-]?\d+$/`. - Then (optionally) convert and verify it’s a safe integer with `Number.isSafeInteger` (or `Number.isInteger` depending on your needs). Example: ```js function isIntegerString(s) { return typeof s === "string" && /^[+-]?\d+$/.test(s); } function isSafeIntegerString(s) { if (!isIntegerString(s)) return false; return Number.isSafeInteger(Number(s)); } ``` This approach aligns with MDN’s warning that `parseInt` stops parsing at the first invalid character rather than requiring the whole string to be consumed [1][2]. (For numeric validation after conversion, MDN documents `Number.isInteger()` behavior for integers vs non-integers) [3]. Citations: - 1: https://developer.mozilla.org/en/JavaScript/Reference/Global%5FObjects/parseInt - 2: https://github.com/mdn/content/blob/main/files/en-us/web/javascript/reference/global_objects/parseint/index.md - 3: https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Number/isInteger --- 🏁 Script executed: ```shell #!/bin/bash set -euo pipefail # Show parseArgs implementation around the reported lines rg -n "function parseArgs|parseArgs\(" -S forge/network-health/report.ts || true nl -ba forge/network-health/report.ts | sed -n '1,120p' nl -ba forge/network-health/report.ts | sed -n '120,240p' ``` Repository: lambda-curry/openclaw-forge Length of output: 296 --- 🏁 Script executed: ```shell #!/bin/bash set -euo pipefail # Print parseArgs implementation with line numbers using cat -n cat -n forge/network-health/report.ts | sed -n '1,120p' # Print the exact CLI parsing block where --workspace / --format / --stale-days are handled rg -n "\"--workspace\"|\"--format\"|\"--stale-days\"|parseArgs" forge/network-health/report.ts -n -S ``` Repository: lambda-curry/openclaw-forge Length of output: 4951 --- </details> **Reject empty `--workspace` and partially-numeric `--stale-days`.** - `--workspace ""` currently passes because the “missing value” check only rejects `undefined` or tokens starting with `-`; empty/whitespace workspace values should be rejected. - `--stale-days` uses `Number.parseInt(nextArg, 10)`, which accepts prefixes like `"3foo"` as `3`; validate using a full-string integer check (e.g., regex anchors) before converting. <details> <summary>Suggested fix</summary> ```diff - if (arg === "--workspace") { - if (nextArg === undefined || nextArg.startsWith("-")) { + if (arg === "--workspace") { + if (nextArg === undefined || nextArg.startsWith("-") || nextArg.trim() === "") { throw new Error("Missing value for --workspace"); } workspace = nextArg; i += 1; @@ - } else if (arg === "--stale-days") { - if (nextArg === undefined || nextArg.startsWith("-")) { + } else if (arg === "--stale-days") { + if (nextArg === undefined || nextArg.startsWith("-")) { throw new Error("Missing value for --stale-days"); } - const parsedStaleDays = Number.parseInt(nextArg, 10); - if (!Number.isInteger(parsedStaleDays) || parsedStaleDays < 0) { + if (!/^\d+$/.test(nextArg)) { + throw new Error("Invalid --stale-days: expected a non-negative integer"); + } + const parsedStaleDays = Number(nextArg); + if (!Number.isInteger(parsedStaleDays) || parsedStaleDays < 0) { throw new Error("Invalid --stale-days: expected a non-negative integer"); } staleDays = parsedStaleDays; i += 1; ``` </details> <details> <summary>🤖 Prompt for AI Agents</summary> ``` Verify each finding against current code. Fix only still-valid issues, skip the rest with a brief reason, keep changes minimal, and validate. In `@forge/network-health/report.ts` around lines 35 - 60, The CLI parsing currently accepts empty/whitespace workspace values and allows partially-numeric stale-days; update the --workspace branch (where variable workspace is set) to trim nextArg and throw "Missing value for --workspace" if the trimmed string is empty, and update the --stale-days branch (where parsedStaleDays and staleDays are set) to validate nextArg with a full-string integer check (e.g., /^[0-9]+$/) before converting; if the regex fails, throw the same "Invalid --stale-days: expected a non-negative integer" error, otherwise parseInt and assign staleDays. ``` </details> <!-- fingerprinting:phantom:poseidon:hawk --> <!-- This is an auto-generated comment by CodeRabbit -->"
- [+24 more]

### Candidate Rule — Error Handling

**Why (failure mode):** _⚠️ Potential issue_ | _🔴 Critical_ **Reject target paths inside the package directory tree.** If `targetDir` is `packa...

**The Rule:** Handle failure paths explicitly. For async calls and risky operations, wrap with try/catch (or equivalent) and surface actionable errors instead of allowing silent failures.

**✅ DO:** ```ts
const relativeTarget = path.relative(resolvedPackageDir, resolvedTargetDir);
  if (relativeTarget === "" || (!relativeTarget.startsWith("..") && !path.isAbsolute(relativeTarget))) {
    throw new Error("Target directory must be outside the package directory");
  }
```

**❌ DON'T:** ```ts
export function deployLocal(packageDir: string, targetDir: string): DeployLocalResult {
  const resolvedPackageDir = path.resolve(packageDir);
  const resolvedTargetDir = path.resolve(targetDir);
  const relativeTarget = path.relative(resolvedPackageDir, resolvedTargetDir);

  if (!fs.existsSync(resolvedPackageDir)) {
    throw new Error(`Package directory not found: ${resolvedPackageDir}`);
  }
  if (relativeTarget === "" || (!relativeTarget.startsWith("..") && !path.isAbsolute(relativeTarget))) {
    throw new Error("Target directory must be outside the package directory");
  }

  const copied: string[] = [];
  copyDirRecursive(resolvedPackageDir, resolvedTargetDir, copied, resolvedPackageDir);
```

**Scope:** alwaysApply: true

*Candidate rule — review before adopting. Frequency=28 in 47 PRs (59.6%). Evidence: 4 PR comments shown above.*

---

## Theme: Naming (29 comments, score 23.2)

### Candidate: Use descriptive, convention-aligned names for variables, functions, and classes
*Frequency: 29 · Severity signals: high · Representativeness: high*

**Supporting comments:**
- PR #47: "Keeping the current test-local router harness for this lane. The remaining suggestion is a style-only refactor, and the same helper path was previously unstable in this Bun/React runtime (`React.act is not a function`) during verification here. Since the accessibility bug is fixed and CodeRabbit is green, I am deferring the wider test-harness cleanup instead of widening this PR."
- PR #47: "_⚠️ Potential issue_ | _🟡 Minor_ | _⚡ Quick win_ **Sanitize filenames before using them in tab IDs.** Line 116 and Line 146 compose `id`/`aria-labelledby` from raw filenames. Filenames with spaces or special characters can produce invalid/fragile IDs and weaken tab-to-panel accessibility linkage. <details> <summary>Suggested patch</summary> ```diff +function toDomIdToken(filename: string): string { + return encodeURIComponent(filename).replaceAll('%', '_'); +} + export function StoredExamplePreview({ workspace }: { workspace: GeneratedWorkspaceExampleDetail }) { @@ - id={`${tabListId}-${filename}`} + id={`${tabListId}-${toDomIdToken(filename)}`} @@ - aria-labelledby={`${tabListId}-${currentFile}`} + aria-labelledby={`${tabListId}-${toDomIdToken(currentFile)}`} ``` </details> Also applies to: 116-117, 146-147 <details> <summary>🤖 Prompt for AI Agents</summary> ``` Verify each finding against current code. Fix only still-valid issues, skip the rest with a brief reason, keep changes minimal, and validate. In `@docs-site/apps/network-viz/app/routes/example-preview.tsx` around lines 41 - 43, Filenames are being used directly in id and aria-labelledby attribute composition at lines 116-117 and 146-147, which can produce invalid IDs when filenames contain spaces or special characters, breaking accessibility linkage. Create a filename sanitization function that removes or replaces invalid characters (spaces, special characters, etc.) with valid alternatives (like hyphens or underscores), and apply this sanitization to any raw filename values before they are used to construct the id and aria-labelledby attributes in the tab components. ``` </details> <!-- fingerprinting:phantom:poseidon:hawk --> <!-- cr-comment:v1:82bf8f12b7b9a728c617bb23 --> <!-- This is an auto-generated comment by CodeRabbit --> ✅ Addressed in commit f358d02"
- PR #41: "_⚠️ Potential issue_ | _🟡 Minor_ | _⚡ Quick win_ **Remove redundant aria-label.** The `aria-label` on line 22 duplicates the visible text content (line 27). Since the text is already explicit and descriptive, the aria-label is unnecessary and may cause screen readers to announce the same content twice. Remove the `aria-label` attribute to improve accessibility. <details> <summary>♿ Proposed fix</summary> ```diff return ( <span - aria-label={text} className={`rounded-md border px-2 py-1 text-xs ${ enabled ? enabledClassName : 'border-slate-600 text-slate-400' }`} ``` </details> <!-- suggestion_start --> <details> <summary>📝 Committable suggestion</summary> > ‼️ **IMPORTANT** > Carefully review the code before committing. Ensure that it accurately replaces the highlighted code, contains no missing lines, and has no issues with indentation. Thoroughly test & benchmark the code to ensure it meets the requirements. ```suggestion return ( <span className={`rounded-md border px-2 py-1 text-xs ${ enabled ? enabledClassName : 'border-slate-600 text-slate-400' }`} > {text} </span> ); ``` </details> <!-- suggestion_end --> <details> <summary>🤖 Prompt for AI Agents</summary> ``` Verify each finding against current code. Fix only still-valid issues, skip the rest with a brief reason, keep changes minimal, and validate. In `@docs-site/apps/network-viz/app/components/workspace-capability-pill.tsx` around lines 20 - 29, Remove the redundant aria-label from the span in the WorkspaceCapabilityPill component: the visible {text} is already descriptive so delete the aria-label={text} attribute on the rendered <span> (the JSX that uses text, enabled, and enabledClassName) to avoid duplicate announcements by screen readers. ``` </details> <!-- fingerprinting:phantom:poseidon:puma --> <!-- cr-comment:v1:1bfa21eb87295fe4f2295208 --> <!-- This is an auto-generated comment by CodeRabbit --> ✅ Addressed in commit dabd451"
- PR #40: "_⚠️ Potential issue_ | _🟡 Minor_ | _⚡ Quick win_ **Align the assertion with “every stored file” intent.** Line 71-Line 72 only validate two options. Consider asserting the full option set (or count) so missing files fail this test. <details> <summary>Suggested tightening</summary> ```diff const filePicker = screen.getByRole('combobox', { name: 'Choose a file' }); - expect(screen.getByRole('option', { name: 'TOOLS.md' })).toBeInTheDocument(); - expect(screen.getByRole('option', { name: 'openclaw.json' })).toBeInTheDocument(); + const options = Array.from(filePicker.querySelectorAll('option')).map((o) => o.textContent); + expect(options).toEqual([ + 'SOUL.md', + 'AGENTS.md', + 'IDENTITY.md', + 'USER.md', + 'TOOLS.md', + 'MEMORY.md', + 'openclaw.json', + ]); ``` </details> <details> <summary>🤖 Prompt for AI Agents</summary> ``` Verify each finding against current code. Fix only still-valid issues, skip the rest with a brief reason, keep changes minimal, and validate. In `@docs-site/apps/network-viz/app/routes/example-preview.test.tsx` around lines 70 - 73, The test currently only checks two option elements after grabbing filePicker via screen.getByRole('combobox', { name: 'Choose a file' }); expand the assertion to validate every stored file by either asserting the exact count of option elements or comparing the set of option names to the expected list; locate the getByRole('option', ...) checks (and the filePicker variable) and replace them with a single assertion that queries all options (e.g., screen.getAllByRole('option')) and then verifies length or that the returned option names match the expected filenames. ``` </details> <!-- fingerprinting:phantom:poseidon:hawk --> <!-- This is an auto-generated comment by CodeRabbit --> ✅ Addressed in commit ad41e79"
- [+25 more]

### Candidate Rule — Naming

**Why (failure mode):** _⚠️ Potential issue_ | _🟠 Major_ <details> <summary>🧩 Analysis chain</summary> 🏁 Script executed: ```shell # First, c...

**The Rule:** Use descriptive, convention-aligned names for variables, functions, and classes. Favor consistency with existing project naming standards.

**✅ DO:** ```ts
import { fileURLToPath } from "url";
const FORGE_DIR = path.dirname(fileURLToPath(import.meta.url));
```

**❌ DON'T:** ```shell
# Search for all instances of import.meta.url and fileURLToPath usage in the file
rg 'import\.meta\.url|fileURLToPath' forge/generator.ts -n
```

**Scope:** alwaysApply: true

*Candidate rule — review before adopting. Frequency=29 in 47 PRs (61.7%). Evidence: 4 PR comments shown above.*

---

## Theme: Documentation (7 comments, score 4.9)

### Candidate: Document intent when behavior is non-obvious
*Frequency: 7 · Severity signals: low · Representativeness: high*

**Supporting comments:**
- PR #47: "`@jaruesink` — understood, that's a solid reason to keep the manual harness. A `React.act is not a function` runtime failure in Bun would make the helper actively harmful here, not just a style difference. Deferring the harness cleanup to a focused follow-up PR is the right call. <!-- <review_comment_withdrawn> --> --- <details> <summary>✏️ Learnings added</summary> ``` Learnt from: jaruesink Repo: lambda-curry/openclaw-forge PR: 47 File: docs-site/apps/network-viz/app/routes/example-preview.test.tsx:183-193 Timestamp: 2026-06-19T20:06:08.692Z Learning: In `docs-site/apps/network-viz/app/routes/example-preview.test.tsx` (Bun test + React Testing Library), the shared `renderWithRouter` helper from `docs-site/apps/network-viz/test/test-utils.tsx` is unstable at runtime — it produces `React.act is not a function` in the Bun/React test environment. Tests in this file intentionally use a manual harness (`createRoot`, `flushSync`, `MemoryRouter`) instead of `renderWithRouter` for this reason. Do not suggest migrating to `renderWithRouter` until the Bun/React runtime compatibility issue is resolved. ``` > Note: Learnings are effective only in the context of similar code segments. To apply general coding guidelines, please configure [review instructions](https://docs.coderabbit.ai/guides/review-instructions). You can manage existing learnings in the [app](https://app.coderabbit.ai/learnings). </details> <!-- This is an auto-generated reply by CodeRabbit -->"
- PR #38: "`@jaruesink`, thanks for the fix! Confirmed — suppressed candidates now correctly carry only `status`/`disposition`, keeping the machine-readable semantics clean. <!-- <review_comment_addressed> --> <!-- This is an auto-generated reply by CodeRabbit -->"
- PR #10: "`@jaruesink`, thanks for the confirmation! Removing the ternary entirely is the clean solution, and good to see the full suite (35 passing) backing it up. ✅ <!-- <review_comment_addressed> --> <!-- This is an auto-generated reply by CodeRabbit -->"
- PR #7: "_⚠️ Potential issue_ | _🟡 Minor_ **Fix the charter link in the generated project README.** From `projects/<agentId>/README.md`, `../charters/...` resolves under `projects/charters`, not the workspace root. The generated governance link is broken here; it should match the `../../charters/...` form already used in `buildProjectAgents()`. <details> <summary>🛠️ Proposed fix</summary> ```diff - `- Charter source of truth: ../charters/${manifest.agent.id}/AGENTS.md`, + `- Charter source of truth: ../../charters/${manifest.agent.id}/AGENTS.md`, ``` </details> <details> <summary>🤖 Prompt for AI Agents</summary> ``` Verify each finding against the current code and only fix it if needed. In `@forge/factory-v2/project-surface.ts` around lines 102 - 105, The generated README governance charter link uses a single-level relative path that resolves incorrectly from projects/<agentId>/README.md; update the string in project-surface.ts where the README content is built (the line producing `- Charter source of truth: ../charters/${manifest.agent.id}/AGENTS.md`) to use the same two-level relative path as buildProjectAgents() (i.e. `../../charters/${manifest.agent.id}/AGENTS.md`) so the link points to the workspace root charters directory. ``` </details> <!-- fingerprinting:phantom:medusa:grasshopper --> <!-- This is an auto-generated comment by CodeRabbit --> ✅ Addressed in commit 8f5e974"
- [+3 more]

### Candidate Rule — Documentation

**Why (failure mode):** _⚠️ Potential issue_ | _🟠 Major_ **Archetype taxonomy here diverges from the implemented generator archetypes.** This d...

**The Rule:** Document intent when behavior is non-obvious. Keep README/comments aligned with current code behavior.

**✅ DO:** ```ts
`- Charter source of truth: ../../charters/${manifest.agent.id}/AGENTS.md`,
```

**❌ DON'T:** ```ts
`- Charter source of truth: ../charters/${manifest.agent.id}/AGENTS.md`,
```

**Scope:** alwaysApply: true

*Candidate rule — review before adopting. Frequency=7 in 47 PRs (14.9%). Evidence: 4 PR comments shown above.*

---

## Theme: Testing (6 comments, score 4.3)

### Candidate: Add or update tests when behavior changes
*Frequency: 6 · Severity signals: low · Representativeness: high*

**Supporting comments:**
- PR #46: "_🛠️ Refactor suggestion_ | _🟠 Major_ | _⚡ Quick win_ **Add explicit non-leak assertions for Bridge transport internals.** These tests build fixtures with `url` and `tokenFile` but never assert they are absent from output. That leaves the “no Bridge URLs/tokens/internal transport exposure” requirement unguarded. <details> <summary>Suggested test hardening</summary> ```diff const report = JSON.parse(stdout); + const serialized = JSON.stringify(report); + expect(serialized).not.toContain("http://atticus.example"); + expect(serialized).not.toContain("atticus.token"); + expect(serialized).not.toContain("http://meg.example"); + expect(serialized).not.toContain("meg.token"); ``` ```diff expect(stdout).toContain("Provenance incomplete: yes"); + expect(stdout).not.toContain("http://atticus.example"); + expect(stdout).not.toContain("atticus.token"); ``` </details> Also applies to: 341-399 <details> <summary>🤖 Prompt for AI Agents</summary> ``` Verify each finding against current code. Fix only still-valid issues, skip the rest with a brief reason, keep changes minimal, and validate. In `@forge/agent-cards/report.test.ts` around lines 54 - 179, The test "emits target-driven cards with narrow owned charter data" builds fixture data with Bridge transport internals like url and tokenFile fields, but does not explicitly assert that these sensitive fields are absent from the report output. Add explicit assertions after the existing expect statements for both the atticus and meg card objects to verify that Bridge transport details (url and tokenFile) are not present in the generated report, and similarly verify that the ownedCharters arrays within each card do not expose any Bridge internal information. This ensures the test actively guards against unintended leakage of sensitive transport configuration. ``` </details> <!-- fingerprinting:phantom:poseidon:hawk --> <!-- cr-comment:v1:9e9cfbc6834985e93a2f464e --> _Source: MCP tools_ <!-- This is an auto-generated comment by CodeRabbit --> ✅ Addressed in commit f2f39eb"
- PR #39: "_⚠️ Potential issue_ | _🟠 Major_ | _🏗️ Heavy lift_ **Refactor to use `renderWithRouter` and React Testing Library queries.** The test violates project testing guidelines by using `createRoot` + `MemoryRouter` directly and `querySelector` with attribute selectors. Since `StoredExamplePreview` contains `Link` components, the test should use the shared `renderWithRouter` helper and React Testing Library queries (`getByRole`, etc.) instead of brittle DOM selectors. As per coding guidelines: "Use React Testing Library for component and DOM testing", "Use `renderWithRouter` for any component that uses `Link`, `useNavigate`, `useFetcher`, or `useHref`", and "Prefer `getByRole`, `getByLabelText`, `getByPlaceholderText` over brittle selectors". <details> <summary>♻️ Suggested refactor using renderWithRouter and RTL</summary> ```diff -import { describe, expect, test } from 'bun:test'; -import { createRoot } from 'react-dom/client'; -import { flushSync } from 'react-dom'; -import { MemoryRouter } from 'react-router'; +import { describe, expect, test } from 'bun:test'; +import { screen } from '`@testing-library/react`'; +import { renderWithRouter } from '~/test/helpers'; // adjust path to your helper import type { GeneratedWorkspaceExampleDetail } from '~/lib/examples.server'; import { StoredExamplePreview } from './example-preview'; // ... WORKSPACE fixture ... describe('StoredExamplePreview', () => { test('supports keyboard navigation across stored workspace tabs', () => { - const container = document.createElement('div'); - document.body.appendChild(container); - const root = createRoot(container); - - flushSync(() => { - root.render( - <MemoryRouter> - <StoredExamplePreview workspace={WORKSPACE} /> - </MemoryRouter> - ); - }); + renderWithRouter(<StoredExamplePreview workspace={WORKSPACE} />); - const soulTab = container.querySelector<HTMLButtonElement>('[role="tab"][id$="SOUL.md"]'); - expect(soulTab).not.toBeNull(); - soulTab.focus(); + const soulTab = screen.getByRole('tab', { name: /SOUL\.md/i }); + soulTab.focus(); - flushSync(() => { - soulTab?.dispatchEvent(new KeyboardEvent('keydown', { bubbles: true, key: 'ArrowRight' })); - }); + soulTab.dispatchEvent(new KeyboardEvent('keydown', { bubbles: true, key: 'ArrowRight' })); - const agentsTab = container.querySelector<HTMLButtonElement>('[role="tab"][id$="AGENTS.md"]'); - expect(agentsTab).not.toBeNull(); + const agentsTab = screen.getByRole('tab', { name: /AGENTS\.md/i }); expect(agentsTab).toHaveFocus(); expect(agentsTab).toHaveAttribute('aria-selected', 'true'); - expect(container.querySelector('[role="tabpanel"]')).toHaveTextContent('Agent content'); + expect(screen.getByRole('tabpanel')).toHaveTextContent('Agent content'); - flushSync(() => { - agentsTab?.dispatchEvent(new KeyboardEvent('keydown', { bubbles: true, key: 'End' })); - }); + agentsTab.dispatchEvent(new KeyboardEvent('keydown', { bubbles: true, key: 'End' })); - const configTab = container.querySelector<HTMLButtonElement>('[role="tab"][id$="openclaw.json"]'); - expect(configTab).not.toBeNull(); + const configTab = screen.getByRole('tab', { name: /openclaw\.json/i }); expect(configTab).toHaveFocus(); expect(configTab).toHaveAttribute('aria-selected', 'true'); - expect(container.querySelector('[role="tabpanel"]')).toHaveTextContent('"name": "Code Reviewer"'); - - flushSync(() => { - root.unmount(); - }); - container.remove(); + expect(screen.getByRole('tabpanel')).toHaveTextContent('"name": "Code Reviewer"'); }); }); ``` </details> Also applies to: 32-42, 44-66 <details> <summary>🤖 Prompt for AI Agents</summary> ``` Verify each finding against current code. Fix only still-valid issues, skip the rest with a brief reason, keep changes minimal, and validate. In `@docs-site/apps/network-viz/app/routes/example-preview.test.tsx` around lines 1 - 6, Replace the manual React DOM mounting and querySelector usage with the project's test helpers and RTL queries: import and use the shared renderWithRouter to render StoredExamplePreview (so Link/navigation works) instead of createRoot/MemoryRouter, then replace attribute-based querySelector assertions with React Testing Library queries such as getByRole/getByText/getByTestId as appropriate (e.g., assert link presence via getByRole('link', { name: /.../ }) and button/text via getByRole or getByText). Update tests in example-preview.test.tsx (affecting the blocks covering StoredExamplePreview and the ranges mentioned) to remove direct DOM access and use the RTL return utilities from renderWithRouter for assertions. ``` </details> <!-- fingerprinting:phantom:poseidon:puma --> <!-- This is an auto-generated comment by CodeRabbit -->"
- PR #39: "_⚠️ Potential issue_ | _🟡 Minor_ | _⚡ Quick win_ **Consider adding test coverage for ArrowLeft and Home keys.** The test verifies ArrowRight and End key navigation but doesn't exercise ArrowLeft or Home. Adding these cases would provide complete coverage of the keyboard navigation implementation. <details> <summary>🧪 Suggested additional test assertions</summary> ```diff expect(configTab).toHaveFocus(); expect(configTab).toHaveAttribute('aria-selected', 'true'); expect(screen.getByRole('tabpanel')).toHaveTextContent('"name": "Code Reviewer"'); + + // Test ArrowLeft wraps to previous tab + configTab.dispatchEvent(new KeyboardEvent('keydown', { bubbles: true, key: 'ArrowLeft' })); + const memoryTab = screen.getByRole('tab', { name: /MEMORY\.md/i }); + expect(memoryTab).toHaveFocus(); + expect(memoryTab).toHaveAttribute('aria-selected', 'true'); + + // Test Home jumps to first tab + memoryTab.dispatchEvent(new KeyboardEvent('keydown', { bubbles: true, key: 'Home' })); + expect(soulTab).toHaveFocus(); + expect(soulTab).toHaveAttribute('aria-selected', 'true'); }); }); ``` </details> <details> <summary>🤖 Prompt for AI Agents</summary> ``` Verify each finding against current code. Fix only still-valid issues, skip the rest with a brief reason, keep changes minimal, and validate. In `@docs-site/apps/network-viz/app/routes/example-preview.test.tsx` around lines 31 - 72, Add assertions to the test 'supports keyboard navigation across stored workspace tabs' to also simulate ArrowLeft and Home key navigation: after moving focus from soulTab -> agentsTab via ArrowRight, dispatch a KeyboardEvent with key 'ArrowLeft' on agentsTab and assert focus and aria-selected return to soulTab and the tabpanel shows 'Soul content'; then from agentsTab dispatch 'Home' and assert focus and aria-selected move to the first tab (soulTab) and the tabpanel reflects the first tab content; reference the StoredExamplePreview render and existing DOM queries soulTab, agentsTab, configTab to locate where to insert these additional dispatchEvent and expect checks. ``` </details> <!-- fingerprinting:phantom:poseidon:puma --> <!-- This is an auto-generated comment by CodeRabbit --> ✅ Addressed in commit 6596331"
- PR #38: "_⚠️ Potential issue_ | _🟡 Minor_ | _⚡ Quick win_ <details> <summary>🧩 Analysis chain</summary> 🏁 Script executed: ```shell #!/bin/bash rg -nP --type=ts -C3 '\brejectionReason\b' forge/knowledge-extraction/report.ts ``` Repository: lambda-curry/openclaw-forge Length of output: 1612 --- 🏁 Script executed: ```shell #!/bin/bash set -euo pipefail # 1) Find all places where rejectionReason is set/assigned rg -nP --type=ts '\brejectionReason\s*[:=]' forge/knowledge-extraction/report.ts # 2) Find all places where suppressed candidates are constructed rg -nP --type=ts '\b(suppressed)\b' forge/knowledge-extraction/report.ts -C3 # 3) Look for logic that conditionally pushes "Why not now" (rejectionReason) rg -nP --type=ts 'Why not now|candidate\.rejectionReason' forge/knowledge-extraction/report.ts -C3 ``` Repository: lambda-curry/openclaw-forge Length of output: 3303 --- 🏁 Script executed: ```shell #!/bin/bash set -euo pipefail # Inspect the suppressed candidate construction area in the generator nl -ba forge/knowledge-extraction/report.ts | sed -n '420,510p' # Inspect the rejected candidate construction area nl -ba forge/knowledge-extraction/report.ts | sed -n '560,640p' # Inspect where/when rejectionReason is set (broader scan) rg -nP --type=ts '\brejectionReason\b' forge/knowledge-extraction/report.ts -n # Inspect the generated JSON around the reported lines (120-125) nl -ba data/knowledge-extraction-report-2026-05-31.json | sed -n '95,155p' ``` Repository: lambda-curry/openclaw-forge Length of output: 114 --- 🏁 Script executed: ```shell #!/bin/bash set -euo pipefail # Use awk for numbered line output (nl not available) echo "=== forge/knowledge-extraction/report.ts (suppressed candidate area) ===" awk 'NR>=430 && NR<=490 {printf "%d:%s\n", NR, $0}' forge/knowledge-extraction/report.ts echo "=== forge/knowledge-extraction/report.ts (rejected candidate area) ===" awk 'NR>=560 && NR<=620 {printf "%d:%s\n", NR, $0}' forge/knowledge-extraction/report.ts echo "=== Repo search: SFE-502 references ===" rg -n --hidden --glob '!.git/*' 'SFE-502' . || true echo "=== Repo search: rejectionReason usage ===" rg -n --hidden --glob '!.git/*' '\brejectionReason\b' . || true echo "=== Repo search: dispositionReason usage ===" rg -n --hidden --glob '!.git/*' '\bdispositionReason\b' . || true ``` Repository: lambda-curry/openclaw-forge Length of output: 9227 --- </details> **Avoid treating `rejectionReason` as a proxy for `rejected`, since `suppressed` candidates also set it.** `forge/knowledge-extraction/report.ts`’s `applySharedCoverageReduction()` returns `status: "suppressed"` / `disposition: "suppressed"` while also assigning `rejectionReason` (the “Already captured in shared cycle guidance…” string). `SFE-502` references in the repo only mention candidateId-level disposition stability, not `rejectionReason` semantics. Update the generator to omit `rejectionReason` for suppressed candidates, or update any machine-readable consumer to key off `status`/`disposition` rather than `rejectionReason` presence. <details> <summary>🤖 Prompt for AI Agents</summary> ``` Verify each finding against current code. Fix only still-valid issues, skip the rest with a brief reason, keep changes minimal, and validate. In `@data/knowledge-extraction-report-2026-05-31.json` around lines 120 - 125, applySharedCoverageReduction() is emitting suppressed candidates with a populated rejectionReason which causes consumers to incorrectly infer rejection; update the generator inside applySharedCoverageReduction() (and any helper that builds the candidate object) to skip setting the rejectionReason field when status === "suppressed" or disposition === "suppressed" so suppressed items only carry status/disposition, or alternatively adjust downstream consumers to determine rejection solely from status/disposition (i.e., key off candidate.status/candidate.disposition instead of presence of candidate.rejectionReason). ``` </details> <!-- fingerprinting:phantom:poseidon:capybara --> <!-- This is an auto-generated reply by CodeRabbit --> ✅ Addressed in commit 028c82c"
- [+2 more]

### Candidate Rule — Testing

**Why (failure mode):** _⚠️ Potential issue_ | _🟡 Minor_ | _⚡ Quick win_ <details> <summary>🧩 Analysis chain</summary> 🏁 Script executed: ```...

**The Rule:** Add or update tests when behavior changes. Prefer tests that assert observable behavior and cover likely regressions.

**✅ DO:** ```ts
const baseDir = path.resolve(workspacesDir);
  const resolved = path.resolve(workspacePath);
  if (resolved !== baseDir && !resolved.startsWith(baseDir + path.sep)) return null;
```

**❌ DON'T:** ```ts
export function getWorkspaceExampleById(id: string): GeneratedWorkspaceExampleDetail | null {
  const workspacesDir = resolveWorkspacesDir();
  if (!workspacesDir) return null;

  const workspacePath = path.join(workspacesDir, id);
  const baseDir = path.resolve(workspacesDir);
  const resolved = path.resolve(workspacePath);
  if (resolved !== baseDir && !resolved.startsWith(baseDir + path.sep)) return null;
  if (!fs.existsSync(workspacePath) || !fs.statSync(workspacePath).isDirectory()) return null;

  return buildWorkspaceExample(id, workspacesDir);
}
```

**Scope:** globs: ["**/*.test.ts", "**/*.spec.ts", "**/*.test.tsx"]

*Candidate rule — review before adopting. Frequency=6 in 47 PRs (12.8%). Evidence: 4 PR comments shown above.*

---

## Theme: Types (4 comments, score 3.3)

### Candidate: Prefer precise types over `any` and unsafe assertions
*Frequency: 4 · Severity signals: medium · Representativeness: medium*

**Supporting comments:**
- PR #37: "_⚠️ Potential issue_ | _🟡 Minor_ | _⚡ Quick win_ <details> <summary>🧩 Analysis chain</summary> 🌐 Web query: `React Router 7 route module +types Route.LoaderArgs Route.ComponentProps loaderData usage` 💡 Result: In React Router 7, type safety is achieved by automatically generating types for each route based on your route configuration (defined in app/routes.ts). These types are stored in a virtual./+types/[route-name] directory, which you import into your route modules to provide typed interfaces for loaders, actions, and components [1]. Route.LoaderArgs The Route.LoaderArgs type is used to type the argument object passed to your loader function. This object typically includes properties like params (the route parameters) and request (the fetch Request object) [2][3][4]. import type { Route } from "./+types/my-route"; export async function loader({ params }: Route.LoaderArgs) { const data = await fetchData(params.id); return { data }; } Route.ComponentProps The Route.ComponentProps type provides the type for the props object received by your route's default component. This prop object automatically includes fully-typed versions of loaderData, actionData, params, and matches [2][1]. Using these props is an alternative to using hooks like useLoaderData or useParams, and they offer superior type safety because the types are derived directly from your loader and route configuration [2][5][3]. import type { Route } from "./+types/my-route"; export default function MyComponent({ loaderData }: Route.ComponentProps) { // loaderData is automatically typed based on the return value of the loader return <div>{loaderData.data}</div>; } Key Usage Notes 1. Automatic Generation: Types are generated automatically when running the development server (react-router dev) or via the typegen command (react-router typegen). TypeScript is configured to resolve the./+types/ import path to these generated files [1]. 2. loaderData: This property on Route.ComponentProps corresponds specifically to the return value of your loader function. If the loader is async, the type will represent the resolved value [2][5][1]. 3. Why Use Props over Hooks: Using Route.ComponentProps allows you to avoid manual hook typing (e.g., useLoaderData<typeof loader>). It simplifies component signatures and ensures consistency across the route module [2][1]. Citations: - 1: https://reactrouter.com/explanation/type-safety - 2: https://reactrouter.com/7.6.0/start/framework/route-module - 3: https://reactrouter.com/start/framework/routing - 4: https://www.youtube.com/watch?v=h7MTWLv3xvw - 5: https://reactrouter.com/7.6.0/start/framework/data-loading --- </details> **Use React Router generated `+types` route types in `example-preview` (remove manual params typing + `useLoaderData` cast).** In `docs-site/apps/network-viz/app/routes/example-preview.tsx`, type the loader args with the generated `Route.LoaderArgs` and type the component with `Route.ComponentProps` (typed `loaderData`) instead of hand-written `params`/`LoaderData` types and `useLoaderData() as LoaderData`. <details> <summary>🤖 Prompt for AI Agents</summary> ``` Verify each finding against current code. Fix only still-valid issues, skip the rest with a brief reason, keep changes minimal, and validate. In `@docs-site/apps/network-viz/app/routes/example-preview.tsx` around lines 34 - 45, Replace the manual params typing and useLoaderData cast by using the generated route types from the +types file: change the loader signature to accept Route.LoaderArgs (e.g. function loader({ params }: Route.LoaderArgs) and keep the workspace return using getWorkspaceExampleById), remove the hand-rolled LoaderData type, and type the page component with Route.ComponentProps so you receive typed loaderData (access loaderData.workspace) instead of calling useLoaderData() as LoaderData; update imports to pull the Route types from the route +types file and remove the manual types and the useLoaderData cast in ExamplePreviewPage. ``` </details> <!-- fingerprinting:phantom:poseidon:capybara --> <!-- This is an auto-generated comment by CodeRabbit -->"
- PR #6: "_⚠️ Potential issue_ | _🔴 Critical_ <details> <summary>🧩 Analysis chain</summary> 🏁 Script executed: ```shell #!/bin/bash rg -n '^type CharterKind\b' scripts/diff-workspace.ts # Expect exactly one match after the fix. ``` Repository: lambda-curry/openclaw-forge Length of output: 207 --- </details> **Remove the duplicate `CharterKind` declaration.** Line 116 re-declares the alias from Line 79. In TypeScript this is a duplicate identifier error (TS2300), causing the script to fail compilation. <details> <summary>💡 Minimal fix</summary> ```diff type CharterKind = "product" | "process" | "unknown-structural"; @@ - type CharterKind = "product" | "process" | "unknown-structural"; ``` </details> <!-- suggestion_start --> <details> <summary>📝 Committable suggestion</summary> > ‼️ **IMPORTANT** > Carefully review the code before committing. Ensure that it accurately replaces the highlighted code, contains no missing lines, and has no issues with indentation. Thoroughly test & benchmark the code to ensure it meets the requirements. ```suggestion type CharterKind = "product" | "process" | "unknown-structural"; const UNIVERSAL_CHARTER_FILES = [ "AGENTS.md", "IDENTITY.md", "TOOLS.md", "USER.md", "SOUL.md", "HEARTBEAT.md", "MEMORY.md", ] as const; const PRODUCT_CHARTER_FILES = [ ".project/ROADMAP.md", ".project/BLOCKERS.md", ".project/SCRATCHPAD.md", ] as const; const PROCESS_CHARTER_FILES = [ "GOALS.md", "SCRATCHPAD.md", ] as const; const CHARTER_OPTIONAL_FILES = [ "memory", "data", "README.md", "cycle-evidence", ".project/setup.md", ] as const; const FORGE_METADATA_FILES = [ ".forge/archetype.json", ".forge/interview.json", ".skills", ] as const; ``` </details> <!-- suggestion_end --> <details> <summary>🤖 Prompt for AI Agents</summary> ``` Verify each finding against the current code and only fix it if needed. In `@scripts/diff-workspace.ts` around lines 79 - 117, The file declares the type alias CharterKind twice causing TS2300; remove the second duplicate declaration `type CharterKind = "product" | "process" | "unknown-structural";` (the later one near the bottom of the snippet) so only the original `CharterKind` alias (the first declaration) remains; after removal, run tsc to confirm no duplicate identifier errors and that all uses of CharterKind still resolve. ``` </details> <!-- fingerprinting:phantom:medusa:grasshopper --> <!-- This is an auto-generated comment by CodeRabbit -->"
- PR #2: "_⚠️ Potential issue_ | _🔴 Critical_ **The generated answers JSON is never passed back into `generate-workspace.ts`.** Line 421 writes a temp answers file, but Lines 430-444 only forward a subset of fields as CLI flags. `userContext` from Q3 and `extraContext` from Q7 are serialized and then dropped, and any future answer that only exists in the JSON file will be ignored too. <details> <summary>One way to wire the file through</summary> ```diff -// Write answers JSON -if (answersOut) { - writeFileSync(answersOut, JSON.stringify(answersJson, null, 2)); +const answersFilePath = answersOut ?? join(process.cwd(), ".forge-interview-temp.json"); +if (answersOut || !noGenerate) { + writeFileSync(answersFilePath, JSON.stringify(answersJson, null, 2)); +} +if (answersOut) { console.log(fmt(` ✅ Answers saved → ${answersOut}\n`, GREEN)); -} else if (!noGenerate) { - // Will be written into .forge/ by generate-workspace.ts; also save a temp copy - const tmpPath = join(process.cwd(), ".forge-interview-temp.json"); - writeFileSync(tmpPath, JSON.stringify(answersJson, null, 2)); } @@ const args: string[] = [ "run", generatorPath, + "--answers-file", answersFilePath, "--archetype", answers.archetype, "--tone", answers.tone, ``` </details> Also applies to: 430-444 <details> <summary>🤖 Prompt for AI Agents</summary> ``` Verify each finding against the current code and only fix it if needed. In `@scripts/interview.ts` around lines 399 - 423, The temp JSON written to tmpPath (".forge-interview-temp.json") contains fields (answersJson, e.g., userContext and extraContext) that are not forwarded to generate-workspace.ts; modify the code path that launches or calls generate-workspace.ts (the branch where answersOut is falsy and noGenerate is falsy) to pass the temp file path through as a CLI arg (e.g. --answers-file <tmpPath>) instead of only forwarding the existing individual flags, and update generate-workspace.ts to read and merge that file when present so all keys in answersJson are preserved; ensure the logic around answersOut, tmpPath, writeFileSync and the caller of generate-workspace.ts uses that new --answers-file argument. ``` </details> <!-- fingerprinting:phantom:medusa:grasshopper --> <!-- This is an auto-generated comment by CodeRabbit -->"
- PR #2: "_⚠️ Potential issue_ | _🔴 Critical_ <details> <summary>🧩 Analysis chain</summary> 🏁 Script executed: ```shell #!/bin/bash set -euo pipefail rg -n -C2 'USER_CONTEXT:|userContext|interview\.tools' \ --iglob '*update-workspace.ts' \ --iglob '*generate-workspace.ts' ``` Repository: lambda-curry/openclaw-forge Length of output: 2791 --- 🏁 Script executed: ```shell #!/bin/bash set -euo pipefail # Find and examine the InterviewMeta interface definition echo "=== InterviewMeta interface definition ===" rg -A10 'interface InterviewMeta' scripts/update-workspace.ts # Check context around the problematic lines (257-276) echo "" echo "=== Context around lines 257-276 ===" sed -n '255,278p' scripts/update-workspace.ts # Check context around the problematic lines (363-380) echo "" echo "=== Context around lines 363-380 ===" sed -n '361,383p' scripts/update-workspace.ts ``` Repository: lambda-curry/openclaw-forge Length of output: 2603 --- </details> **Add missing `userContext` field to `InterviewMeta` interface and use it consistently.** The `InterviewMeta` interface is missing the `userContext` field that is needed to round-trip user context across update flows. Currently: - Line 270 incorrectly maps `USER_CONTEXT` to `interview.tools` instead of user context - Line 367 attempts to access the non-existent `interview.userContext`, silently losing context data - Line 380 uses the raw prompt value instead of preserving the stored context This causes user context to be replaced by the tools list during `/forge update soul`, or wiped entirely during `/forge update user` when the user provides a blank answer. <details> <summary>Required changes</summary> ```diff interface InterviewMeta { userName?: string; userCallName?: string; userTimezone?: string; + userContext?: string; tools?: string; skills?: string[]; extraContext?: string; interviewedAt?: string; } - USER_CONTEXT: interview.tools ?? "", + USER_CONTEXT: interview.userContext ?? "", - USER_CONTEXT: context, + USER_CONTEXT: newInterview.userContext ?? "", ``` </details> Also applies to: 257-276, 363-380 <details> <summary>🤖 Prompt for AI Agents</summary> ``` Verify each finding against the current code and only fix it if needed. In `@scripts/update-workspace.ts` around lines 58 - 66, Add a new optional userContext?: string field to the InterviewMeta interface and update the round-trip mapping and usages so USER_CONTEXT maps to interview.userContext (not interview.tools); change any code that writes USER_CONTEXT from the prompt raw value to use interview.userContext as the preserved fallback when the user submits a blank answer (so current stored context is not overwritten), and update reads that access interview.userContext (instead of the non-existent property) so user context is consistently stored and restored across the update flows (fix occurrences around the USER_CONTEXT mapping, where interview.tools was used, and where prompt/raw prompt values were incorrectly used). ``` </details> <!-- fingerprinting:phantom:medusa:grasshopper --> <!-- This is an auto-generated comment by CodeRabbit -->"

### Candidate Rule — Types

**Why (failure mode):** _⚠️ Potential issue_ | _🔴 Critical_ <details> <summary>🧩 Analysis chain</summary> 🏁 Script executed: ```shell #!/bin/...

**The Rule:** Prefer precise types over `any` and unsafe assertions. Model contracts with interfaces/types so invalid states are caught at compile time.

**✅ DO:** ```ts
const answersFilePath = answersOut ?? join(process.cwd(), ".forge-interview-temp.json");
if (answersOut || !noGenerate) {
  writeFileSync(answersFilePath, JSON.stringify(answersJson, null, 2));
}
if (answersOut) {
    "--answers-file", answersFilePath,
```

**❌ DON'T:** ```ts
// Write answers JSON
if (answersOut) {
  writeFileSync(answersOut, JSON.stringify(answersJson, null, 2));
} else if (!noGenerate) {
  // Will be written into .forge/ by generate-workspace.ts; also save a temp copy
  const tmpPath = join(process.cwd(), ".forge-interview-temp.json");
  writeFileSync(tmpPath, JSON.stringify(answersJson, null, 2));
```

**Scope:** alwaysApply: true

*Candidate rule — review before adopting. Frequency=4 in 47 PRs (8.5%). Evidence: 4 PR comments shown above.*

---

## Theme: Imports (4 comments, score 2.5)

### Candidate: Use consistent import/export structure and avoid ambiguous barrel patterns when they reduce clarity or create coupling
*Frequency: 4 · Severity signals: low · Representativeness: high*

**Supporting comments:**
- PR #34: "_⚠️ Potential issue_ | _🟠 Major_ | _⚡ Quick win_ **Preserve bounded-contract evidence when cross-charter filtering yields none.** Line 309 currently drops same-charter bounded cues, which can produce a candidate without explicit bounded-contract evidence even though Line 289 requires it. That weakens the audit trail promised by this report. <details> <summary>Suggested fix</summary> ```diff - evidence: makeEvidence( - [ - ...decisionArtifactCues.slice(0, 2), - ...boundedContractCues.filter((cue) => cue.charter !== decisionArtifactCues[0]?.charter).slice(0, 2), - ].filter(Boolean) as SurfaceCue[], - 4, - ), + evidence: makeEvidence( + (() => { + const primaryDecisionCharter = decisionArtifactCues[0]?.charter; + const preferredBounded = boundedContractCues.filter( + (cue) => cue.charter !== primaryDecisionCharter, + ); + const boundedForEvidence = (preferredBounded.length > 0 + ? preferredBounded + : boundedContractCues + ).slice(0, 2); + + return [...decisionArtifactCues.slice(0, 2), ...boundedForEvidence]; + })(), + 4, + ), ``` </details> <!-- suggestion_start --> <details> <summary>📝 Committable suggestion</summary> > ‼️ **IMPORTANT** > Carefully review the code before committing. Ensure that it accurately replaces the highlighted code, contains no missing lines, and has no issues with indentation. Thoroughly test & benchmark the code to ensure it meets the requirements. ```suggestion evidence: makeEvidence( (() => { const primaryDecisionCharter = decisionArtifactCues[0]?.charter; const preferredBounded = boundedContractCues.filter( (cue) => cue.charter !== primaryDecisionCharter, ); const boundedForEvidence = (preferredBounded.length > 0 ? preferredBounded : boundedContractCues ).slice(0, 2); return [...decisionArtifactCues.slice(0, 2), ...boundedForEvidence]; })(), 4, ), ``` </details> <!-- suggestion_end --> <details> <summary>🤖 Prompt for AI Agents</summary> ``` Verify each finding against current code. Fix only still-valid issues, skip the rest with a brief reason, keep changes minimal, and validate. In `@forge/knowledge-extraction/report.ts` around lines 306 - 312, The current evidence assembly uses decisionArtifactCues and a filtered boundedContractCues list that excludes cues sharing the same charter as decisionArtifactCues[0], which can yield zero bounded-contract items and violate the later requirement for bounded-contract evidence; update the logic inside the makeEvidence call (the code building evidence from decisionArtifactCues and boundedContractCues) to first compute the cross-charter filtered bounded list, then if that filtered list is empty fall back to taking the first boundedContractCues (i.e., use filtered.slice(0,2) if filtered.length>0 else boundedContractCues.slice(0,2)), and then concatenate with decisionArtifactCues before calling makeEvidence so bounded-contract evidence is always preserved. ``` </details> <!-- fingerprinting:phantom:poseidon:hawk --> <!-- This is an auto-generated comment by CodeRabbit --> ✅ Addressed in commit 8b7ec16"
- PR #11: "_⚠️ Potential issue_ | _🟠 Major_ | _⚡ Quick win_ **Keep terminal and playground legacy command families non-overlapping** Line 13 currently pulls `scripts/generate.ts` into the terminal lane, but Line 14 already assigns the quick-generator family to playground. This weakens the “pick one named track” contract. <details> <summary>Suggested doc alignment</summary> ```diff -| **Legacy terminal generator** | Older terminal-driven single-surface generation | Secondary | `scripts/interview.ts`, `scripts/generate-workspace.ts`, `scripts/generate.ts` | generated legacy workspace under a local output directory | +| **Legacy terminal generator** | Older terminal-driven single-surface generation | Secondary | `scripts/interview.ts`, `scripts/generate-workspace.ts`, `scripts/update-workspace.ts`, `scripts/review-workspace.ts` | generated legacy workspace under a local output directory | ``` </details> <!-- suggestion_start --> <details> <summary>📝 Committable suggestion</summary> > ‼️ **IMPORTANT** > Carefully review the code before committing. Ensure that it accurately replaces the highlighted code, contains no missing lines, and has no issues with indentation. Thoroughly test & benchmark the code to ensure it meets the requirements. ```suggestion | **Legacy terminal generator** | Older terminal-driven single-surface generation | Secondary | `scripts/interview.ts`, `scripts/generate-workspace.ts`, `scripts/update-workspace.ts`, `scripts/review-workspace.ts` | generated legacy workspace under a local output directory | | **Legacy playground preview** | Browser preview of the legacy generator contract | Secondary | `/playground`, `scripts/generate.ts quick` | `generated/<agent-name>/` style legacy preview | ``` </details> <!-- suggestion_end --> <details> <summary>🤖 Prompt for AI Agents</summary> ``` Verify each finding against the current code and only fix it if needed. In `@docs-site/apps/network-viz/app/content/QUICKSTART.md` around lines 13 - 14, The table currently lists scripts/generate.ts under the "Legacy terminal generator" row while the "Legacy playground preview" row already maps the quick-generator family to scripts/generate.ts quick and /playground, causing overlap; update the table so each command family is unique by removing scripts/generate.ts from the terminal generator row and leaving only scripts/interview.ts and scripts/generate-workspace.ts there (or alternatively move the quick entry out of the playground row), ensuring the terminal lane references only scripts/interview.ts and scripts/generate-workspace.ts and the playground lane references scripts/generate.ts quick and /playground. ``` </details> <!-- fingerprinting:phantom:poseidon:hawk --> <!-- This is an auto-generated comment by CodeRabbit --> ✅ Addressed in commit 650ad0c"
- PR #5: "_⚠️ Potential issue_ | _🟡 Minor_ **Remove the stale six-file checklist entry.** This checklist now mixes the old Forge-only baseline with the broader charter baseline and repeats `.forge/archetype.json`, so it can tell operators they are done while required charter files are still missing. Collapse this to the new baseline once. <details> <summary>📝 Suggested cleanup</summary> ```diff - - [ ] All 6 workspace files written - - [ ] .forge/archetype.json written - [ ] Required workspace files written - - [ ] .forge/archetype.json written + - [ ] Forge metadata written for Forge-generated workspaces - [ ] `.skills` present when the generated workspace uses a Forge-managed skills manifest ``` </details> <!-- suggestion_start --> <details> <summary>📝 Committable suggestion</summary> > ‼️ **IMPORTANT** > Carefully review the code before committing. Ensure that it accurately replaces the highlighted code, contains no missing lines, and has no issues with indentation. Thoroughly test & benchmark the code to ensure it meets the requirements. ```suggestion After generation, confirm: - [ ] Required workspace files written - [ ] Forge metadata written for Forge-generated workspaces - [ ] `.skills` present when the generated workspace uses a Forge-managed skills manifest ``` </details> <!-- suggestion_end --> <details> <summary>🤖 Prompt for AI Agents</summary> ``` Verify each finding against the current code and only fix it if needed. In `@skills/forge/SKILL.md` around lines 161 - 166, The checklist in SKILL.md contains a stale "All 6 workspace files written" item and a duplicate ".forge/archetype.json" entry; remove the "All 6 workspace files written" line and the duplicate .forge/archetype.json checkbox, and collapse the checklist to the single new baseline that includes required workspace files, .forge/archetype.json, and the `.skills` presence when using a Forge-managed skills manifest so the checklist only reflects the current charter baseline. ``` </details> <!-- fingerprinting:phantom:medusa:grasshopper --> <!-- This is an auto-generated comment by CodeRabbit -->"
- PR #2: "_⚠️ Potential issue_ | _🟠 Major_ **Back up `.skills` before overwriting it.** Every other update path preserves the previous file with `backupFile(...)`, but Line 517 replaces `.skills` in place. That loses the old manifest even though the command footer says backups are saved as `*.forge-backup`. <details> <summary>Minimal fix</summary> ```diff if (dryRun) { console.log(`\n${c.yellow("--- DRY RUN: .skills ---")}`); console.log(content); } else { + backupFile(destPath); writeFileSync(destPath, content); const newInterview = { ...interview, skills: newSkills }; ``` </details> <!-- suggestion_start --> <details> <summary>📝 Committable suggestion</summary> > ‼️ **IMPORTANT** > Carefully review the code before committing. Ensure that it accurately replaces the highlighted code, contains no missing lines, and has no issues with indentation. Thoroughly test & benchmark the code to ensure it meets the requirements. ```suggestion if (dryRun) { console.log(`\n${c.yellow("--- DRY RUN: .skills ---")}`); console.log(content); } else { backupFile(destPath); writeFileSync(destPath, content); const newInterview = { ...interview, skills: newSkills }; saveInterviewMeta(workspaceDir, newInterview, dryRun); console.log(`\n${c.green("✓ .skills updated:")} ${newSkills.join(", ")}`); ``` </details> <!-- suggestion_end --> <details> <summary>🤖 Prompt for AI Agents</summary> ``` Verify each finding against the current code and only fix it if needed. In `@scripts/update-workspace.ts` around lines 513 - 521, Before overwriting .skills, call the existing backupFile helper to preserve the previous manifest (same backup pattern used elsewhere) — i.e., invoke backupFile for the target .skills file prior to writeFileSync(destPath, content); keep the dryRun branch unchanged so backups are only made when not a dry run, and leave saveInterviewMeta(newInterview, ...) as-is. Ensure you use the same backup naming convention (*.forge-backup) as the other update paths by using the backupFile symbol. ``` </details> <!-- fingerprinting:phantom:medusa:grasshopper --> <!-- This is an auto-generated comment by CodeRabbit -->"

### Candidate Rule — Imports

**Why (failure mode):** _⚠️ Potential issue_ | _🟠 Major_ **Back up `.skills` before overwriting it.** Every other update path preserves the pre...

**The Rule:** Use consistent import/export structure and avoid ambiguous barrel patterns when they reduce clarity or create coupling.

**✅ DO:** ```ts
backupFile(destPath);
```

**❌ DON'T:** ```ts
if (dryRun) {
    console.log(`\n${c.yellow("--- DRY RUN: .skills ---")}`);
    console.log(content);
  } else {
    backupFile(destPath);
    writeFileSync(destPath, content);

    const newInterview = { ...interview, skills: newSkills };
    saveInterviewMeta(workspaceDir, newInterview, dryRun);
    console.log(`\n${c.green("✓ .skills updated:")} ${newSkills.join(", ")}`);
```

**Scope:** globs: ["**/*.ts", "**/*.tsx", "**/*.js"]

*Candidate rule — review before adopting. Frequency=4 in 47 PRs (8.5%). Evidence: 4 PR comments shown above.*

---

## Skipped (low signal)
- 0 comments below length threshold
- 0 pure approvals
- 0 questions without suggestions
