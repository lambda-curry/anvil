# Rule Candidates — lambda-curry/podcast-platform
*Mined: 2026-07-05 · PRs analyzed: 47 · Comments reviewed: 102 · Candidates surfaced: 7*

---

## Theme: Error Handling (30 comments, score 34.6)

### Candidate: Handle failure paths explicitly
*Frequency: 30 · Severity signals: high · Representativeness: high*

**Supporting comments:**
- PR #25: "_⚠️ Potential issue_ | _🟠 Major_ <details> <summary>🧩 Analysis chain</summary> 🏁 Script executed: ```shell #!/bin/bash # Verify the type of imageUrl in RssFeedContract ast-grep --pattern 'type $_ = { $$$ imageUrl$_ $$$ }' # Check how imageUrl is defined in the RSS parser rg -n "imageUrl" packages/rss/src/parser.ts -C2 ``` Repository: lambda-curry/podcast-platform Length of output: 695 --- 🏁 Script executed: ```shell # Find and examine ShowCover component find . -name "*show-cover*" -type f | head -20 ``` Repository: lambda-curry/podcast-platform Length of output: 113 --- 🏁 Script executed: ```shell # Check the ShowCoverProps type definition rg -n "type ShowCoverProps|interface ShowCoverProps" --type ts --type tsx -A 8 ``` Repository: lambda-curry/podcast-platform Length of output: 100 --- 🏁 Script executed: ```shell # Verify the feed.show type definition rg -n "feed\.show" apps/web/src/routes/shows.\$slug.\$episodeId.tsx -B 3 -A 3 | head -50 ``` Repository: lambda-curry/podcast-platform Length of output: 2016 --- 🏁 Script executed: ```shell # Read the ShowCover component file cat -n ./apps/web/src/components/show-cover.tsx ``` Repository: lambda-curry/podcast-platform Length of output: 1911 --- 🏁 Script executed: ```shell # Also check the types file for show and feed types rg -n "type.*FeedListener|interface.*FeedListener" apps/web/src --type ts --type tsx -A 15 | head -60 ``` Repository: lambda-curry/podcast-platform Length of output: 100 --- 🏁 Script executed: ```shell # Find the feed/FeedListener type definition rg -n "feed\s*:" apps/web/src/routes/shows.\$slug.\$episodeId.tsx -B 5 -A 2 | head -40 ``` Repository: lambda-curry/podcast-platform Length of output: 1254 --- 🏁 Script executed: ```shell # Check the ParsedRssFeed type which defines the shape of feed.show rg -n "type ParsedRssFeed|interface ParsedRssFeed" packages/rss/src -A 20 ``` Repository: lambda-curry/podcast-platform Length of output: 1350 --- 🏁 Script executed: ```shell # Check if there are any tsconfig or build errors being reported find . -name "tsconfig.json" -type f -exec head -50 {} \; ``` Repository: lambda-curry/podcast-platform Length of output: 1506 --- 🏁 Script executed: ```shell # Verify that feed.show is typed as RssFeedContract to confirm type flow rg -n "type RssFeedContract\|interface RssFeedContract" apps/web/src -A 15 ``` Repository: lambda-curry/podcast-platform Length of output: 55 --- 🏁 Script executed: ```shell # Check if there's a difference between ParsedRssFeed and RssFeedContract rg -n "RssFeedContract" packages/rss/src -B 2 -A 10 ``` Repository: lambda-curry/podcast-platform Length of output: 10797 --- 🏁 Script executed: ```shell # Check the RssFeedContract definition in contract.ts rg -n "rssFeedContractSchema\|showSchema" packages/rss/src/contract.ts -A 20 | head -80 ``` Repository: lambda-curry/podcast-platform Length of output: 55 --- 🏁 Script executed: ```shell # Read the contract.ts file to see RssFeedContract definition cat -n packages/rss/src/contract.ts | head -100 ``` Repository: lambda-curry/podcast-platform Length of output: 3324 --- </details> **Type mismatch: `feed.show.imageUrl` can be `undefined` but `ShowCover` requires `string`.** Per `packages/rss/src/parser.ts` (line 190), `imageUrl` is assigned as `imageUrl ?? undefined`, making it `string | undefined`. However, `ShowCoverProps` declares `imageUrl: string` as required. This violates the type contract. Either update `ShowCoverProps` to accept `string | undefined` and handle it internally, or provide a fallback here: <details> <summary>🛠️ Option A: Fix at the call site with fallback</summary> ```diff - <ShowCover imageUrl={feed.show.imageUrl} title={feed.show.title} size="md" /> + <ShowCover imageUrl={feed.show.imageUrl ?? ""} title={feed.show.title} size="md" /> ``` </details> <details> <summary>🛠️ Option B: Update ShowCoverProps to handle undefined (preferred)</summary> In `apps/web/src/components/show-cover.tsx`: ```diff type ShowCoverProps = { - imageUrl: string; + imageUrl: string | undefined; title: string; size?: "sm" | "md" | "lg"; className?: string; }; ``` Then update the component to show fallback when `imageUrl` is falsy: ```diff - if (hasError) { + if (hasError || !imageUrl) { ``` </details> <!-- suggestion_start --> <details> <summary>📝 Committable suggestion</summary> > ‼️ **IMPORTANT** > Carefully review the code before committing. Ensure that it accurately replaces the highlighted code, contains no missing lines, and has no issues with indentation. Thoroughly test & benchmark the code to ensure it meets the requirements. ```suggestion <div className="flex items-start gap-6"> <ShowCover imageUrl={feed.show.imageUrl ?? ""} title={feed.show.title} size="md" /> <div className="space-y-3 min-w-0"> <p className="text-sm font-semibold uppercase tracking-[0.2em] text-[--color-text-muted]"> Ready to listen </p> <h1 className="text-4xl font-bold tracking-tight">{episode.title}</h1> <p className="text-base leading-7 text-[--color-text-muted]">{episode.description}</p> <p className="text-sm leading-7 text-[--color-text-muted]"> Read the episode details, check the show context, and press play when you are ready. </p> </div> </div> ``` </details> <!-- suggestion_end --> <details> <summary>🤖 Prompt for AI Agents</summary> ``` Verify each finding against the current code and only fix it if needed. In `@apps/web/src/routes/shows`.$slug.$episodeId.tsx around lines 238 - 250, feed.show.imageUrl is string | undefined but ShowCover requires imageUrl: string; update the contract so types align. Preferred: change ShowCoverProps in show-cover.tsx to imageUrl?: string and update the ShowCover component to handle a missing image (render a placeholder/avatar or null) and use a safe alt/title fallback; alternatively, if you want to keep ShowCoverProps as required, provide a call-site fallback here by passing feed.show.imageUrl ?? '<fallback>' (or an empty string) when rendering <ShowCover>. Ensure all references to ShowCover (props and usage) reflect the new optional type. ``` </details> <!-- fingerprinting:phantom:poseidon:ocelot --> <!-- This is an auto-generated comment by CodeRabbit -->"
- PR #25: "_⚠️ Potential issue_ | _🟠 Major_ **Same type mismatch: `feed.show.imageUrl` may be `undefined`.** Consistent with the other usages, `feed.show.imageUrl` can be `undefined` but `ShowCover` requires a `string`. Apply the same fix pattern as recommended for the other files. <details> <summary>🛠️ Proposed fix</summary> ```diff - <ShowCover imageUrl={feed.show.imageUrl} title={feed.show.title} size="lg" /> + <ShowCover imageUrl={feed.show.imageUrl ?? ""} title={feed.show.title} size="lg" /> ``` Or update `ShowCover` to accept `string | undefined` as the centralized fix. </details> <!-- suggestion_start --> <details> <summary>📝 Committable suggestion</summary> > ‼️ **IMPORTANT** > Carefully review the code before committing. Ensure that it accurately replaces the highlighted code, contains no missing lines, and has no issues with indentation. Thoroughly test & benchmark the code to ensure it meets the requirements. ```suggestion <div className="flex items-start gap-6"> <ShowCover imageUrl={feed.show.imageUrl ?? ""} title={feed.show.title} size="lg" /> ``` </details> <!-- suggestion_end --> <details> <summary>🤖 Prompt for AI Agents</summary> ``` Verify each finding against the current code and only fix it if needed. In `@apps/web/src/routes/shows`.$slug.tsx around lines 71 - 72, feed.show.imageUrl may be undefined but ShowCover expects a string; update the call in the component rendering (where ShowCover is used) to pass a guaranteed string (e.g., provide a fallback like an empty string or placeholder URL) or change ShowCover's prop type to accept string | undefined and handle undefined internally; reference the ShowCover component and the feed.show.imageUrl expression when making the change so the type mismatch is resolved consistently with other usages. ``` </details> <!-- fingerprinting:phantom:poseidon:ocelot --> <!-- This is an auto-generated comment by CodeRabbit -->"
- PR #23: "_⚠️ Potential issue_ | _🟡 Minor_ **Avoid leaking internal error messages in 500 responses.** Exposing `error.message` directly to the client could leak sensitive implementation details (e.g., database connection strings, internal paths, or stack traces from certain error types). Return a generic message instead. <details> <summary>🛡️ Proposed fix</summary> ```diff - const message = error instanceof Error ? error.message : "Unexpected error."; - return new Response(JSON.stringify({ error: message }), { + return new Response(JSON.stringify({ error: "Unexpected error." }), { status: 500, headers: { "Content-Type": "application/json" }, }); ``` </details> If you need the error details for debugging, log them server-side before returning the generic response. <!-- suggestion_start --> <details> <summary>📝 Committable suggestion</summary> > ‼️ **IMPORTANT** > Carefully review the code before committing. Ensure that it accurately replaces the highlighted code, contains no missing lines, and has no issues with indentation. Thoroughly test & benchmark the code to ensure it meets the requirements. ```suggestion return new Response(JSON.stringify({ error: "Unexpected error." }), { status: 500, headers: { "Content-Type": "application/json" }, }); ``` </details> <!-- suggestion_end --> <details> <summary>🤖 Prompt for AI Agents</summary> ``` Verify each finding against the current code and only fix it if needed. In `@apps/web/functions/api/shows/`[slug]/episodes/[episodeId].ts around lines 52 - 56, Do not return internal error.message to the client: inside the handler where the code sets const message = error instanceof Error ? error.message : "Unexpected error." and returns new Response(JSON.stringify({ error: message }), ...), replace the client-facing payload with a generic message (e.g., "Internal server error") and instead log the actual error server-side (use your existing logger or console.error(error)) before returning the generic 500 response; ensure you still preserve a 500 status and JSON content-type. ``` </details> <!-- fingerprinting:phantom:poseidon:ocelot --> <!-- This is an auto-generated comment by CodeRabbit -->"
- PR #18: "_⚠️ Potential issue_ | _🟠 Major_ **Imported drafts with missing audio metadata become unloadable after reload** `createDraftFromParsedFeed` stores `audioBytes`/`durationSeconds` as `0`, but reload normalization rejects non-positive integers. Result: imported drafts can fail to load after refresh and appear lost. <details> <summary>🛠️ Proposed fix</summary> ```diff - audioBytes: asPositiveInteger(source.audioBytes), - durationSeconds: asPositiveInteger(source.durationSeconds), + audioBytes: asNonNegativeInteger(source.audioBytes), + durationSeconds: asNonNegativeInteger(source.durationSeconds), ``` ```diff -function asPositiveInteger(input: unknown): number { +function asNonNegativeInteger(input: unknown): number { const numericValue = typeof input === "number" ? input : Number(input); - if (!Number.isInteger(numericValue) || numericValue <= 0) { - throw new TypeError(`Expected a positive integer, got: ${String(input)}`); + if (!Number.isInteger(numericValue) || numericValue < 0) { + throw new TypeError(`Expected a non-negative integer, got: ${String(input)}`); } return numericValue; } ``` </details> <details> <summary>🤖 Prompt for AI Agents</summary> ``` Verify each finding against the current code and only fix it if needed. In `@apps/web/src/lib/studio-state.ts` around lines 182 - 183, Imported drafts are being set with audioBytes and durationSeconds as 0 in createDraftFromParsedFeed which later fails normalization that rejects non-positive integers; change the assignment in createDraftFromParsedFeed to leave those fields undefined (or omit them) when the parsed feed value is missing or non-positive so normalization accepts the draft. Specifically, in createDraftFromParsedFeed replace setting audioBytes: ep.audioBytes ?? 0 and durationSeconds: ep.durationSeconds ?? 0 with logic that only sets audioBytes/durationSeconds when ep.audioBytes > 0 / ep.durationSeconds > 0 (or use undefined when absent) so the normalization path that validates positive integers will not reject valid imports. ``` </details> <!-- fingerprinting:phantom:poseidon:hawk --> <!-- This is an auto-generated comment by CodeRabbit -->"
- [+26 more]

### Candidate Rule — Error Handling

**Why (failure mode):** _⚠️ Potential issue_ | _🟠 Major_ <details> <summary>🧩 Analysis chain</summary> 🏁 Script executed: ```shell # Check if...

**The Rule:** Handle failure paths explicitly. For async calls and risky operations, wrap with try/catch (or equivalent) and surface actionable errors instead of allowing silent failures.

**✅ DO:** ```yaml
steps:
  - uses: actions/checkout@v4
  - uses: oven-sh/setup-bun@v2
    with:
      bun-version: 1.3.3
  - run: bun install --frozen-lockfile
  - run: bun test
```

**❌ DON'T:** ```toml
[install]
     frozenLockfile = true
```

**Scope:** alwaysApply: true

*Candidate rule — review before adopting. Frequency=30 in 47 PRs (63.8%). Evidence: 4 PR comments shown above.*

---

## Theme: Naming (19 comments, score 13.0)

### Candidate: Use descriptive, convention-aligned names for variables, functions, and classes
*Frequency: 19 · Severity signals: medium · Representativeness: high*

**Supporting comments:**
- PR #46: "_⚠️ Potential issue_ | _🟠 Major_ **Show chooser can be bypassed; submit still routes to a hardcoded show.** When the chooser is shown, users can submit immediately without picking a show, and navigation falls back to `foundry` (Line 200). That breaks the intent of the new show-choice step. <details> <summary>Suggested fix</summary> ```diff async function completeAuth(nextMode: AuthMode, nextDisplayName: string, nextEmail: string) { + if (destinationChoiceState.shouldChooseShow) { + setErrorMessage("Choose a show before continuing."); + return; + } + const normalizedDisplayName = nextDisplayName.trim(); const normalizedEmail = nextEmail.trim().toLowerCase(); const normalizedPassword = password; @@ await navigate({ to: "/studio", search: { - show: redirectParams.get("show") ?? "foundry", + show: redirectParams.get("show") ?? destinationOptions[0]?.slug ?? "foundry", }, }); @@ void navigate({ to: "/studio", search: { - show: destinationShowSlug ?? "foundry", + show: destinationShowSlug ?? destinationOptions[0]?.slug ?? "foundry", }, }); ``` </details> Also applies to: 236-272, 332-337 <details> <summary>🤖 Prompt for AI Agents</summary> ``` Verify each finding against the current code and only fix it if needed. In `@apps/web/src/routes/sign-in.tsx` around lines 230 - 233, The sign-in flow currently allows submitting while destinationChoiceState.shouldChooseShow is true and defaults navigation to the hardcoded 'foundry' value—fix this in the submit handler used for sign-in (the function that reads destinationLabel/destinationChoiceState and performs the navigation) by making it validate that when destinationChoiceState.shouldChooseShow is true a show is selected before routing; if no show is selected, prevent submission and surface an inline validation error or disable the submit action, and only fall back to 'foundry' when destinationChoiceState.shouldChooseShow is false; update any related logic that computes destinationLabel and the routing decision (the branch that currently defaults to 'foundry') to respect the chosen show. ``` </details> <!-- fingerprinting:phantom:poseidon:hawk --> <!-- This is an auto-generated comment by CodeRabbit --> ✅ Addressed in commits 79ca4d5 to 09b31dc"
- PR #43: "_⚠️ Potential issue_ | _🔴 Critical_ **Block fork PR code from running on self-hosted runner.** Line 14 combined with the `pull_request` trigger allows untrusted PR code to run on your self-hosted machine (checkout/install/build happen before Cloudflare-secret checks). Add a job-level guard for same-repo PRs (or move PR runs to GitHub-hosted runners). <details> <summary>Suggested guard</summary> ```diff jobs: deploy: name: Deploy web preview + if: github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository runs-on: [self-hosted, macos, arm64] ``` </details> <details> <summary>🤖 Prompt for AI Agents</summary> ``` Verify each finding against the current code and only fix it if needed. In @.github/workflows/web-preview-deploy.yml at line 14, The workflow currently uses a self-hosted runner (runs-on: [self-hosted, macos, arm64]) and allows pull_request events to execute untrusted fork code; add a job-level guard using the GitHub context to only run PR jobs when the PR is from the same repo (e.g., add an if condition using github.event_name and github.event.pull_request.head.repo.full_name compared to github.repository) or alternatively move pull_request-triggered runs to GitHub-hosted runners; update the job that uses runs-on to include this if-check so forked PRs will be skipped on the self-hosted runner. ``` </details> <!-- fingerprinting:phantom:poseidon:hawk --> <!-- This is an auto-generated comment by CodeRabbit -->"
- PR #42: "_⚠️ Potential issue_ | _🔴 Critical_ **Add a trust gate before running PR previews on self-hosted runners.** This workflow executes build/deploy preparation steps on `pull_request`; without a trust check, forked PRs can run arbitrary code on your private runner. <details> <summary>🔒 Suggested guard</summary> ```diff jobs: deploy: name: Deploy web preview + if: github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository runs-on: [self-hosted, macos, arm64] ``` </details> <details> <summary>🤖 Prompt for AI Agents</summary> ``` Verify each finding against the current code and only fix it if needed. In @.github/workflows/web-preview-deploy.yml at line 10, The job currently targeting self-hosted mac runners (runs-on: [self-hosted, macos, arm64]) needs a trust gate to block forked PRs; add a job-level condition that only runs for PRs from the same repo (for example add if: github.event.pull_request.head.repo.full_name == github.repository on the job that uses runs-on) so the preview steps won't execute for forked PRs, referencing the runs-on line as the place to add this guard. ``` </details> <!-- fingerprinting:phantom:poseidon:hawk --> <!-- This is an auto-generated comment by CodeRabbit -->"
- PR #40: "_⚠️ Potential issue_ | _🟠 Major_ **Upsert currently clears `shows.feedUrl` on reseed.** At Line 47, `showRecord.feedUrl` is always `null`; at Line 74 this now overwrites existing rows on conflict. That can silently wipe previously populated feed URLs during production reseeding. <details> <summary>Suggested fix</summary> ```diff .onConflictDoUpdate({ target: shows.id, set: { slug: showRecord.slug, title: showRecord.title, description: showRecord.description, author: showRecord.author, subtitle: showRecord.subtitle, summary: showRecord.summary, ownerName: showRecord.ownerName, ownerEmail: showRecord.ownerEmail, category: showRecord.category, subcategory: showRecord.subcategory, type: showRecord.type, explicit: showRecord.explicit, imageUrl: showRecord.imageUrl, language: showRecord.language, copyright: showRecord.copyright, websiteUrl: showRecord.websiteUrl, - feedUrl: showRecord.feedUrl, applePodcastsUrl: showRecord.applePodcastsUrl, spotifyUrl: showRecord.spotifyUrl, }, }); ``` </details> <!-- suggestion_start --> <details> <summary>📝 Committable suggestion</summary> > ‼️ **IMPORTANT** > Carefully review the code before committing. Ensure that it accurately replaces the highlighted code, contains no missing lines, and has no issues with indentation. Thoroughly test & benchmark the code to ensure it meets the requirements. ```suggestion .onConflictDoUpdate({ target: shows.id, set: { slug: showRecord.slug, title: showRecord.title, description: showRecord.description, author: showRecord.author, subtitle: showRecord.subtitle, summary: showRecord.summary, ownerName: showRecord.ownerName, ownerEmail: showRecord.ownerEmail, category: showRecord.category, subcategory: showRecord.subcategory, type: showRecord.type, explicit: showRecord.explicit, imageUrl: showRecord.imageUrl, language: showRecord.language, copyright: showRecord.copyright, websiteUrl: showRecord.websiteUrl, applePodcastsUrl: showRecord.applePodcastsUrl, spotifyUrl: showRecord.spotifyUrl, }, }); ``` </details> <!-- suggestion_end --> <details> <summary>🤖 Prompt for AI Agents</summary> ``` Verify each finding against the current code and only fix it if needed. In `@packages/db/src/seed.ts` at line 74, The upsert is overwriting existing shows.feedUrl with null because showRecord.feedUrl is null; update the upsert/ON CONFLICT merge so it preserves existing feedUrl when the incoming showRecord.feedUrl is null—e.g., in the INSERT ... ON CONFLICT ... DO UPDATE clause (the upsert call where showRecord is used) set feedUrl = COALESCE(EXCLUDED.feedUrl, shows.feedUrl) or only include feedUrl in the update if showRecord.feedUrl is not null; locate the code around the showRecord variable and the upsert/insert(...).onConflict(...).merge()/doUpdate() call in seed.ts and change the feedUrl update to use COALESCE or a conditional merge to avoid wiping populated feedUrl values. ``` </details> <!-- fingerprinting:phantom:poseidon:hawk --> <!-- This is an auto-generated comment by CodeRabbit -->"
- [+15 more]

### Candidate Rule — Naming

**Why (failure mode):** _⚠️ Potential issue_ | _🟡 Minor_ **Resolve naming-rule contradiction in examples.** The guideline bans `res`, but the “...

**The Rule:** Use descriptive, convention-aligned names for variables, functions, and classes. Favor consistency with existing project naming standards.

**✅ DO:** ```md
it("should return 404 for unknown show slug", async () => {
  const response = await app.fetch(new Request("http://localhost/rss/unknown-show.xml"));
  expect(response.status).toBe(404);
```

**❌ DON'T:** ```md
it("should return 404 for unknown show slug", async () => {
  const res = await app.fetch(new Request("http://localhost/rss/unknown-show.xml"));
  expect(res.status).toBe(404);
```

**Scope:** alwaysApply: true

*Candidate rule — review before adopting. Frequency=19 in 47 PRs (40.4%). Evidence: 4 PR comments shown above.*

---

## Theme: Testing (17 comments, score 11.5)

### Candidate: Add or update tests when behavior changes
*Frequency: 17 · Severity signals: medium · Representativeness: high*

**Supporting comments:**
- PR #43: "_⚠️ Potential issue_ | _🟠 Major_ <details> <summary>🧩 Analysis chain</summary> 🏁 Script executed: ```shell #!/bin/bash set -euo pipefail rg -n 'bun-version:' .github/workflows rg -n '"packageManager"' package.json ``` Repository: lambda-curry/podcast-platform Length of output: 320 --- </details> **Pin Bun to a fixed version instead of `latest` to match `package.json`.** Line 27 introduces non-reproducible deploy behavior and can silently pick up regressions. The repository specifies `bun@1.3.3` in `package.json`; pin workflows to this version for consistency. Note: This issue also affects `.github/workflows/web-preview-deploy.yml:27` and `.github/workflows/pr-checks.yml:25`. <details> <summary>Suggested patch</summary> ```diff - bun-version: latest + bun-version: 1.3.3 ``` </details> <!-- suggestion_start --> <details> <summary>📝 Committable suggestion</summary> > ‼️ **IMPORTANT** > Carefully review the code before committing. Ensure that it accurately replaces the highlighted code, contains no missing lines, and has no issues with indentation. Thoroughly test & benchmark the code to ensure it meets the requirements. ```suggestion bun-version: 1.3.3 ``` </details> <!-- suggestion_end --> <details> <summary>🤖 Prompt for AI Agents</summary> ``` Verify each finding against the current code and only fix it if needed. In @.github/workflows/production-deploy.yml at line 27, The workflow uses bun-version: latest which causes non-reproducible deploys; change the bun-version key value from "latest" to the pinned version "1.3.3" (match package.json's bun@1.3.3) in the production deploy workflow and apply the same replacement for the corresponding bun-version entries in the other CI workflows (the web-preview and pr-checks workflows) so all workflows consistently use bun 1.3.3. ``` </details> <!-- fingerprinting:phantom:poseidon:hawk --> <!-- This is an auto-generated comment by CodeRabbit -->"
- PR #27: "_🛠️ Refactor suggestion_ | _🟠 Major_ **Test wrapper duplicates component markup, making tests fragile.** `EpisodeListItemStatic` copies the entire JSX structure from `EpisodeListItem`. If the component's markup changes (classes, structure, elements), these tests will continue passing against stale markup rather than failing to catch regressions. Consider using `@testing-library/react` with a router wrapper instead, or mock the `Link` component at the module level to preserve the real component behavior: <details> <summary>♻️ Alternative approach using vi.mock</summary> ```typescript import { renderToString } from "react-dom/server"; import { describe, expect, it, vi } from "vitest"; // Mock Link to render as plain anchor vi.mock("@tanstack/react-router", () => ({ Link: ({ to, params, children, ...props }: any) => { const href = to.replace("$slug", params?.slug).replace("$episodeId", params?.episodeId); return <a href={href} {...props}>{children}</a>; }, })); import { EpisodeListItem } from "../components/episode-list-item"; // Now use EpisodeListItem directly in tests ``` This ensures tests exercise the actual component code. </details> <details> <summary>🤖 Prompt for AI Agents</summary> ``` Verify each finding against the current code and only fix it if needed. In `@apps/web/src/tests/episode-list-item.test.tsx` around lines 17 - 54, The test helper EpisodeListItemStatic duplicates EpisodeListItem's JSX making tests brittle; instead remove EpisodeListItemStatic and update tests to render the real EpisodeListItem (from ../components/episode-list-item) while mocking Link at module-level or using a router test wrapper. Specifically, replace usages of EpisodeListItemStatic with direct renders of EpisodeListItem and add a module mock (e.g., vi.mock of "@tanstack/react-router" that returns a Link -> <a> implementation) or wrap render calls with a memory/router provider so the real EpisodeListItem markup is exercised and tests fail on regressions. ``` </details> <!-- fingerprinting:phantom:medusa:ocelot --> <!-- This is an auto-generated comment by CodeRabbit -->"
- PR #26: "_⚠️ Potential issue_ | _🟡 Minor_ **Tighten the published-date expectation to one concrete value.** This assertion is overly broad and can pass incorrect day output (e.g., `Jan 10`, `Jan 19`). Assert one literal rendered date instead. <details> <summary>Suggested assertion diff</summary> ```diff - expect(html).toContain("Jan 1"); - expect(html).toContain("2026"); + expect(html).toContain("Jan 15, 2026"); ``` </details> As per coding guidelines, `**/*.test.{ts,tsx,js,jsx}`: Tests: Assert concrete values using literal expected values, not re-derived expressions. <!-- suggestion_start --> <details> <summary>📝 Committable suggestion</summary> > ‼️ **IMPORTANT** > Carefully review the code before committing. Ensure that it accurately replaces the highlighted code, contains no missing lines, and has no issues with indentation. Thoroughly test & benchmark the code to ensure it meets the requirements. ```suggestion expect(html).toContain("Jan 15, 2026"); ``` </details> <!-- suggestion_end --> <details> <summary>🤖 Prompt for AI Agents</summary> ``` Verify each finding against the current code and only fix it if needed. In `@apps/web/src/tests/episode-player.test.tsx` around lines 38 - 39, Replace the two loose assertions that check "Jan 1" and "2026" with a single concrete expectation against the exact rendered date string (e.g., "Jan 1, 2026") using the same test variable (html) in apps/web/src/tests/episode-player.test.tsx; locate the two expects that reference html and change them to one expect asserting the full literal date to avoid false positives from dates like "Jan 10" or "Jan 19". ``` </details> <!-- fingerprinting:phantom:poseidon:hawk --> <!-- This is an auto-generated comment by CodeRabbit -->"
- PR #17: "_⚠️ Potential issue_ | _🟠 Major_ <details> <summary>🧩 Analysis chain</summary> 🌐 Web query: `In Vitest 3.1.0, does `vi.restoreAllMocks()` also revert globals created by `vi.stubGlobal()`, or is `vi.unstubAllGlobals()` required for cleanup?` 💡 Result: In Vitest 3.1.0, vi.restoreAllMocks does not revert globals created by vi.stubGlobal. vi.unstubAllGlobals is required for cleanup. Citations: - 1: https://vitest.dev/guide/mocking - 2: https://v3.vitest.dev/api/vi - 3: https://vitest.dev/guide/mocking/globals - 4: https://vitest.dev/config/unstubglobals - 5: https://vitest.dev/api/vi.html --- 🏁 Script executed: ```shell cat -n apps/web/src/tests/studio-import-function.test.ts | head -80 ``` Repository: lambda-curry/podcast-platform Length of output: 3317 --- 🏁 Script executed: ```shell rg "vi\.stubGlobal|vi\.unstubAllGlobals" apps/web/src/tests/studio-import-function.test.ts -n ``` Repository: lambda-curry/podcast-platform Length of output: 288 --- 🏁 Script executed: ```shell sed -n '115,130p' apps/web/src/tests/studio-import-function.test.ts ``` Repository: lambda-curry/podcast-platform Length of output: 710 --- 🏁 Script executed: ```shell sed -n '60,210p' apps/web/src/tests/studio-import-function.test.ts | grep -n "as {" | head -10 ``` Repository: lambda-curry/podcast-platform Length of output: 790 --- </details> **Add `vi.unstubAllGlobals()` to the `afterEach` block to prevent global state leakage between tests.** `vi.restoreAllMocks()` does not clean up globals created by `vi.stubGlobal()`. This test file stubs `fetch` in multiple tests (lines 120, 149, 169, 187) without unstubbing them, which can cause state to leak across test cases and violate test isolation. <details> <summary>Suggested fix</summary> ```diff afterEach(() => { vi.restoreAllMocks(); + vi.unstubAllGlobals(); }); ``` </details> <!-- suggestion_start --> <details> <summary>📝 Committable suggestion</summary> > ‼️ **IMPORTANT** > Carefully review the code before committing. Ensure that it accurately replaces the highlighted code, contains no missing lines, and has no issues with indentation. Thoroughly test & benchmark the code to ensure it meets the requirements. ```suggestion afterEach(() => { vi.restoreAllMocks(); vi.unstubAllGlobals(); }); ``` </details> <!-- suggestion_end --> <details> <summary>🤖 Prompt for AI Agents</summary> ``` Verify each finding against the current code and only fix it if needed. In `@apps/web/src/tests/studio-import-function.test.ts` around lines 62 - 64, The afterEach teardown only calls vi.restoreAllMocks(), but globals created by vi.stubGlobal (e.g., stubbing fetch in this test file) are not removed; update the afterEach block to also call vi.unstubAllGlobals() so any vi.stubGlobal-created globals (like fetch) are cleaned up between tests—modify the afterEach that contains vi.restoreAllMocks() to add vi.unstubAllGlobals() after (or before) the restore call to ensure no global state leaks across the tests. ``` </details> <!-- fingerprinting:phantom:poseidon:hawk --> <!-- This is an auto-generated comment by CodeRabbit -->"
- [+13 more]

### Candidate Rule — Testing

**Why (failure mode):** _⚠️ Potential issue_ | _🔴 Critical_ <details> <summary>🧩 Analysis chain</summary> 🌐 Web query: `For TanStack Router v...

**The Rule:** Add or update tests when behavior changes. Prefer tests that assert observable behavior and cover likely regressions.

**✅ DO:** ```shell
#!/bin/bash
# Read the route file
cat -n apps/web/src/routes/shows.\$slug.\$episodeId.tsx | head -80
```

**❌ DON'T:** ```shell
#!/bin/bash
# Get file size first
wc -l apps/web/src/routes/shows.\$slug.\$episodeId.ts
```

**Scope:** globs: ["**/*.test.ts", "**/*.spec.ts", "**/*.test.tsx"]

*Candidate rule — review before adopting. Frequency=17 in 47 PRs (36.2%). Evidence: 4 PR comments shown above.*

---

## Theme: Style (7 comments, score 7.3)

### Candidate: Follow established formatting and styling conventions (lint/formatter/CSS patterns) to reduce review churn
*Frequency: 7 · Severity signals: high · Representativeness: medium*

**Supporting comments:**
- PR #5: "_⚠️ Potential issue_ | _🟡 Minor_ **Formatter failure: file is missing trailing newline.** Line 13 triggers the reported style failure; please reformat this file to match repository formatting rules. <details> <summary>🧰 Tools</summary> <details> <summary>🪛 GitHub Actions: PR Checks</summary> [error] 13-13: Formatter would have printed the following content: missing trailing newline; code style issues detected during lint. </details> </details> <details> <summary>🤖 Prompt for AI Agents</summary> ``` Verify each finding against the current code and only fix it if needed. In `@packages/db/migrations/meta/_journal.json` at line 13, The file _journal.json is missing a trailing newline which triggers the formatter failure; open the _journal.json file and add a single newline character at the end of the file (ensure the file ends with '\n'), then run the repository formatter/linters (or save via your editor configured with repo settings) to confirm the style check passes before committing. ``` </details> <!-- fingerprinting:phantom:poseidon:hawk --> <!-- This is an auto-generated comment by CodeRabbit --> ✅ Addressed in commit 1ac16c7"
- PR #5: "_⚠️ Potential issue_ | _🟡 Minor_ **Snapshot formatting is currently blocking checks.** CI reports this array block as formatter-inconsistent. Please regenerate or format the migration metadata so `columnsFrom` / `columnsTo` match the expected style. <details> <summary>🧰 Tools</summary> <details> <summary>🪛 GitHub Actions: PR Checks</summary> [error] 115-119: Formatter would have printed the following content: expected columnsFrom/columnsTo as arrays; code style issues detected during lint. </details> </details> <details> <summary>🤖 Prompt for AI Agents</summary> ``` Verify each finding against the current code and only fix it if needed. In `@packages/db/migrations/meta/0000_snapshot.json` around lines 115 - 121, The migration snapshot's JSON arrays for the keys "columnsFrom" and "columnsTo" are formatted inconsistently with project style; fix by regenerating or reformatting the migration metadata so those arrays match the repository's snapshot style (e.g., consistent single-line or multi-line item formatting and trailing commas as used elsewhere). Locate the migration snapshot named 0000_snapshot.json and either re-run the migration snapshot generator or run the project's formatter (e.g., prettier/format script) to normalize the arrays for "columnsFrom" and "columnsTo" so CI formatting checks pass. ``` </details> <!-- fingerprinting:phantom:poseidon:hawk --> <!-- This is an auto-generated comment by CodeRabbit --> ✅ Addressed in commit 1ac16c7"
- PR #5: "_⚠️ Potential issue_ | _🟡 Minor_ **CI lint failures in imports/formatting need cleanup.** The pipeline reports `organizeImports` and formatter changes on this file; please apply the repository formatter/import organizer. <details> <summary>🧰 Tools</summary> <details> <summary>🪛 GitHub Actions: PR Checks</summary> [error] 1-1: Import statements could be sorted: organizeImports --- [error] 1-1: Formatter would have printed formatting changes for this file. </details> </details> <details> <summary>🤖 Prompt for AI Agents</summary> ``` Verify each finding against the current code and only fix it if needed. In `@packages/db/src/seed.ts` around lines 1 - 5, The imports in seed.ts are mis-ordered/unsorted and the formatter/organizeImports wants cleanup: open the top import block containing DrizzleD1Database, foundryShowFeedFixture, signalShowFeedFixture, RssFeedContract, episodes, shows, NewEpisode and NewShow, remove any unused imports, convert value vs type-only imports appropriately (keep "import type" for types), group and sort imports per repository rules, fix quote/semicolon/formatting consistency, then run the repository formatter/import-organizer (e.g., eslint --fix or repo-preferred prettier/TS organizeImports) to persist the changes. ``` </details> <!-- fingerprinting:phantom:poseidon:hawk --> <!-- This is an auto-generated comment by CodeRabbit --> ✅ Addressed in commit 7b10f60"
- PR #5: "_⚠️ Potential issue_ | _🟡 Minor_ **Import order needs formatter/organizeImports pass.** Pipeline reports import sorting issues at the top of the file; please apply the configured organizer. <details> <summary>🧰 Tools</summary> <details> <summary>🪛 GitHub Actions: PR Checks</summary> [error] 1-1: Import statements could be sorted: organizeImports </details> </details> <details> <summary>🤖 Prompt for AI Agents</summary> ``` Verify each finding against the current code and only fix it if needed. In `@packages/db/src/test/queries.test.ts` around lines 1 - 3, Reorder the top-of-file imports to satisfy the project's import-organizer: put external packages (vitest) imports first, then a blank line, then local module imports ("../queries"), then type-only imports last (DrizzleD1Database), and/or run the project's automated organizer/formatter (e.g., the configured "organizeImports" or eslint --fix / prettier with import sorting) to apply the configured ordering for the imports around describe/it/expect/vi/beforeEach, getShowBySlug/getEpisodesByShowId/getEpisodeById, and DrizzleD1Database. ``` </details> <!-- fingerprinting:phantom:poseidon:hawk --> <!-- This is an auto-generated comment by CodeRabbit --> ✅ Addressed in commits 1ac16c7 to fdcd247"
- [+3 more]

### Candidate Rule — Style

**Why (failure mode):** _⚠️ Potential issue_ | _🟠 Major_ <details> <summary>🧩 Analysis chain</summary> 🏁 Script executed: ```shell # Find pac...

**The Rule:** Follow established formatting and styling conventions (lint/formatter/CSS patterns) to reduce review churn.

**✅ DO:** ```tsx
<main className="min-h-screen flex items-center justify-center bg-(--color-background)">
        <h1 className="text-4xl font-bold text-(--color-text)">
        <p className="text-lg text-(--color-text-muted)">
          className="inline-block mt-4 px-6 py-2 rounded-md bg-(--color-brand) text-white font-medium hover:bg-(--color-brand-hover) transition-colors"
```

**❌ DON'T:** ```tsx
<main className="min-h-screen flex items-center justify-center bg-[var(--color-background)]">
        <h1 className="text-4xl font-bold text-[var(--color-text)]">
        <p className="text-lg text-[var(--color-text-muted)]">
          className="inline-block mt-4 px-6 py-2 rounded-md bg-[var(--color-brand)] text-white font-medium hover:bg-[var(--color-brand-hover)] transition-colors"
```

**Scope:** globs: ["**/*.css", "**/*.scss", "**/*.tsx"]

*Candidate rule — review before adopting. Frequency=7 in 47 PRs (14.9%). Evidence: 4 PR comments shown above.*

---

## Theme: Types (8 comments, score 7.0)

### Candidate: Prefer precise types over `any` and unsafe assertions
*Frequency: 8 · Severity signals: medium · Representativeness: high*

**Supporting comments:**
- PR #46: "_⚠️ Potential issue_ | _🟡 Minor_ <details> <summary>🧩 Analysis chain</summary> 🏁 Script executed: ```shell #!/bin/bash # Verify unsafe casts introduced in this test file rg -nP '\bas\s+(typeof fetch|ReturnType<\s*typeof vi\.fn\s*>)' apps/web/src/tests/sign-in-route.test.ts ``` Repository: lambda-curry/podcast-platform Length of output: 266 --- 🏁 Script executed: ```shell #!/bin/bash # Read the test file to see full context around the unsafe casts sed -n '50,100p' apps/web/src/tests/sign-in-route.test.ts | cat -n ``` Repository: lambda-curry/podcast-platform Length of output: 1865 --- 🌐 Web query: `vitest vi.stubGlobal vi.mocked fetch mock best practices TypeScript` 💡 Result: Best practices for mocking the global fetch in Vitest with TypeScript involve using vi.stubGlobal or direct assignment to global.fetch with vi.fn, combined with proper TypeScript typing. Official Vitest documentation recommends Mock Service Worker (MSW) for comprehensive request mocking as the primary approach, especially for complex scenarios. For simple global fetch stubs, use vi.stubGlobal('fetch', ...) or global.fetch = vi.fn(...). Key methods: - vi.stubGlobal('fetch', vi.fn( => Promise.resolve({ json: => Promise.resolve(mockData) }))) - Adds to globalThis, does not auto-reset unless unstubGlobals: true in config. - global.fetch = vi.fn(...) - Common for Node environment where fetch is not native. Always clean up mocks with vi.clearAllMocks or mockReset in afterEach/beforeEach hooks to prevent test leakage. For TypeScript: - Extend global types: declare global { interface Window { fetch: typeof globalThis.fetch; } } or use vi.mocked(global.fetch) for type safety. - vi.mocked(fetch) provides full mock API typing (e.g., .mockResolvedValueOnce). Example: import { vi, describe, it, expect, beforeEach, afterEach } from 'vitest'; const mockFetch = vi.fn; vi.stubGlobal('fetch', mockFetch); describe('fetch tests', => { beforeEach( => { vi.clearAllMocks; mockFetch.mockResolvedValue({ ok: true, json: => Promise.resolve({ data: 'test' }), } as Response); }); it('mocks fetch', async => { const res = await fetch('/api'); expect(mockFetch).toHaveBeenCalledWith('/api'); const data = await res.json; expect(data).toEqual({ data: 'test' }); }); }); For modules importing fetch (e.g., node-fetch), use vi.mock('node-fetch'). vi.mocked is used for type-safe access to mocks after vi.mock/vi.spyOn, e.g., vi.mocked(fetch).mockReturnValue(...). Prefer MSW for production-grade request mocking: setupServer with http.get/post handlers in beforeAll/afterEach. Avoid over-mocking; reset globals manually or via config (unstubGlobals: true). Use vi.spyOn(global, 'fetch') as alternative for spying without full replacement. Citations: - 1: https://v4.vitest.dev/guide/mocking/globals - 2: https://vitest.dev/guide/mocking - 3: https://stevekinney.com/courses/testing/mocking-fetch-and-network-requests - 4: https://runthatline.com/how-to-mock-fetch-api-with-vitest/ - 5: https://vitest.dev/guide/mocking/globals - 6: https://v4.vitest.dev/guide/mocking/requests - 7: https://tannerhodges.com/blog/mocking-fetch-with-vitest/ --- </details> **Replace unsafe fetch mock casts with typed stubbing.** The fetch mocking uses unchecked casts (`as typeof fetch`, `as ReturnType<typeof vi.fn>`), which violates the TypeScript guideline: "Avoid unsafe casts — do not use `as SomeType` without explicit narrowing". Use `vi.stubGlobal()` and `vi.mocked()` instead for type-safe mocking. <details> <summary>Suggested refactor</summary> ```diff describe("sign-in route session probe", () => { const originalFetch = globalThis.fetch; beforeEach(() => { - globalThis.fetch = vi.fn() as typeof fetch; + vi.stubGlobal("fetch", vi.fn()); }); afterEach(() => { + vi.unstubAllGlobals(); vi.restoreAllMocks(); - globalThis.fetch = originalFetch; }); it("reads the live session endpoint instead of trusting stale local state", async () => { - const mockFetch = globalThis.fetch as ReturnType<typeof vi.fn>; + const mockFetch = vi.mocked(globalThis.fetch); mockFetch.mockResolvedValueOnce( new Response(JSON.stringify({ session: { email: "creator@example.com" } }), { status: 200, headers: { "Content-Type": "application/json" }, }), ); ``` </details> Affects lines 56, 65, 88. <details> <summary>🤖 Prompt for AI Agents</summary> ``` Verify each finding against the current code and only fix it if needed. In `@apps/web/src/tests/sign-in-route.test.ts` around lines 56 - 57, Replace the unsafe cast-based global fetch mock in the test (the globalThis.fetch = vi.fn() as typeof fetch usage) with type-safe stubbing by calling vi.stubGlobal('fetch', vi.fn()) and then use vi.mocked(fetch) where you need typed access to the mock; update any other places that currently use casts (e.g., the ReturnType<typeof vi.fn> casts around mocked fetch in this file) to reference the vi.mocked(fetch) typed helper so the mock has correct types for assertions and returnValue setups in sign-in-route.test.ts. ``` </details> <!-- fingerprinting:phantom:poseidon:hawk --> <!-- This is an auto-generated comment by CodeRabbit --> ✅ Addressed in commits 79ca4d5 to 09b31dc"
- PR #11: "_⚠️ Potential issue_ | _🔴 Critical_ <details> <summary>🧩 Analysis chain</summary> 🏁 Script executed: ```shell #!/bin/bash # Verify the shows table schema to confirm slug is unique ast-grep --pattern 'export const shows = sqliteTable($$$)' ``` Repository: lambda-curry/podcast-platform Length of output: 1034 --- 🏁 Script executed: ```shell # Read the writes.ts file to see the full context of the upserts and the operations mentioned cat -n packages/db/src/writes.ts | head -110 ``` Repository: lambda-curry/podcast-platform Length of output: 4042 --- 🏁 Script executed: ```shell # Check the episodes schema for the episodeId structure rg -A 20 'export const episodes' packages/db/src/schema.ts ``` Repository: lambda-curry/podcast-platform Length of output: 912 --- 🏁 Script executed: ```shell # Look for any transaction or batch usage in the publishStudioFeed function rg -B 5 -A 15 'publishStudioFeed' packages/db/src/writes.ts | head -60 ``` Repository: lambda-curry/podcast-platform Length of output: 670 --- </details> **Fix upsert logic for shows and episodes: use natural keys, preserve immutable fields, and add transaction protection.** Three critical issues: 1. **Conflict target should use `slug` (natural key) instead of `id`.** The `shows.id` is client-generated and can vary between publishes. If the same show is published with a different `showId` but the same `slug`, a new row is inserted rather than updating the existing one, creating duplicates. The schema declares `slug` as `unique()` and is the natural deduplication key. 2. **Never update immutable fields `guid` and `audioUrl` on conflict.** Per coding guidelines, "Episode GUIDs are immutable once published" and "Audio file URLs for published episodes are immutable." Lines 56 and 60 update these fields, violating RSS Feed Immutability requirements. Remove them from the `set` block. 3. **Wrap show and episode upserts in a transaction.** Lines 97–101 execute sequentially without explicit transaction protection. If the show succeeds but an episode fails, the database is left in an inconsistent state. Use D1's batch API or explicit transaction wrapper. <details> <summary>🐛 Proposed fixes</summary> ```diff export async function upsertShow(db: DrizzleD1Database, show: NewShow): Promise<Show> { const rows = await db .insert(shows) .values(show) .onConflictDoUpdate({ - target: shows.id, + target: shows.slug, set: { + id: show.id, slug: show.slug, title: show.title, ``` ```diff export async function upsertEpisode(db: DrizzleD1Database, episode: NewEpisode): Promise<Episode> { const rows = await db .insert(episodes) .values(episode) .onConflictDoUpdate({ target: episodes.id, set: { title: episode.title, description: episode.description, - audioUrl: episode.audioUrl, audioType: episode.audioType, audioLengthBytes: episode.audioLengthBytes, durationSec: episode.durationSec, - guid: episode.guid, pubDate: episode.pubDate, ``` ```diff export async function publishStudioFeed( binding: D1DatabaseBinding, input: StudioPublishFeedInput, ): Promise<StudioPublishFeedResult> { const db = drizzle(binding); - - await upsertShow(db, input.show); - - for (const episode of input.episodes) { - await upsertEpisode(db, episode); - } + await db.batch([ + db.insert(shows).values(input.show).onConflictDoUpdate({ target: shows.slug, set: { /* ... */ } }), + ...input.episodes.map(ep => + db.insert(episodes).values(ep).onConflictDoUpdate({ target: episodes.id, set: { /* ... */ } }) + ), + ]); ``` </details> <details> <summary>🤖 Prompt for AI Agents</summary> ``` Verify each finding against the current code and only fix it if needed. In `@packages/db/src/writes.ts` around lines 17 - 18, The upsert logic currently conflicts on shows.id and updates immutable episode fields without transaction protection; change the conflict target in the .onConflictDoUpdate call from shows.id to shows.slug (the natural unique key), remove guid and audioUrl from the fields updated in the episode .set block, and wrap the show and episode upserts together in a single transaction (use D1's batch/transaction API or an explicit transaction wrapper) so both operations commit or roll back atomically; locate the .onConflictDoUpdate for shows, the episode .set that mentions guid and audioUrl, and the sequential upsert calls in this file to apply these fixes. ``` </details> <!-- fingerprinting:phantom:poseidon:ocelot --> <!-- This is an auto-generated comment by CodeRabbit --> ✅ Addressed in commits 30a5863 to 86179aa"
- PR #11: "_⚠️ Potential issue_ | _🔴 Critical_ **Updating `guid` and `audioUrl` on conflict violates RSS feed immutability requirements.** Per coding guidelines, "Episode GUIDs are immutable once published" and "Audio file URLs for published episodes are immutable." The `seed.ts` file (context snippet 3, lines 76-77) explicitly excludes these fields from conflict updates for this reason. Overwriting these fields can break podcast client subscriptions that rely on GUID stability for deduplication and can invalidate cached audio downloads. As per coding guidelines: "Episode GUIDs are immutable once published — never regenerate or reformat them" and "Audio file URLs for published episodes are immutable." <details> <summary>🐛 Proposed fix</summary> ```diff .onConflictDoUpdate({ target: episodes.id, set: { title: episode.title, description: episode.description, - audioUrl: episode.audioUrl, audioType: episode.audioType, audioLengthBytes: episode.audioLengthBytes, durationSec: episode.durationSec, - guid: episode.guid, pubDate: episode.pubDate, season: episode.season, episode: episode.episode, explicit: episode.explicit, }, }) ``` </details> <!-- suggestion_start --> <details> <summary>📝 Committable suggestion</summary> > ‼️ **IMPORTANT** > Carefully review the code before committing. Ensure that it accurately replaces the highlighted code, contains no missing lines, and has no issues with indentation. Thoroughly test & benchmark the code to ensure it meets the requirements. ```suggestion .onConflictDoUpdate({ target: episodes.id, set: { title: episode.title, description: episode.description, audioType: episode.audioType, audioLengthBytes: episode.audioLengthBytes, durationSec: episode.durationSec, pubDate: episode.pubDate, season: episode.season, episode: episode.episode, explicit: episode.explicit, }, }) ``` </details> <!-- suggestion_end --> <details> <summary>🤖 Prompt for AI Agents</summary> ``` Verify each finding against the current code and only fix it if needed. In `@packages/db/src/writes.ts` around lines 56 - 60, The upsert currently allows updating immutable fields guid and audioUrl on conflict; modify the conflict-update logic in packages/db/src/writes.ts (the upsert/INSERT ... ON CONFLICT handler used when writing episodes) to exclude guid and audioUrl from the update assignment so they are only set on initial insert (keep audioType, audioLengthBytes, durationSec, etc. updatable). Mirror the behavior from seed.ts that omits guid and audioUrl on conflict, ensuring GUIDs and published audio URLs remain immutable after first insert. ``` </details> <!-- fingerprinting:phantom:poseidon:ocelot --> <!-- This is an auto-generated comment by CodeRabbit --> ✅ Addressed in commits 30a5863 to 86179aa"
- PR #9: "_⚠️ Potential issue_ | _🟠 Major_ <details> <summary>🧩 Analysis chain</summary> 🏁 Script executed: ```shell cat -n apps/web/src/routes/studio.tsx | head -140 ``` Repository: lambda-curry/podcast-platform Length of output: 5333 --- 🏁 Script executed: ```shell # Find the normalizeStudioSearch function rg "normalizeStudioSearch" --type ts --type tsx -A 15 ``` Repository: lambda-curry/podcast-platform Length of output: 100 --- 🏁 Script executed: ```shell # Check if there's a Zod schema for StudioSearch rg "StudioSearch" --type ts --type tsx -B 3 -A 3 ``` Repository: lambda-curry/podcast-platform Length of output: 100 --- 🏁 Script executed: ```shell rg "validateSearch" -A 2 --type ts ``` Repository: lambda-curry/podcast-platform Length of output: 539 --- 🏁 Script executed: ```shell # Check if z (Zod) is imported and used elsewhere in the project rg "from \"zod\"" --type ts --type tsx -B 1 -A 1 ``` Repository: lambda-curry/podcast-platform Length of output: 100 --- 🏁 Script executed: ```shell # Look for existing schema patterns rg "z\.object" --type ts --type tsx -B 2 -A 5 | head -100 ``` Repository: lambda-curry/podcast-platform Length of output: 100 --- 🏁 Script executed: ```shell # Search for Zod imports using rg with pattern matching rg "zod" -i ``` Repository: lambda-curry/podcast-platform Length of output: 3484 --- 🏁 Script executed: ```shell # Check the sign-in route to see if it has similar pattern cat -n apps/web/src/routes/sign-in.tsx | head -80 ``` Repository: lambda-curry/podcast-platform Length of output: 3319 --- 🏁 Script executed: ```shell # Look for how other routes handle validation fd "routes" -type d ``` Repository: lambda-curry/podcast-platform Length of output: 243 --- 🏁 Script executed: ```shell # Check how other routes properly use Zod for validation cat -n apps/web/src/routes/shows.\$slug.tsx | head -50 ``` Repository: lambda-curry/podcast-platform Length of output: 2151 --- 🏁 Script executed: ```shell # Check the other route that uses Zod cat -n apps/web/src/routes/shows.\$slug.\$episodeId.tsx | head -60 ``` Repository: lambda-curry/podcast-platform Length of output: 2185 --- </details> **Add Zod validation for route search parameters before parsing.** Line 113 uses an unsafe cast `search as StudioSearch` without validating the external query parameters. Route search params must be validated with a Zod schema before being passed to `normalizeStudioSearch()`. Create a schema like: ```typescript const studioSearchSchema = z.object({ show: z.string().optional(), }); ``` Then update the `validateSearch` function to: ```typescript validateSearch: (search) => normalizeStudioSearch(studioSearchSchema.parse(search)), ``` This follows the pattern used in `shows.$slug.tsx` and aligns with the guidelines to validate all external input and avoid unsafe casts. <details> <summary>🤖 Prompt for AI Agents</summary> ``` Verify each finding against the current code and only fix it if needed. In `@apps/web/src/routes/studio.tsx` around lines 112 - 118, The route's validateSearch currently unsafely casts search to StudioSearch; define a Zod schema (e.g., const studioSearchSchema = z.object({ show: z.string().optional() })) and update validateSearch to parse the incoming search with studioSearchSchema.parse(search) before calling normalizeStudioSearch, i.e., replace the cast in the Route validateSearch with parsing via studioSearchSchema so normalizeStudioSearch only receives validated data. ``` </details> <!-- fingerprinting:phantom:medusa:grasshopper --> <!-- This is an auto-generated comment by CodeRabbit --> ✅ Addressed in commits 08bd449 to 0f32045"
- [+4 more]

### Candidate Rule — Types

**Why (failure mode):** _⚠️ Potential issue_ | _🟠 Major_ <details> <summary>🧩 Analysis chain</summary> 🏁 Script executed: ```shell #!/bin/bas...

**The Rule:** Prefer precise types over `any` and unsafe assertions. Model contracts with interfaces/types so invalid states are caught at compile time.

**✅ DO:** ```ts
Verify each finding against the current code and only fix it if needed.

In `@packages/db/src/test/queries.test.ts` at line 51, The test creates a mock DB
using an unsafe double-cast ("as unknown as DrizzleD1Database") which bypasses
type checking; change the mock's type to Partial<DrizzleD1Database> (or return
type of buildMockDb) and declare const db = { select: selectFn } as
Partial<DrizzleD1Database> so the minimal mock matches the expected shape
without unsafe casts—update uses of db in queries.test.ts accordingly (or adjust
buildMockDb to return Partial<DrizzleD1Database>).
```

**❌ DON'T:** ```shell
#!/bin/bash
# See if there's a pattern in how the db parameter is used in actual code
rg 'function.*\(db: DrizzleD1Database' packages/db/src --type ts -A 2
```

**Scope:** alwaysApply: true

*Candidate rule — review before adopting. Frequency=8 in 47 PRs (17.0%). Evidence: 4 PR comments shown above.*

---

## Theme: Imports (11 comments, score 7.0)

### Candidate: Use consistent import/export structure and avoid ambiguous barrel patterns when they reduce clarity or create coupling
*Frequency: 11 · Severity signals: low · Representativeness: high*

**Supporting comments:**
- PR #43: "_⚠️ Potential issue_ | _🔴 Critical_ **Same self-hosted PR execution risk applies to checks workflow.** Line 16 on a `pull_request` workflow can run untrusted PR code on your self-hosted runner. Add a same-repo PR guard or route PR checks to GitHub-hosted runners. <details> <summary>Suggested guard</summary> ```diff jobs: checks: name: Format · Lint · Typecheck · Test · Build + if: github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository runs-on: [self-hosted, macos, arm64] ``` </details> <!-- suggestion_start --> <details> <summary>📝 Committable suggestion</summary> > ‼️ **IMPORTANT** > Carefully review the code before committing. Ensure that it accurately replaces the highlighted code, contains no missing lines, and has no issues with indentation. Thoroughly test & benchmark the code to ensure it meets the requirements. ```suggestion jobs: checks: name: Format · Lint · Typecheck · Test · Build if: github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository runs-on: [self-hosted, macos, arm64] ``` </details> <!-- suggestion_end --> <details> <summary>🤖 Prompt for AI Agents</summary> ``` Verify each finding against the current code and only fix it if needed. In @.github/workflows/pr-checks.yml at line 16, The pull_request workflow currently uses a self-hosted runner via the runs-on: [self-hosted, macos, arm64] line which can execute untrusted PR code; either add a same-repo PR guard or route PR events to GitHub-hosted runners. Fix by modifying the workflow to check the PR source before using the self-hosted runner (for example add an if: condition that tests github.event.pull_request.head.repo.full_name == github.repository to the job that uses runs-on) or change the job to use a GitHub-hosted label (e.g., macos-latest) for pull_request events while keeping self-hosted only for push or trusted branches; locate the runs-on declaration in the workflow and implement the conditional guard or runner selection accordingly. ``` </details> <!-- fingerprinting:phantom:poseidon:hawk --> <!-- This is an auto-generated comment by CodeRabbit -->"
- PR #42: "_⚠️ Potential issue_ | _🔴 Critical_ **Block fork PRs from running on self-hosted hardware.** Because this workflow runs on `pull_request`, untrusted code from forks can execute on your private runner. Add a guard so only same-repo PRs use this job (or route fork PRs to GitHub-hosted runners). <details> <summary>🔒 Suggested guard</summary> ```diff jobs: checks: name: Format · Lint · Typecheck · Test · Build + if: github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository runs-on: [self-hosted, macos, arm64] ``` </details> <!-- suggestion_start --> <details> <summary>📝 Committable suggestion</summary> > ‼️ **IMPORTANT** > Carefully review the code before committing. Ensure that it accurately replaces the highlighted code, contains no missing lines, and has no issues with indentation. Thoroughly test & benchmark the code to ensure it meets the requirements. ```suggestion jobs: checks: name: Format · Lint · Typecheck · Test · Build if: github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository runs-on: [self-hosted, macos, arm64] ``` </details> <!-- suggestion_end --> <details> <summary>🤖 Prompt for AI Agents</summary> ``` Verify each finding against the current code and only fix it if needed. In @.github/workflows/pr-checks.yml at line 12, The job that uses self-hosted runners (the one declaring runs-on: [self-hosted, macos, arm64]) must be guarded so fork PRs cannot run on private hardware; add an if condition to that job which permits execution only when the PR comes from the same repository, e.g. check github.event.pull_request.head.repo.full_name == github.repository (or equivalently compare github.event.pull_request.head.repo.id == github.repository_id) so fork-origin PRs are skipped or routed to GitHub-hosted runners. ``` </details> <!-- fingerprinting:phantom:poseidon:hawk --> <!-- This is an auto-generated comment by CodeRabbit -->"
- PR #27: "_⚠️ Potential issue_ | _🟠 Major_ **Duplicated formatting functions bypass actual component logic.** These local copies of `formatPublishedDate`, `formatDuration`, and `formatMegabytes` mean tests verify the test's own implementations rather than the component's. If formatting logic changes in the component, tests will continue passing with stale assertions. If extracting helpers to a shared module (as suggested for the component), import them here too. Otherwise, export the helpers from the component and import them in tests. <details> <summary>🤖 Prompt for AI Agents</summary> ``` Verify each finding against the current code and only fix it if needed. In `@apps/web/src/tests/episode-list-item.test.tsx` around lines 56 - 83, The test currently defines local duplicates of formatPublishedDate, formatDuration, and formatMegabytes which bypass the component's real logic; remove these local functions from episode-list-item.test.tsx and instead import the canonical implementations (either from the component export or the newly extracted shared helpers module) so the tests exercise the same formatting code used by the component (ensure the component/file exports formatPublishedDate, formatDuration, formatMegabytes or create a shared utils file and update both the component and test imports accordingly). ``` </details> <!-- fingerprinting:phantom:medusa:ocelot --> <!-- This is an auto-generated comment by CodeRabbit -->"
- PR #25: "_⚠️ Potential issue_ | _🟠 Major_ **Type mismatch: assigning potentially `undefined` value to required `imageUrl: string` field.** `HomeShowCard.imageUrl` is typed as `string` (required), but `feed.show.imageUrl` can be `string | undefined` per the RSS parser. This assignment may pass TypeScript if `RssFeedContract` isn't strictly typed, leading to `undefined` values propagating to `ShowCover`. <details> <summary>🛠️ Proposed fix: provide fallback at assignment</summary> ```diff - imageUrl: feed.show.imageUrl, + imageUrl: feed.show.imageUrl ?? "", ``` </details> Alternatively, if `ShowCoverProps` is updated to accept `string | undefined` (as suggested in the episode page review), update `HomeShowCard` type accordingly: ```diff - imageUrl: string; + imageUrl: string | undefined; ``` <!-- suggestion_start --> <details> <summary>📝 Committable suggestion</summary> > ‼️ **IMPORTANT** > Carefully review the code before committing. Ensure that it accurately replaces the highlighted code, contains no missing lines, and has no issues with indentation. Thoroughly test & benchmark the code to ensure it meets the requirements. ```suggestion imageUrl: feed.show.imageUrl ?? "", ``` </details> <!-- suggestion_end --> <details> <summary>🤖 Prompt for AI Agents</summary> ``` Verify each finding against the current code and only fix it if needed. In `@apps/web/src/routes/index.tsx` at line 79, The HomeShowCard is assigning feed.show.imageUrl (which may be undefined) to the required imageUrl string field; update the assignment in HomeShowCard to supply a safe fallback (e.g., default cover URL or empty string) when feed.show.imageUrl is undefined, or alternatively update the HomeShowCard/ShowCoverProps types so imageUrl accepts string | undefined (ensure consistency between HomeShowCard, ShowCoverProps and ShowCover); locate symbols to change: HomeShowCard, imageUrl, feed.show.imageUrl, ShowCoverProps, and ShowCover and apply the chosen fix across those places. ``` </details> <!-- fingerprinting:phantom:poseidon:ocelot --> <!-- This is an auto-generated comment by CodeRabbit -->"
- [+7 more]

### Candidate Rule — Imports

**Why (failure mode):** _⚠️ Potential issue_ | _🟠 Major_ **Do not upsert immutable published fields (`guid`, `audioUrl`).** Line 81 and Line 84...

**The Rule:** Use consistent import/export structure and avoid ambiguous barrel patterns when they reduce clarity or create coupling.

**✅ DO:** ```tsx
{show.latestEpisodePublishedAt && show.latestEpisodeDurationSeconds !== undefined ? (
```

**❌ DON'T:** ```tsx
{show.latestEpisodePublishedAt && show.latestEpisodeDurationSeconds ? (
```

**Scope:** globs: ["**/*.ts", "**/*.tsx", "**/*.js"]

*Candidate rule — review before adopting. Frequency=11 in 47 PRs (23.4%). Evidence: 4 PR comments shown above.*

---

## Theme: Documentation (7 comments, score 4.3)

### Candidate: Document intent when behavior is non-obvious
*Frequency: 7 · Severity signals: low · Representativeness: high*

**Supporting comments:**
- PR #27: "_⚠️ Potential issue_ | _🟡 Minor_ **Test count appears stale.** The PR objectives state "284 total tests passing (221 web + 32 db + 31 rss)" but this document shows 273 total (210 web). Consider updating to reflect the current test count after the 11 new tests added in this PR. <details> <summary>🤖 Prompt for AI Agents</summary> ``` Verify each finding against the current code and only fix it if needed. In @.project/agents/scout.md at line 11, Update the stale test count in .project/agents/scout.md: change the line currently reading "**Test count:** 273 total (210 web, 32 db, 31 rss)" to the new totals from the PR (284 total (221 web, 32 db, 31 rss)) so the document matches the PR objectives and includes the 11 added web tests. ``` </details> <!-- fingerprinting:phantom:medusa:ocelot --> <!-- This is an auto-generated comment by CodeRabbit -->"
- PR #15: "_⚠️ Potential issue_ | _🟡 Minor_ **Docstring mentions "published" but query has no filter.** The docstring states "Fetch all published shows" but the query returns all rows from the `shows` table without filtering by a `published` or `status` column. If shows can exist in an unpublished state, this may expose draft shows unintentionally. If all shows in the table are implicitly published, consider updating the docstring to "Fetch all shows" for accuracy. <details> <summary>🤖 Prompt for AI Agents</summary> ``` Verify each finding against the current code and only fix it if needed. In `@packages/db/src/queries.ts` around lines 10 - 15, The JSDoc for getShows says "Fetch all published shows" but the implementation db.select().from(shows) returns every row; either update the docstring to "Fetch all shows" or actually filter for published rows. Fix getShows by adding a WHERE clause that filters by your publish indicator (e.g., shows.published === true or shows.status === 'published') using your query helpers (keep the function signature getShows(db: DrizzleD1Database): Promise<Show[]>), or if every row is truly published, change the comment to "Fetch all shows" to match the code. ``` </details> <!-- fingerprinting:phantom:poseidon:ocelot --> <!-- This is an auto-generated comment by CodeRabbit -->"
- PR #8: "_⚠️ Potential issue_ | _🟠 Major_ **Audio URL editing should be restricted for published episodes.** The audio URL field is editable regardless of episode status, but coding guidelines state: "Audio file URLs for published episodes are immutable — changing storage paths requires URL redirects, not in-place moves." Consider restricting this field for published episodes to prevent breaking subscriber playback. <details> <summary>🤖 Prompt for AI Agents</summary> ``` Verify each finding against the current code and only fix it if needed. In `@apps/web/src/routes/studio.tsx` around lines 370 - 375, The Audio URL TextField (id "episode-audio-url", value episode.audioUrl, onChange calling onFieldChange with episode.episodeId) must be made non-editable for published episodes; detect publication via episode.status (or an isPublished flag) and set the TextField to readOnly/disabled (and optionally show a tooltip or helper text explaining URLs for published episodes are immutable) so onFieldChange is not invoked for published episodes. Ensure the disable/readOnly logic is applied where the TextField is rendered to prevent changes for published episodes. ``` </details> <!-- fingerprinting:phantom:medusa:ocelot --> <!-- This is an auto-generated comment by CodeRabbit --> ✅ Addressed in commit 7f2e6bc"
- PR #8: "_⚠️ Potential issue_ | _🟡 Minor_ **Use repo-relative or URL-based contract links instead of home-directory paths.** `~/saffron/shared/docs/...` is environment-specific and can break for other contributors. Prefer repo-relative paths (or canonical URLs) so links are portable. <details> <summary>🤖 Prompt for AI Agents</summary> ``` Verify each finding against the current code and only fix it if needed. In `@README.md` around lines 25 - 28, The README uses home-directory paths like `~/saffron/shared/docs/project-delivery-contract.md`, which are environment-specific; replace those with repo-relative paths (e.g., `shared/docs/project-delivery-contract.md`) or canonical URLs for each of the four links (`project-delivery-contract.md`, `project-verification-contract.md`, `project-verification-defaults.md`, `project-receipt-contract.md`) so links work for all contributors and CI environments. ``` </details> <!-- fingerprinting:phantom:triton:hawk --> <!-- This is an auto-generated comment by CodeRabbit --> ✅ Addressed in commit 7f2e6bc"
- [+3 more]

### Candidate Rule — Documentation

**Why (failure mode):** _⚠️ Potential issue_ | _🟠 Major_ **Update stale audio-storage guidance to match the resolved decision.** The doc still...

**The Rule:** Document intent when behavior is non-obvious. Keep README/comments aligned with current code behavior.

**✅ DO:** ```md
### Audio URL Stability
- Audio files must be served from a stable, CDN-backed URL (Cloudflare R2 with public/CDN access)
## Open Questions (resolve before implementing)

- [x] **Audio storage:** Cloudflare R2 approved (resolved 2026-03-09)
```

**❌ DON'T:** ```md
### Audio URL Stability
- Audio files must be served from a stable, CDN-backed URL (R2 or Railway volume with public access)
## Open Questions (resolve before implementing)

- [ ] **Audio storage:** R2 (Cloudflare) vs. Railway volume — decision needed from Derek before media upload work begins
```

**Scope:** alwaysApply: true

*Candidate rule — review before adopting. Frequency=7 in 47 PRs (14.9%). Evidence: 4 PR comments shown above.*

---

## Skipped (low signal)
- 0 comments below length threshold
- 0 pure approvals
- 0 questions without suggestions
