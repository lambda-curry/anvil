# Rule Candidates — lambda-curry/saffron-starter
*Mined: 2026-07-05 · PRs analyzed: 100 · Comments reviewed: 93 · Candidates surfaced: 6*

---

## Theme: Testing (25 comments, score 16.9)

### Candidate: Add or update tests when behavior changes
*Frequency: 25 · Severity signals: medium · Representativeness: high*

**Supporting comments:**
- PR #225: "_📐 Maintainability & Code Quality_ | _🟡 Minor_ | _⚡ Quick win_ <details> <summary>🧩 Analysis chain</summary> 🏁 Script executed: ```shell #!/bin/bash fd -g '*CanonicalStarterTemplateShowcase*' packages/ui/src ``` Repository: lambda-curry/saffron-starter Length of output: 338 --- 🏁 Script executed: ```shell #!/bin/bash set -euo pipefail ast-grep outline packages/ui/src/CanonicalStarterTemplateShowcase.tsx --view expanded printf '\n--- TEST FILE ---\n' ast-grep outline packages/ui/src/CanonicalStarterTemplateShowcase.test.tsx --view expanded printf '\n--- COMPONENT SNIPPET ---\n' cat -n packages/ui/src/CanonicalStarterTemplateShowcase.tsx | sed -n '1,220p' printf '\n--- TEST SNIPPET ---\n' cat -n packages/ui/src/CanonicalStarterTemplateShowcase.test.tsx | sed -n '1,260p' ``` Repository: lambda-curry/saffron-starter Length of output: 13978 --- 🏁 Script executed: ```shell #!/bin/bash set -euo pipefail ast-grep outline packages/ui/src/canonicalStarterTemplateSelectionPersistence.ts --view expanded printf '\n--- PERSISTENCE SNIPPET ---\n' cat -n packages/ui/src/canonicalStarterTemplateSelectionPersistence.ts | sed -n '1,260p' printf '\n--- SEARCH SHOWCASE Fallback TESTS ---\n' rg -n "unknown|invalid|fallback|parseCanonicalStarterTemplateSelection|createCanonicalStarterTemplateSelection" packages/ui/src/CanonicalStarterTemplateShowcase.test.tsx packages/ui/src -g '*.test.tsx' -g '*.test.ts' ``` Repository: lambda-curry/saffron-starter Length of output: 9041 --- </details> **Add showcase coverage for invalid template fallback.** `CanonicalStarterTemplateShowcase.test.tsx` covers the default and controlled paths, but it still needs a component-level case for an unknown `defaultTemplate`/`template` value falling back to the default starter key. <details> <summary>🤖 Prompt for AI Agents</summary> ``` Verify each finding against current code. Fix only still-valid issues, skip the rest with a brief reason, keep changes minimal, and validate. In `@packages/ui/src/CanonicalStarterTemplateShowcase.tsx` around lines 57 - 66, Add a component-level test in CanonicalStarterTemplateShowcase.test.tsx that renders CanonicalStarterTemplateShowcase with an unknown defaultTemplate and with an unknown controlled template value, then verifies parseCanonicalStarterTemplateSelection falls back to the default starter key via activeTemplate/activeSelection. Cover both the uncontrolled and controlled paths so the showcase behavior is validated when invalid values are passed in. ``` </details> <!-- fingerprinting:phantom:poseidon:beignet --> <!-- cr-indicator-types:potential_issue --> <!-- cr-comment:v1:f5fa66891857147a8e22c1e0 --> _Source: Coding guidelines_ <!-- This is an auto-generated comment by CodeRabbit --> ✅ Addressed in commits ec97e4c to a584284"
- PR #215: "_📐 Maintainability & Code Quality_ | _🟡 Minor_ | _⚡ Quick win_ **The CI-gates docs no longer match the actual pre-push lane.** Line 27 points readers at `bun run test`, and Line 35’s expansion drops `fmt:check`, but `package.json` now gates pre-push on `test:ci` and `fmt:check`. That leaves contributors validating a different path than the one hooks/CI enforce. <details> <summary>Suggested doc fix</summary> ```diff -bun run test # standard repo test lane +bun run test:ci # serialized repo test lane used by pre-push/CI bun run build # production build (Turbo) bun run storybook:build # Storybook static build @@ -bun run check:prepush # check:lock → lint → typecheck → test:ci → build → storybook:build +bun run check:prepush # check:lock → fmt:check → lint → typecheck → test:ci → build → storybook:build ``` </details> As per coding guidelines, `.project/setup.md` should be the source of truth for environment bootstrap, dev commands, and CI gates. <!-- suggestion_start --> <details> <summary>📝 Committable suggestion</summary> > ‼️ **IMPORTANT** > Carefully review the code before committing. Ensure that it accurately replaces the highlighted code, contains no missing lines, and has no issues with indentation. Thoroughly test & benchmark the code to ensure it meets the requirements. ```suggestion bun run test:ci # serialized repo test lane used by pre-push/CI bun run build # production build (Turbo) bun run storybook:build # Storybook static build Or run the full pre-push suite at once: ``` </details> <!-- suggestion_end --> <details> <summary>🤖 Prompt for AI Agents</summary> ``` Verify each finding against current code. Fix only still-valid issues, skip the rest with a brief reason, keep changes minimal, and validate. In @.project/setup.md around lines 27 - 35, The pre-push/CI gate documentation in .project/setup.md is out of sync with the actual scripts. Update the listed validation commands and the check:prepush expansion to match package.json, using the same named lanes (especially test:ci and fmt:check) so contributors run the exact hooks/CI path; revise the surrounding pre-push section and any references to bun run test accordingly. ``` </details> <!-- fingerprinting:phantom:poseidon:grasshopper --> <!-- cr-indicator-types:potential_issue --> <!-- cr-comment:v1:60dd84d946e14c733dbab9d0 --> _Source: Coding guidelines_ <!-- This is an auto-generated comment by CodeRabbit --> ✅ Addressed in commit 03d7d6e"
- PR #215: "_🎯 Functional Correctness_ | _🟠 Major_ | _⚡ Quick win_ **Remove or restore the missing `ContentSeriesArchiveShell` dark story.** This contract now expects `media-contentseriesarchiveshell--dark-mode-stack`, but `packages/ui/src/ContentSeriesArchiveShell.stories.tsx` removed `DarkModeStack`. As written, the test no longer matches the exported story set. <details> <summary>Suggested fix</summary> ```diff - { - name: "Dark-ContentSeriesArchiveShell", - id: "media-contentseriesarchiveshell--dark-mode-stack", - }, ``` </details> If dark-mode coverage is still required, re-add the story and enroll it consistently instead of only updating the contract. As per coding guidelines, "Behavior changes require test updates or additions." <!-- suggestion_start --> <details> <summary>📝 Committable suggestion</summary> > ‼️ **IMPORTANT** > Carefully review the code before committing. Ensure that it accurately replaces the highlighted code, contains no missing lines, and has no issues with indentation. Thoroughly test & benchmark the code to ensure it meets the requirements. ```suggestion ``` </details> <!-- suggestion_end --> <details> <summary>🤖 Prompt for AI Agents</summary> ``` Verify each finding against current code. Fix only still-valid issues, skip the rest with a brief reason, keep changes minimal, and validate. In `@packages/ui/src/StarterFacingStoryVisualBaselineContract.test.ts` around lines 404 - 407, The starter-facing visual baseline contract for ContentSeriesArchiveShell is out of sync with the exported stories because it still references the missing DarkModeStack entry. Update either ContentSeriesArchiveShell.stories.tsx to restore the DarkModeStack story and keep it enrolled consistently, or remove the corresponding Dark-ContentSeriesArchiveShell expectation from StarterFacingStoryVisualBaselineContract so the contract matches the actual story set. ``` </details> <!-- fingerprinting:phantom:poseidon:grasshopper --> <!-- cr-indicator-types:potential_issue --> <!-- cr-comment:v1:e6d5192546b2afe3896c1979 --> _Source: Coding guidelines_ <!-- This is an auto-generated comment by CodeRabbit -->"
- PR #210: "`@jaruesink` Thanks for the update — the added alternate-fixture regression coverage and verification reruns sound good. <!-- <review_comment_addressed> --> <!-- This is an auto-generated reply by CodeRabbit -->"
- [+21 more]

### Candidate Rule — Testing

**Why (failure mode):** _📐 Maintainability & Code Quality_ | _🟡 Minor_ | _⚡ Quick win_ **Add responsive proof for the account-orders shell ado...

**The Rule:** Add or update tests when behavior changes. Prefer tests that assert observable behavior and cover likely regressions.

**✅ DO:** ```ts
expect(product.price).toBe("$168");
  expect(product.fulfillment).toEqual(["free-2-day"]);
```

**❌ DON'T:** ```ts
it("supports targeted product overrides without rewriting the full product object", () => {
    const product = createStorefrontCatalogProductFixture({
      title: "Pilot Weekender",
      price: "$168",
      priceValue: 168,
      fulfillment: ["free-2-day"],
    });

    expect(product.title).toBe("Pilot Weekender");
    expect(product.price).toBe("$168");
    expect(product.priceValue).toBe(168);
    expect(product.fulfillment).toEqual(["free-2-day"]);
    expect(product.eyebrow).toMatchObject({ label: "Travel bestsellers", variant: "brand" });
  });
```

**Scope:** globs: ["**/*.test.ts", "**/*.spec.ts", "**/*.test.tsx"]

*Candidate rule — review before adopting. Frequency=25 in 100 PRs (25.0%). Evidence: 4 PR comments shown above.*

---

## Theme: Naming (18 comments, score 11.2)

### Candidate: Use descriptive, convention-aligned names for variables, functions, and classes
*Frequency: 18 · Severity signals: low · Representativeness: high*

**Supporting comments:**
- PR #219: "Removed the named re-export from `CanonicalStarterTemplateSurface.stories.tsx` so the file only exports actual CSF stories. Re-ran the targeted UI tests and the full `check:prepush` gate before pushing commit `1984afa`."
- PR #210: "Addressed in `012f478`: `ActionButton` now routes its local className merge through the shared `cn` helper."
- PR #210: "_📐 Maintainability & Code Quality_ | _🟡 Minor_ | _⚡ Quick win_ **Use `cn` for the local className merge.** Line 51 manually joins class names; route this merge through the shared `cn` helper. <details> <summary>Proposed fix</summary> ```diff +import { cn } from "./cn"; import { createMarketingLandingFixture, type MarketingLandingActionFixture, type MarketingLandingFeatureCardFixture, @@ - const classes = [action.withArrow ? "gap-2" : "", className].filter(Boolean).join(" "); + const classes = cn(action.withArrow ? "gap-2" : undefined, className); ``` </details> As per coding guidelines, “route every component `className` merge through `packages/ui/src/cn.ts`.” Also applies to: 51-51 <details> <summary>🤖 Prompt for AI Agents</summary> ``` Verify each finding against current code. Fix only still-valid issues, skip the rest with a brief reason, keep changes minimal, and validate. In `@packages/ui/src/MarketingLandingComposition.tsx` around lines 4 - 15, The local className merge in MarketingLandingComposition should use the shared cn helper instead of manually joining strings. Update MarketingLandingComposition to import cn from the shared utility and route the component’s className combination through it, replacing the existing inline merge so it follows the ui package convention for className handling. ``` </details> <!-- cr-indicator-types:potential_issue --> <!-- cr-comment:v1:d62a549f219f03395624650b --> _Source: Coding guidelines_ <!-- This is an auto-generated reply by CodeRabbit --> ✅ Addressed in commit 012f478"
- PR #205: "_🎯 Functional Correctness_ | _🟡 Minor_ | _⚡ Quick win_ **Guard the progress math for empty checklist fixtures.** The new public fixture prop allows `checklistItems: []`; in that case this computes `0 / 0`, so the progress bar receives `NaN`. Clamp the empty-list case to `0%`. <details> <summary>Suggested diff</summary> ```diff const checklistCount = fixture.checklistItems.filter((item) => checklist[item.key]).length; - const checklistPercent = Math.round((checklistCount / fixture.checklistItems.length) * 100); + const checklistTotal = fixture.checklistItems.length; + const checklistPercent = + checklistTotal === 0 ? 0 : Math.round((checklistCount / checklistTotal) * 100); ``` </details> <!-- suggestion_start --> <details> <summary>📝 Committable suggestion</summary> > ‼️ **IMPORTANT** > Carefully review the code before committing. Ensure that it accurately replaces the highlighted code, contains no missing lines, and has no issues with indentation. Thoroughly test & benchmark the code to ensure it meets the requirements. ```suggestion const checklistCount = fixture.checklistItems.filter((item) => checklist[item.key]).length; const checklistTotal = fixture.checklistItems.length; const checklistPercent = checklistTotal === 0 ? 0 : Math.round((checklistCount / checklistTotal) * 100); ``` </details> <!-- suggestion_end --> <details> <summary>🤖 Prompt for AI Agents</summary> ``` Verify each finding against current code. Fix only still-valid issues, skip the rest with a brief reason, keep changes minimal, and validate. In `@packages/ui/src/MarketingLeadQualificationComposition.tsx` around lines 108 - 109, The progress calculation in MarketingLeadQualificationComposition should handle empty checklist fixtures, since checklistItems can now be an empty array and the current checklistCount/checklistPercent math produces NaN. Update the progress logic around checklistCount and checklistPercent to short-circuit when fixture.checklistItems.length is zero and return 0% instead, while preserving the existing calculation for non-empty fixtures. ``` </details> <!-- fingerprinting:phantom:poseidon:grasshopper --> <!-- cr-indicator-types:potential_issue --> <!-- cr-comment:v1:683dbff63a67526034dcaad9 --> <!-- This is an auto-generated comment by CodeRabbit --> ✅ Addressed in commit 135b13e"
- [+14 more]

### Candidate Rule — Naming

**Why (failure mode):** _⚠️ Potential issue_ | _🟡 Minor_ | _⚡ Quick win_ **Remove story-style fixed root height from the exported composition.*...

**The Rule:** Use descriptive, convention-aligned names for variables, functions, and classes. Favor consistency with existing project naming standards.

**✅ DO:** ```tsx
return (
      <AppShell>
      </AppShell>
```

**❌ DON'T:** ```tsx
return (
    <div style={{ height: 940 }}>
      <AppShell>
      </AppShell>
    </div>
```

**Scope:** alwaysApply: true

*Candidate rule — review before adopting. Frequency=18 in 100 PRs (18.0%). Evidence: 4 PR comments shown above.*

---

## Theme: Error Handling (13 comments, score 11.2)

### Candidate: Handle failure paths explicitly
*Frequency: 13 · Severity signals: high · Representativeness: high*

**Supporting comments:**
- PR #229: "_🩺 Stability & Availability_ | _🟡 Minor_ | _⚡ Quick win_ <details> <summary>🧩 Analysis chain</summary> 🏁 Script executed: ```shell #!/bin/bash set -euo pipefail # Inspect the target component and nearby context. sed -n '1,140p' packages/ui/src/CanonicalStarterTemplateActionForm.tsx # Find other hidden inputs / similar patterns in the repo. rg -n "<input[^>]+type=['\"]hidden['\"][^>]*value=" packages/ui/src packages -g '!**/dist/**' -g '!**/build/**' || true ``` Repository: lambda-curry/saffron-starter Length of output: 2405 --- 🌐 Web query: `React hidden input value without onChange readOnly warning controlled hidden input` 💡 Result: In React, providing a value prop to an input element (including type="hidden") makes it a controlled component [1][2]. Because React expects controlled components to be manageable by the user, it triggers a warning if you provide a value without an onChange handler [1][3]. Even though users cannot interact with hidden inputs [4], React's validation logic treats them like any other input [3][5]. To resolve the read-only warning or uncontrolled-to-controlled warnings, follow these approaches: 1. Use readOnly: If you want to keep the input controlled by React (passing a value prop) but do not need to update it via user interaction, add the readOnly prop to your input [3][6]. This explicitly tells React that the component is intentionally read-only, which silences the warning [3]. <input type="hidden" value={someValue} readOnly /> 2. Use defaultValue: If the value of the hidden input is static or only set once during initialization, use the defaultValue prop instead of value [6][2]. This makes the component uncontrolled, meaning React sets the initial value but does not continue to manage it [2]. <input type="hidden" defaultValue={initialValue} /> 3. Ensure Defined Values: A common source of the warning "changing an uncontrolled input... to be controlled" is passing undefined or null as the value [5][7][8]. Ensure your value prop is always initialized to a string (e.g., an empty string "") or a defined value, rather than undefined [5][8]. Note: Because input type="hidden" elements do not trigger onChange events in the browser [9][4], they are rarely intended to be "controlled" in the same way as text inputs. Using defaultValue is generally the most idiomatic way to handle hidden fields [2]. Citations: - 1: https://react.dev/reference/react-dom/components/input - 2: https://stackoverflow.com/questions/58630365/react-warning-contains-an-input-of-type-hidden-with-value-and-default-value-h - 3: https://github.com/facebook/react/issues/13835 - 4: https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/input/hidden - 5: https://stackoverflow.com/questions/42628486/react-warning-is-changing-an-uncontrolled-input-of-type-hidden-to-be-controlle - 6: https://github.com/facebook/react/issues/7700 - 7: https://github.com/facebook/react/issues/17069 - 8: https://github.com/facebook/react/issues/6222 - 9: https://stackoverflow.com/questions/57943234/hidden-input-doesnt-trigger-reacts-onchange-event --- 🌐 Web query: `React input type="hidden" value readOnly defaultValue warning official docs` 💡 Result: In React, you will encounter warnings related to input components (including type="hidden") if you switch between controlled and uncontrolled states or provide inconsistent prop values. Official documentation and best practices dictate the following: Core Concepts An input is considered controlled if it provides a value prop [1][2]. It is uncontrolled if it uses the defaultValue prop (or no value-related prop at all) to specify an initial value [2]. A component must remain either controlled or uncontrolled for its entire lifetime; it cannot switch between the two [2][3]. Common Warnings and Causes 1. Switching between controlled and uncontrolled: This warning occurs if an input starts without a value (undefined or null) and later receives a value (making it controlled), or vice versa [3][4][5]. To avoid this, ensure your value prop is always defined as a string, number, or an empty string ('') rather than undefined [6][3][5]. 2. Providing both value and defaultValue: You should not provide both value and defaultValue to the same input [7]. If you specify a value, the component is controlled, and the defaultValue is ignored [2]. 3. Missing onChange for controlled inputs: If you provide a value prop without an onChange handler, React will log a warning indicating that the field is read-only [1][2]. If you intend for the field to be read-only, you should explicitly add the readOnly attribute to satisfy the warning [2]. Recommendations for Hidden Inputs Hidden inputs follow the same rules as other input elements [3]. If you need to manage a hidden input's value programmatically: - Controlled approach: Use the value prop, ensure it is always a defined string (e.g., provide '' if the value is missing), and handle updates through your component's state [6][3]. - Uncontrolled approach: Use the defaultValue prop for the initial value and interact with the input's underlying DOM element using a ref if you need to update it later [6]. Note: Unlike standard text inputs, hidden inputs do not trigger the standard onChange event in response to user interaction, as they are not user-interactive fields [8]. If you need to track changes, manage the value in your React state and rely on that state rather than the hidden input's DOM events [8]. Citations: - 1: https://react.dev/reference/react-dom/components/input - 2: https://uk.react.dev/reference/react-dom/components/input - 3: https://stackoverflow.com/questions/42628486/react-warning-is-changing-an-uncontrolled-input-of-type-hidden-to-be-controlle - 4: https://github.com/facebook/react/issues/17069 - 5: https://github.com/facebook/react/issues/6222 - 6: https://stackoverflow.com/questions/54147160/reactjs-warning-component-change-uncontrolled-hidden-field-to-controlled - 7: https://stackoverflow.com/questions/58630365/react-warning-contains-an-input-of-type-hidden-with-value-and-default-value-h - 8: https://stackoverflow.com/questions/57943234/hidden-input-doesnt-trigger-reacts-onchange-event --- </details> **Mark the hidden input readOnly** `value={templateKey}` on a hidden input still triggers React’s read-only warning. Add `readOnly` here since the field is intentionally not editable. <details> <summary>🤖 Prompt for AI Agents</summary> ``` Verify each finding against current code. Fix only still-valid issues, skip the rest with a brief reason, keep changes minimal, and validate. In `@packages/ui/src/CanonicalStarterTemplateActionForm.tsx` at line 46, The hidden template field in CanonicalStarterTemplateActionForm still triggers React’s controlled-input warning because it is given a value without being marked read-only. Update the hidden input that uses templateInputName and templateKey to include readOnly, keeping it intentionally non-editable while preserving the current value binding. ``` </details> <!-- fingerprinting:phantom:poseidon:beignet --> <!-- cr-indicator-types:potential_issue --> <!-- cr-comment:v1:a28ebb284b00c762a3b3086c --> <!-- This is an auto-generated comment by CodeRabbit --> ✅ Addressed in commit e4a5dfd"
- PR #227: "_📐 Maintainability & Code Quality_ | _🟠 Major_ | _⚡ Quick win_ **Formatting check is failing in CI for this file.** Same `fmt:check` failure as the other two files in this cohort; run the formatter before merge. <details> <summary>🧰 Tools</summary> <details> <summary>🪛 GitHub Actions: PR Checks / 1_quality.txt</summary> [error] 1-1: fmt:check failed. vp fmt --check reported format issues in this file. </details> </details> <details> <summary>🤖 Prompt for AI Agents</summary> ``` Verify each finding against current code. Fix only still-valid issues, skip the rest with a brief reason, keep changes minimal, and validate. In `@packages/ui/src/CanonicalStarterTemplateFirstScreenShell.test.tsx` around lines 1 - 81, This test file is failing the formatting check, so update the formatting to match the repo’s formatter output. Re-run the formatter on CanonicalStarterTemplateFirstScreenShell.test.tsx and keep the existing test logic intact; the only change should be stylistic alignment with the rest of the cohort. ``` </details> <!-- fingerprinting:phantom:poseidon:beignet --> <!-- cr-indicator-types:potential_issue --> <!-- cr-comment:v1:2f0981b87a055cbfe4142fb5 --> _Source: Pipeline failures_ <!-- This is an auto-generated comment by CodeRabbit --> ✅ Addressed in commits 8425cc0 to 371f97a"
- PR #227: "_📐 Maintainability & Code Quality_ | _🟠 Major_ | _⚡ Quick win_ **Formatting check is failing in CI for this file.** Pipeline reports `fmt:check` failures here. Run the repo formatter before merge. <details> <summary>🧰 Tools</summary> <details> <summary>🪛 GitHub Actions: PR Checks / 1_quality.txt</summary> [error] 1-1: fmt:check failed. vp fmt --check reported format issues in this file. </details> </details> <details> <summary>🤖 Prompt for AI Agents</summary> ``` Verify each finding against current code. Fix only still-valid issues, skip the rest with a brief reason, keep changes minimal, and validate. In `@packages/ui/src/CanonicalStarterTemplateFirstScreenShell.tsx` around lines 1 - 131, The file’s formatting does not match the repo style, causing fmt:check to fail in CI. Re-run the project formatter on CanonicalStarterTemplateFirstScreenShell and keep the existing logic unchanged; this should normalize imports, type declarations, and JSX spacing to the expected format. ``` </details> <!-- fingerprinting:phantom:poseidon:beignet --> <!-- cr-indicator-types:potential_issue --> <!-- cr-comment:v1:9f65c738e9f94061aea53b9c --> _Source: Pipeline failures_ <!-- This is an auto-generated comment by CodeRabbit --> ✅ Addressed in commits 8425cc0 to 371f97a"
- PR #220: "_🎯 Functional Correctness_ | _🟠 Major_ | _⚡ Quick win_ **Render only one `tabpanel` per selected tab.** `TabsContent` always emits `role="tabpanel"` in `packages/ui/src/Tabs.tsx:223-243`, so Lines 107-141 and Lines 147-178 create two active tabpanels for the same selected tab. That breaks the tabs accessibility contract and makes assistive-tech navigation ambiguous. Keep a single `TabsContent` per tab, and drive the secondary pane from the active tab state with a regular container instead. <details> <summary>🤖 Prompt for AI Agents</summary> ``` Verify each finding against current code. Fix only still-valid issues, skip the rest with a brief reason, keep changes minimal, and validate. In `@packages/ui/src/CanonicalStarterTemplateShowcase.tsx` around lines 103 - 178, The tabs view in CanonicalStarterTemplateShowcase is rendering two active tab panels for the same selection because TabsContent always produces a tabpanel, which breaks accessibility and confuses assistive tech. Update the CanonicalStarterTemplateShowcase layout so only one TabsContent is used per tab key (the template registry loop), and render the marketing/storefront/contentMedia preview area as a normal container driven by the active tab state instead of additional TabsContent instances. ``` </details> <!-- fingerprinting:phantom:poseidon:grasshopper --> <!-- cr-indicator-types:potential_issue --> <!-- cr-comment:v1:43a7d445543a63757b67863e --> <!-- This is an auto-generated comment by CodeRabbit --> ✅ Addressed in commit 6fb59a4"
- [+9 more]

### Candidate Rule — Error Handling

**Why (failure mode):** _🎯 Functional Correctness_ | _🟡 Minor_ | _⚡ Quick win_ **Avoid forcing `"#"` for optional nav links.** Line 273 coerce...

**The Rule:** Handle failure paths explicitly. For async calls and risky operations, wrap with try/catch (or equivalent) and surface actionable errors instead of allowing silent failures.

**✅ DO:** ```tsx
href={item.href}
```

**❌ DON'T:** ```tsx
href={item.href ?? "#"}
```

**Scope:** alwaysApply: true

*Candidate rule — review before adopting. Frequency=13 in 100 PRs (13.0%). Evidence: 4 PR comments shown above.*

---

## Theme: Imports (13 comments, score 9.4)

### Candidate: Use consistent import/export structure and avoid ambiguous barrel patterns when they reduce clarity or create coupling
*Frequency: 13 · Severity signals: medium · Representativeness: high*

**Supporting comments:**
- PR #227: "_📐 Maintainability & Code Quality_ | _🟠 Major_ | _⚡ Quick win_ **Missing dedicated Storybook contract for the new primitive.** `CanonicalStarterTemplateFirstScreenShell` gains source + tests + a barrel export, but only an existing `CanonicalStarterTemplateShowcase` story is repurposed to exercise it — there's no `CanonicalStarterTemplateFirstScreenShell.stories.tsx` of its own. <details> <summary>📦 Suggested follow-up</summary> ``` packages/ui/src/CanonicalStarterTemplateFirstScreenShell.stories.tsx ``` Add a small stories file covering controlled/uncontrolled search-param persistence and the `renderTemplateAction` slot, matching the bundle pattern used for other public primitives. </details> As per coding guidelines, "Public primitives should land as a small bundle: component source in `<Component>.tsx`, behavior coverage in `<Component>.test.tsx`, a Storybook contract in `<Component>.stories.tsx`, and the package export in `packages/ui/src/index.ts`." Also applies to: 67-95 <details> <summary>🤖 Prompt for AI Agents</summary> ``` Verify each finding against current code. Fix only still-valid issues, skip the rest with a brief reason, keep changes minimal, and validate. In `@packages/ui/src/CanonicalStarterTemplateShowcase.stories.tsx` around lines 5 - 7, A dedicated Storybook contract is missing for CanonicalStarterTemplateFirstScreenShell, and the existing CanonicalStarterTemplateShowcase story should not be the only coverage for this new primitive. Add CanonicalStarterTemplateFirstScreenShell.stories.tsx alongside CanonicalStarterTemplateFirstScreenShell, and include stories that exercise controlled and uncontrolled search-param persistence as well as the renderTemplateAction slot, following the same public-primitive bundle pattern used by the component, its test, and the barrel export in index.ts. ``` </details> <!-- fingerprinting:phantom:poseidon:beignet --> <!-- cr-indicator-types:refactor_suggestion --> <!-- cr-comment:v1:b073072f5bf3ba703623624f --> _Source: Coding guidelines_ <!-- This is an auto-generated comment by CodeRabbit --> ✅ Addressed in commits 8425cc0 to 371f97a"
- PR #227: "_📐 Maintainability & Code Quality_ | _🟠 Major_ | _⚡ Quick win_ **Formatting check is failing in CI for this file.** Same `fmt:check` failure reported here as in the shell component; the added export block itself is otherwise correct and consistent with the barrel's existing conventions. <details> <summary>🤖 Prompt for AI Agents</summary> ``` Verify each finding against current code. Fix only still-valid issues, skip the rest with a brief reason, keep changes minimal, and validate. In `@packages/ui/src/index.ts` around lines 229 - 232, The new export block in the barrel file is causing the fmt:check failure because it does not match the repository’s formatting rules. Adjust the existing export for CanonicalStarterTemplateFirstScreenShell and CanonicalStarterTemplateFirstScreenShellProps in index.ts to the formatter’s preferred layout, keeping it consistent with the surrounding barrel exports and the existing module conventions. ``` </details> <!-- fingerprinting:phantom:poseidon:beignet --> <!-- cr-indicator-types:potential_issue --> <!-- cr-comment:v1:013fb713ff6f9022884c9c87 --> _Source: Pipeline failures_ <!-- This is an auto-generated comment by CodeRabbit --> ✅ Addressed in commits 8425cc0 to 371f97a"
- PR #219: "_🎯 Functional Correctness_ | _🟡 Minor_ | _⚡ Quick win_ <details> <summary>🧩 Analysis chain</summary> 🌐 Web query: `In Storybook 10 CSF, are arbitrary named exports from a `.stories.tsx` file treated as stories/docs entries by default?` 💡 Result: In Storybook 10, as with earlier versions of the Component Story Format (CSF), every named export in a `.stories.tsx` file is treated as a story object by default [1][2][3]. Because Storybook processes all named exports as potential stories, you can exclude specific exports (such as helper functions, data objects, or types) by using the `excludeStories` property within your `meta` (the default export) [4]. This prevents Storybook from attempting to render them as stories in the UI. For example, you can configure your meta to ignore certain exports like this: // Button.stories.tsx const meta = { title: 'Button', component: Button, // 👇 Prevents these exports from appearing as stories excludeStories: /.*Data$/, } satisfies Meta<typeof Button>; export default meta; export const myStory = {... }; export const someData = { id: 1 }; // This would be ignored because it matches the regex Citations: - 1: https://storybook.js.org/docs/10.4/api/csf.md - 2: https://storybook.js.org/docs/api/csf - 3: https://storybook.js.org/docs/api/csf.md - 4: https://github.com/storybookjs/storybook/issues/7071 --- 🏁 Script executed: ```shell #!/bin/bash set -euo pipefail # Inspect the story file and nearby conventions. git ls-files 'packages/ui/src/*CanonicalStarterTemplateSurface*' 'packages/ui/src/*stories.tsx' 'packages/ui/src/index.ts' | sed 's#^`#FILE`: #' echo '---' for f in \ packages/ui/src/CanonicalStarterTemplateSurface.stories.tsx \ packages/ui/src/CanonicalStarterTemplateSurface.tsx \ packages/ui/src/index.ts do if [ -f "$f" ]; then echo "### $f" wc -l "$f" sed -n '1,220p' "$f" echo fi done ``` Repository: lambda-curry/saffron-starter Length of output: 18553 --- 🏁 Script executed: ```shell #!/bin/bash set -euo pipefail # Find whether the file declares excludeStories or otherwise suppresses named exports. rg -n "excludeStories|storyName|tags:|storiesOf|export const|export \\{" packages/ui/src/CanonicalStarterTemplateSurface.stories.tsx packages/ui/src -g '*.stories.tsx' -g '*.stories.ts' ``` Repository: lambda-curry/saffron-starter Length of output: 50384 --- 🏁 Script executed: ```shell #!/bin/bash set -euo pipefail # Check how Storybook CSF named exports are used elsewhere in this package. rg -n "^export (const|function|class|\\{)" packages/ui/src -g '*.stories.tsx' | sed -n '1,120p' ``` Repository: lambda-curry/saffron-starter Length of output: 11132 --- 🌐 Web query: `Storybook CSF re-export named export from another module in a .stories.tsx file treated as a story or ignored by the indexer?` 💡 Result: In Storybook, using wildcard re-exports (e.g., `export * from './other.stories'`) in a `.stories.tsx` file is generally not supported and typically results in those stories being ignored by Storybook's indexer [1]. Storybook relies on static analysis to detect stories, and its indexer requires explicit named exports within the file to correctly identify and register them [1][2][3]. Because Storybook does not recursively resolve wildcard re-exports during its static analysis phase, stories exported via `export *` are not processed [1]. For your stories to be correctly indexed and displayed, you must use explicit named exports [1][2]. The following approaches are supported: 1. Explicit Named Exports: Instead of `export * from ...`, explicitly list the stories you wish to re-export [1][2]: `export { Primary, Secondary } from './other.stories';` 2. Direct Definition: Define the stories directly within the file rather than re-exporting them [4][5]. If you use `export * from`, the file itself may be ignored or fail to register the contained stories, leading to them not appearing in the Storybook UI [1][6]. While you can implement a custom indexer to handle non-standard file structures or formats, this is an advanced configuration intended for cases where files are not standard CSF, and it is generally not recommended for resolving standard re-export limitations [7]. Citations: - 1: https://github.com/storybookjs/storybook/issues/32561 - 2: https://github.com/storybookjs/storybook/issues/21734 - 3: https://github.com/storybookjs/storybook/issues/16598 - 4: https://storybook.js.org/docs/api/csf - 5: https://storybook.js.org/docs/writing-stories - 6: https://github.com/storybookjs/storybook/issues/17588 - 7: https://storybook.js.org/docs/api/main-config/main-config-indexers --- 🌐 Web query: `Storybook CSF explicit named re-export of a non-story value from another module in a .stories.tsx file is indexed as a story or ignored?` 💡 Result: In a Storybook.stories.tsx file, any named export (including a named re-export) is generally indexed as a story by Storybook's static analysis by default [1][2]. If the exported value is not intended to be a story, Storybook will attempt to process it as one, which can lead to errors if the value does not conform to the expected story format [1]. To prevent a non-story value—whether it is defined locally or re-exported from another module—from being indexed as a story, you must explicitly exclude it using the includeStories or excludeStories configuration fields in your meta object [3][4][5]. For example: const meta = { component: MyComponent, excludeStories: ['myNonStoryExport'], // 👈 Prevents indexing this export } satisfies Meta<typeof MyComponent>; export { myNonStoryExport } from './other-module'; When you configure excludeStories, Storybook will ignore the specified exports, allowing you to safely import or re-export utility data, mocks, or constants within your story file without them appearing in the Storybook UI or causing parsing errors [3][6]. If you do not provide these configuration fields, Storybook's parser will treat all named exports as potential stories [1]. Citations: - 1: https://github.com/storybookjs/storybook/issues/32620 - 2: https://github.com/storybookjs/storybook/issues/32561 - 3: https://storybook.js.org/docs/api/csf.md - 4: https://github.com/storybookjs/storybook/blob/next/docs/api/csf/index.mdx - 5: https://storybook.js.org/docs/api/csf - 6: https://storybook-5-docs.netlify.app/formats/component-story-format/ --- </details> **Remove the named re-export from `packages/ui/src/CanonicalStarterTemplateSurface.stories.tsx`.** Storybook treats named exports in `.stories.tsx` files as stories by default, so this can surface `CanonicalStarterTemplateSurface` as an unintended entry. If it must stay in the module, add `excludeStories` instead. <details> <summary>🤖 Prompt for AI Agents</summary> ``` Verify each finding against current code. Fix only still-valid issues, skip the rest with a brief reason, keep changes minimal, and validate. In `@packages/ui/src/CanonicalStarterTemplateSurface.stories.tsx` at line 6, Remove the named re-export from CanonicalStarterTemplateSurface.stories.tsx so Storybook does not treat CanonicalStarterTemplateSurface as a story entry; if the symbol needs to remain available from this module, add an appropriate excludeStories setting in the stories file. Locate the exported CanonicalStarterTemplateSurface symbol and keep the file limited to actual story definitions only. ``` </details> <!-- fingerprinting:phantom:poseidon:grasshopper --> <!-- cr-indicator-types:potential_issue --> <!-- cr-comment:v1:9f763b7c89df4a7605c8043b --> <!-- This is an auto-generated comment by CodeRabbit -->"
- PR #205: "_🎯 Functional Correctness_ | _🟡 Minor_ **Use a composite key for recent-lead rows.** `key={lead.company}` is not unique when a company has multiple leads; use the same `company + contact` identity as `isActiveLead` so row state stays attached to the right entry. <details> <summary>Suggested diff</summary> ```diff - key={lead.company} + key={`${lead.company}-${lead.contact}`} ``` </details> <!-- suggestion_start --> <details> <summary>📝 Committable suggestion</summary> > ‼️ **IMPORTANT** > Carefully review the code before committing. Ensure that it accurately replaces the highlighted code, contains no missing lines, and has no issues with indentation. Thoroughly test & benchmark the code to ensure it meets the requirements. ```suggestion {fixture.recentLeads.map((lead, index) => { const isActiveLead = lead.company === fixture.currentLead.company && lead.contact === fixture.currentLead.contact; return ( <TableRow key={`${lead.company}-${lead.contact}`} isSelected={isActiveLead} ``` </details> <!-- suggestion_end --> <details> <summary>🤖 Prompt for AI Agents</summary> ``` Verify each finding against current code. Fix only still-valid issues, skip the rest with a brief reason, keep changes minimal, and validate. In `@packages/ui/src/MarketingLeadQualificationComposition.tsx` around lines 369 - 377, The recent-leads rows in MarketingLeadQualificationComposition’s map are using a non-unique key based only on the company, which can cause row state to move between entries when a company has multiple contacts. Update the TableRow key to use the same composite identity used by isActiveLead (company plus contact) so each lead row is uniquely and consistently identified during renders. ``` </details> <!-- fingerprinting:phantom:poseidon:grasshopper --> <!-- cr-indicator-types:potential_issue --> <!-- cr-comment:v1:a8aca0e0eede4547b081390b --> <!-- This is an auto-generated comment by CodeRabbit --> ✅ Addressed in commit 135b13e"
- [+9 more]

### Candidate Rule — Imports

**Why (failure mode):** _⚠️ Potential issue_ | _🟡 Minor_ | _⚡ Quick win_ **Use a local calendar date literal instead of a UTC timestamp for the...

**The Rule:** Use consistent import/export structure and avoid ambiguous barrel patterns when they reduce clarity or create coupling.

**✅ DO:** ```tsx
const [releaseDate, setReleaseDate] = React.useState<Date | undefined>(
    new Date(2026, 4, 21), // local calendar date: May 21, 2026
  );
```

**❌ DON'T:** ```tsx
const [releaseDate, setReleaseDate] = React.useState<Date | undefined>(
    new Date("2026-05-21T00:00:00Z"),
  );
```

**Scope:** globs: ["**/*.ts", "**/*.tsx", "**/*.js"]

*Candidate rule — review before adopting. Frequency=13 in 100 PRs (13.0%). Evidence: 4 PR comments shown above.*

---

## Theme: Components (8 comments, score 5.5)

### Candidate: Keep component responsibilities narrow
*Frequency: 8 · Severity signals: low · Representativeness: high*

**Supporting comments:**
- PR #227: "_📐 Maintainability & Code Quality_ | _🟠 Major_ | _⚡ Quick win_ **Add a dedicated `CanonicalStarterTemplateFirstScreenShell.stories.tsx`** `CanonicalStarterTemplateFirstScreenShell` is a new public primitive, so it should ship with its own Storybook contract instead of relying on `CanonicalStarterTemplateShowcase.stories.tsx`’s routed proof. Split it out so the component/test/story/export bundle stays complete. <details> <summary>🤖 Prompt for AI Agents</summary> ``` Verify each finding against current code. Fix only still-valid issues, skip the rest with a brief reason, keep changes minimal, and validate. In `@packages/ui/src/CanonicalStarterTemplateFirstScreenShell.tsx` around lines 53 - 131, `CanonicalStarterTemplateFirstScreenShell` is a new public primitive but it does not have its own Storybook contract. Add a dedicated `CanonicalStarterTemplateFirstScreenShell.stories.tsx` that exercises the component directly, using its props and callbacks (including `onSearchParamsChange` and `renderTemplateAction`) rather than relying on `CanonicalStarterTemplateShowcase` stories. Keep the story colocated with the component so the export/story bundle stays complete and the shell’s public API is documented independently. ``` </details> <!-- fingerprinting:phantom:poseidon:beignet --> <!-- cr-indicator-types:refactor_suggestion --> <!-- cr-comment:v1:769d1ef47ca80c046ebb7b44 --> <!-- This is an auto-generated comment by CodeRabbit -->"
- PR #206: "_🎯 Functional Correctness_ | _🟠 Major_ <details> <summary>🧩 Analysis chain</summary> 🌐 Web query: `In React 19, do `false` and `true` children render any DOM output, while `0` still renders as text?` 💡 Result: Yes, that is correct. In React 19, just as in previous versions, boolean values (`false` and `true`) are ignored and do not render any DOM output [1][2][3]. The number `0` continues to be treated as a valid, renderable value and will be rendered as a text node in the DOM [4][5][2]. This behavior is fundamental to how React processes JSX children [1][6]. If you want to prevent `0` from rendering, it is standard practice to convert the expression to a boolean using double negation (`!!0`) or a comparison (`count > 0`) before the logical `&&` operator [4][5][2]. There have been no changes to this specific rendering behavior in React 19 [7][8][9]. Citations: - 1: https://react.wiki/components/react-children/ - 2: https://legacy.reactjs.org/docs/jsx-in-depth.html - 3: https://bobbyhadz.com/blog/react-show-boolean-value - 4: https://github.com/react/react/issues/36245 - 5: https://stackoverflow.com/questions/55377873/when-using-0-in-a-conditional-why-does-the-0-render-in-react - 6: https://stackoverflow.com/questions/75884367/how-to-undetstand-react-keeps-state-for-as-long-as-the-same-component-is-rendere - 7: https://react.dev/blog/2024/12/05/react-19 - 8: https://github.com/facebook/react/releases/tag/v19.0.0 - 9: https://github.com/facebook/react/blob/master/CHANGELOG.md --- 🏁 Script executed: ```shell #!/bin/bash set -euo pipefail git ls-files 'packages/ui/src/StorefrontShell.tsx' 'packages/ui/src/StorefrontShell.test.tsx' 'packages/ui/src/StorefrontShell.stories.tsx' 'packages/ui/src/index.ts' printf '\n--- StorefrontShell.tsx outline ---\n' ast-grep outline packages/ui/src/StorefrontShell.tsx --view expanded || true printf '\n--- StorefrontShell.test.tsx outline ---\n' ast-grep outline packages/ui/src/StorefrontShell.test.tsx --view expanded || true printf '\n--- relevant source excerpt ---\n' sed -n '1,220p' packages/ui/src/StorefrontShell.tsx printf '\n--- relevant test excerpt ---\n' sed -n '1,260p' packages/ui/src/StorefrontShell.test.tsx ``` Repository: lambda-curry/saffron-starter Length of output: 7398 --- 🏁 Script executed: ```shell #!/bin/bash set -euo pipefail python3 - <<'PY' from pathlib import Path p = Path('packages/ui/src/StorefrontShell.tsx') print(p.exists(), p.stat().st_size if p.exists() else None) PY ``` Repository: lambda-curry/saffron-starter Length of output: 176 --- 🏁 Script executed: ```shell #!/bin/bash set -euo pipefail rg -n "hasUtilityBar|hasHero|hasSummary|hasControls|hasAside|hasMainContent|0" packages/ui/src/StorefrontShell.* packages/ui/src -g '!**/*.map' || true ``` Repository: lambda-curry/saffron-starter Length of output: 50385 --- </details> **Treat boolean slot values as absent.** `controls={isOpen && <Button />}` or `aside={false}` still counts as present here, so the shell can render empty wrappers/grid chrome. Keep `0` renderable, but exclude booleans from the `has*` checks and add a `false` regression next to the existing `0` case. <details> <summary>🤖 Prompt for AI Agents</summary> ``` Verify each finding against current code. Fix only still-valid issues, skip the rest with a brief reason, keep changes minimal, and validate. In `@packages/ui/src/StorefrontShell.tsx` around lines 43 - 49, The has* slot checks in StorefrontShell treat boolean values as present, which can cause empty wrappers/grid chrome to render for props like controls={isOpen && <Button />} or aside={false}. Update the presence logic for the slot flags in StorefrontShell so booleans are excluded while still allowing 0 to count as renderable, and add a regression test alongside the existing 0 case to verify false is treated as absent. ``` </details> <!-- fingerprinting:phantom:poseidon:grasshopper --> <!-- cr-indicator-types:potential_issue --> <!-- cr-comment:v1:b8446a0ea416e0883a2a2a4a --> _Source: Coding guidelines_ <!-- This is an auto-generated comment by CodeRabbit --> ✅ Addressed in commit ea4e95d"
- PR #205: "_🎯 Functional Correctness_ | _🟠 Major_ <details> <summary>🧩 Analysis chain</summary> 🏁 Script executed: ```shell #!/bin/bash set -euo pipefail # Map the target file and nearby tests/storybook to inspect behavior around fixture changes. ast-grep outline packages/ui/src/MarketingLeadQualificationComposition.tsx --view expanded || true printf '\n--- FILE (selected lines) ---\n' sed -n '1,220p' packages/ui/src/MarketingLeadQualificationComposition.tsx printf '\n--- FILE (selected lines 220-460) ---\n' sed -n '220,460p' packages/ui/src/MarketingLeadQualificationComposition.tsx printf '\n--- TEST/STORY FILES ---\n' fd -a 'MarketingLeadQualification*' packages/ui/src || true ``` Repository: lambda-curry/saffron-starter Length of output: 20247 --- 🏁 Script executed: ```shell #!/bin/bash set -euo pipefail # Inspect usages and any tests for this composition to see whether fixture changes are handled. rg -n "MarketingLeadQualificationComposition|defaultQualificationFixture|createChecklistState|qualificationState|checklistItems" packages/ui/src -g '!**/dist/**' -g '!**/build/**' ``` Repository: lambda-curry/saffron-starter Length of output: 9368 --- 🏁 Script executed: ```shell #!/bin/bash set -euo pipefail # Read the targeted test and story files to see whether fixture changes and empty checklist cases are already covered. printf '\n--- TEST FILE ---\n' sed -n '1,220p' packages/ui/src/MarketingLeadQualificationComposition.test.tsx printf '\n--- STORY FILE ---\n' sed -n '1,180p' packages/ui/src/MarketingLeadQualificationComposition.stories.tsx printf '\n--- FIXTURE FILE (relevant section) ---\n' sed -n '1,240p' packages/ui/src/fixtures/marketingLeadQualification.ts ``` Repository: lambda-curry/saffron-starter Length of output: 14565 --- 🏁 Script executed: ```shell #!/bin/bash set -euo pipefail # Read the targeted test and story files to see whether fixture changes and empty checklist cases are already covered. printf '\n--- TEST FILE ---\n' sed -n '1,220p' packages/ui/src/MarketingLeadQualificationComposition.test.tsx printf '\n--- STORY FILE ---\n' sed -n '1,180p' packages/ui/src/MarketingLeadQualificationComposition.stories.tsx printf '\n--- FIXTURE FILE (relevant section) ---\n' sed -n '1,240p' packages/ui/src/fixtures/marketingLeadQualification.ts ``` Repository: lambda-curry/saffron-starter Length of output: 14565 --- </details> **Reset the editable state when `fixture` changes.** The local `useState`/`defaultValue` setup only seeds from `fixture` on mount, so rerenders with a different fixture keep the previous company/contact, selects, and checklist while the surrounding content updates. Re-seed the editable fields from `fixture` or remount the composition on fixture changes, and add a rerender test for that path. <details> <summary>🤖 Prompt for AI Agents</summary> ``` Verify each finding against current code. Fix only still-valid issues, skip the rest with a brief reason, keep changes minimal, and validate. In `@packages/ui/src/MarketingLeadQualificationComposition.tsx` around lines 95 - 106, The editable state in MarketingLeadQualificationComposition is only initialized from fixture on mount, so changing fixture later leaves qualificationState, teamSize, timeline, and checklist stale while the rest of the UI updates. Update the component to re-seed or reset those local states whenever fixture changes, or make the composition remount on fixture identity changes; use the existing MarketingLeadQualificationComposition and createChecklistState setup as the fix point, and add a rerender test that switches fixtures and verifies the fields update. ``` </details> <!-- fingerprinting:phantom:poseidon:grasshopper --> <!-- cr-indicator-types:potential_issue --> <!-- cr-comment:v1:f845c668054a8ce1b4d6c820 --> _Source: Coding guidelines_ <!-- This is an auto-generated comment by CodeRabbit --> ✅ Addressed in commit 135b13e"
- PR #171: "_⚠️ Potential issue_ | _🟡 Minor_ | _⚡ Quick win_ **Make “Team size” actually interactive or explicitly non-interactive.** `value="25-50"` with `onValueChange={() => {}}` makes the select appear editable but ignores user changes. Either wire state (`useState`) or mark it disabled/read-only in the UI. <details> <summary>Suggested fix</summary> ```diff + const [teamSize, setTeamSize] = React.useState("25-50"); ... <Select label="Team size" - value="25-50" - onValueChange={() => {}} + value={teamSize} + onValueChange={setTeamSize} hint="Keep rough implementation scale visible without needing CRM data." > ``` </details> <details> <summary>🤖 Prompt for AI Agents</summary> ``` Verify each finding against current code. Fix only still-valid issues, skip the rest with a brief reason, keep changes minimal, and validate. In `@packages/ui/src/MarketingLeadQualificationComposition.stories.tsx` around lines 284 - 289, The Select instance rendering "Team size" is currently controlled (value="25-50") but ignores user input because onValueChange is a no-op; either make it truly controlled by adding local state (e.g., useState and pass setter to onValueChange) for the Select used in MarketingLeadQualificationComposition.stories.tsx, or mark the Select as non-interactive by adding disabled/readOnly (or removing value and using defaultValue) so the UI accurately reflects its behavior; update the Select props and any surrounding story state setup (useState hook) or add disabled/readOnly accordingly. ``` </details> <!-- fingerprinting:phantom:poseidon:hawk --> <!-- This is an auto-generated comment by CodeRabbit --> ✅ Addressed in commit 315daf5"
- [+4 more]

### Candidate Rule — Components

**Why (failure mode):** _🎯 Functional Correctness_ | _🟠 Major_ <details> <summary>🧩 Analysis chain</summary> 🏁 Script executed: ```shell #!/...

**The Rule:** Keep component responsibilities narrow. Extract complex logic/hooks and avoid patterns that make render behavior hard to reason about.

**✅ DO:** ```tsx
export interface StorefrontProductDetailThumbnailProps extends React.ButtonHTMLAttributes<HTMLButtonElement> {
```

**❌ DON'T:** ```shell
#!/bin/bash
# Description: Check if other button components in the codebase use ButtonHTMLAttributes

# Search for button component prop interfaces to establish consistency
rg -n "extends React\.(Button)?HTMLAttributes<HTMLButtonElement>" --type=ts --type=tsx
```

**Scope:** globs: ["**/*.tsx", "**/*.jsx"]

*Candidate rule — review before adopting. Frequency=8 in 100 PRs (8.0%). Evidence: 4 PR comments shown above.*

---

## Theme: Documentation (6 comments, score 3.7)

### Candidate: Document intent when behavior is non-obvious
*Frequency: 6 · Severity signals: low · Representativeness: high*

**Supporting comments:**
- PR #210: "`@jaruesink`, thanks for the update — that addresses my concern. <!-- <review_comment_addressed> --> <!-- This is an auto-generated reply by CodeRabbit -->"
- PR #176: "`@jaruesink`, thanks for verifying — future-neutral labels (`Next Thu`, `Next Fri`, `Next Mon`) look good, and green CI confirms everything is consistent. (∩｀-´)⊃━☆ﾟ.* <!-- <review_comment_addressed> --> <!-- This is an auto-generated reply by CodeRabbit -->"
- PR #148: "`@jaruesink` — Thanks for confirming! You're right, the fix was already applied in the prior commit. The current code correctly extends `React.ButtonHTMLAttributes<HTMLButtonElement>` — this comment can be dismissed. <!-- <review_comment_addressed> --> <!-- This is an auto-generated reply by CodeRabbit -->"
- PR #138: "`@jaruesink`, thanks for the clarification — that context makes sense. I'll record the CODEOWNERS convention so future reviews don't surface this false positive again. (ﾐ･ω･ﾐ) <!-- <review_comment_addressed> --> --- <details> <summary>✏️ Learnings added</summary> ``` Learnt from: jaruesink Repo: lambda-curry/saffron-starter PR: 138 File: packages/ui/src/StorefrontProductCard.tsx:1-1 Timestamp: 2026-05-10T17:34:23.029Z Learning: In the `saffron-starter` repository, `CODEOWNERS` explicitly documents that Meg and Scout do not have GitHub accounts yet. Design-layer ownership for `packages/ui/**`, `packages/design-tokens/**`, `packages/design-system/**`, and `docs/design/**` is informational only — all PR review traffic is routed through `jaruesink`. Do not flag missing design reviewer assignments as an issue in this repo. ``` > Note: Learnings are effective only in the context of similar code segments. To apply general coding guidelines, please configure [review instructions](https://docs.coderabbit.ai/guides/review-instructions). You can manage existing learnings in the [app](https://app.coderabbit.ai/learnings). </details> <!-- This is an auto-generated reply by CodeRabbit -->"
- [+2 more]

### Candidate Rule — Documentation

**Why (failure mode):** `@jaruesink`, thanks for the update — that addresses my concern

**The Rule:** Document intent when behavior is non-obvious. Keep README/comments aligned with current code behavior.

**✅ DO:** ```
// Example: [insert preferred pattern here]
```

**❌ DON'T:** ```tsx
Learnt from: jaruesink
Repo: lambda-curry/saffron-starter PR: 138
File: packages/ui/src/StorefrontProductCard.tsx:1-1
Timestamp: 2026-05-10T17:34:23.029Z
Learning: In the `saffron-starter` repository, `CODEOWNERS` explicitly documents that Meg and Scout do not have GitHub accounts yet. Design-layer ownership for `packages/ui/**`, `packages/design-tokens/**`, `packages/design-system/**`, and `docs/design/**` is informational only — all PR review traffic is routed through `jaruesink`. Do not flag missing design reviewer assignments as an issue in this repo.
```

**Scope:** alwaysApply: true

*Candidate rule — review before adopting. Frequency=6 in 100 PRs (6.0%). Evidence: 4 PR comments shown above.*

---

## Skipped (low signal)
- 0 comments below length threshold
- 0 pure approvals
- 0 questions without suggestions
